## 1. Persistence and Backfill

- [x] 1.1 Audit existing rows against the expected former BGG bindings, then add required `games.slug` through a reversible forward migration: add nullable column, backfill all nineteen exact verified former registry slugs, abort on mapping drift, add canonical DNS-safe length/domain and uniqueness constraints, then make the column non-null.
- [x] 1.2 Update migration/data assertions for all nineteen slug/BGG/stage/engine bindings, canonical and unique TypeIDs, canonical and unique slugs, preserved K-sort order, direct-write constraint failures, and a later autogenerated TypeID.
- [x] 1.3 Keep the reversible string-backed `users.role` enum migration with `user` and `admin`, a `user` default, and a named database domain constraint without promoting an existing account.
- [x] 1.4 Verify account role values/default, trusted administrator assignment, forged-role non-escalation, named database constraint, and reversible migration behavior already delivered by this change.

## 2. Persisted Game Domain

- [x] 2.1 Add required `slug` to `D20.Games.Game`, define its type and canonical validation, and keep `Game.id()` as the owning environment-local TypeID type.
- [x] 2.2 Separate game create and update changesets so create requires slug and BGG id while ordinary updates cannot mutate slug; map unique/check constraints for slug, BGG, stage, and engine.
- [x] 2.3 Add persisted lookup by slug for browser/catalog boundaries while retaining TypeID lookup for Session, Workspace, module, socket, and token boundaries.
- [x] 2.4 Preserve the single launch predicate combining enabled, stage, environment policy, supported engine, and `D20.Game.ensure_engine/1`.
- [x] 2.5 Keep `D20.Games.Registry` and its application configuration removed; do not restore a compatibility registry.
- [x] 2.6 Update context tests for id and slug lookup, missing/invalid/duplicate slug, immutable updates, stage/order cases, BGG edits, metadata fallback, launch policy, and engine enum behavior.
- [x] 2.7 Keep deployed engine modules sourced directly from the permanent `D20.Games.Game` Ecto.Enum mappings.

## 3. Stable Game Identity in Session Runtime

- [x] 3.1 Keep `D20.Sessions` create/state/list functions carrying `Game.id()` TypeIDs and capturing the resolved engine at process creation.
- [x] 3.2 Keep `D20.Sessions.Server`, custom servers, callbacks, publications, and state tuples TypeID-based without re-reading mutable launch or engine fields.
- [x] 3.3 Keep `D20.Accounts.Scope`, ModuleSocket, and SessionChannel authorization bound to the TypeID captured by the Session.
- [x] 3.4 Keep focused Session, server, channel, attachment-registry, and game-specific tests proving TypeID association, mismatch rejection, attachment lifecycle, and captured-engine behavior.

## 4. Slug-Based Browser Routes

- [x] 4.1 Restore `GET /games/:slug` and `POST /games/:slug/sessions` as the only public game detail and page Session-creation routes; do not add id-plus-slug variants or TypeID compatibility redirects.
- [x] 4.2 Refactor `PageController` to resolve persisted slug, emit both string `id` and `slug` plus stage, match selected Sessions by resolved TypeID, and return 404 for unknown slugs independently of BGG metadata.
- [x] 4.3 Keep Session creation and redirects on `/games/:slug`, resolve the row before engine/launch validation, and create the Session under its TypeID.
- [x] 4.4 Update controller tests for canonical slug links, unknown slug, TypeID-as-slug rejection, metadata outage, disabled launch, matching/mismatched Session queries, and unchanged existing-Session behavior after mutable edits.

## 5. Module and Workspace Contracts

- [x] 5.1 Keep ModuleController routes at `OPTIONS` and `POST /modules/:game_id`, TypeID casting, TypeID claims, mismatch rejection, and existing-Session bootstrap semantics.
- [x] 5.2 Change `D20Web.Module` framing to derive iframe host and allowed origin from persisted `<slug>.<shell-host>` while preserving scheme, port, CORS, sandbox, endpoint, topic, actor, expiry, and TypeID token authority.
- [x] 5.3 Keep Workspace and WorkspaceChannel descriptors authoritative as `{id, game_id, phase, module, connection}` without a duplicate top-level slug.
- [x] 5.4 Resolve the persisted game by captured TypeID when constructing Workspace framing and use its immutable slug for module origin without filtering existing Sessions by enabled, stage, BGG, or current engine.
- [x] 5.5 Update module, token, ModuleController, SessionChannel, Workspace, and WorkspaceChannel tests for slug hosts with TypeID claims, unknown records, CORS, monitoring, close, reconnect, edit-insensitive discovery, and finished results.
- [x] 5.6 Keep static developer-spec slugs decoupled from persisted game lookup and keep `workspace.yaml` non-public.

## 6. Public Contracts and Frontend Navigation

- [x] 6.1 Keep `priv/specs/workspace.yaml` requiring canonical TypeID `game_id` in Workspace descriptors; the persisted catalog slug does not replace live connection authority.
- [x] 6.2 Update shared frontend catalog/detail types and Inertia props to carry both `id` and `slug`, retain `stage`, and keep runtime metadata separate.
- [x] 6.3 Restore slug URLs for home tiles, launch form submission, successful/error redirects, and Lobby cleanup while retaining TypeID for Session matching and Workspace state.
- [x] 6.4 Keep Workspace TypeScript descriptors and state keyed by Session id with TypeID `gameId`; do not derive or normalize slug in the client Workspace.
- [x] 6.5 Update home, game, launch form, Lobby, Workspace model/component, and browser fixtures/tests for slug navigation, TypeID Session authority, stage badges, disabled Play hiding, and unchanged Session/window interactions.

## 7. Backpex Dependency and Asset Integration

- [x] 7.1 Keep Backpex `~> 0.20.0`, PubSub, formatter, Bun local dependency, LiveSocket hooks, Tailwind sources, and existing Vite/Svelte asset integration.
- [x] 7.2 Keep the shared Backpex admin layout using existing root document, flash, themes, navigation, responsive shell, and accessibility treatment.

## 8. Admin Resource and Authorization

- [x] 8.1 Extend `D20Web.Admin.GameLive` from update-only to new/index/show/edit, with required slug on new, generated read-only TypeID, read-only persisted slug, mutable BGG/stage/enabled/engine fields, and no delete action.
- [x] 8.2 Authorize `:new` and `:create` through `D20.Games.Policy/:manage_games` alongside index/show/edit and continue denying unsupported actions.
- [x] 8.3 Keep the protected `/dashboard` `live_session`, Backpex preferences, direct game index mount, HTTP administrator Plug, and LiveView administrator hook.
- [x] 8.4 Update Backpex tests for authorized creation, defaults, invalid/duplicate slug, generated TypeID, immutable post-create slug, mutable inline/form updates, absent delete, and anonymous/non-admin denial.
- [x] 8.5 Ensure every account registration, completion, settings, identity, and ordinary update changeset ignores `role`, and retain tests proving forged `role = admin` parameters do not escalate.

## 9. Trusted Administrator Assignment

- [x] 9.1 Keep administrator assignment and revocation outside application and release interfaces.
- [x] 9.2 Document trusted production and development database assignment and revocation, including prerequisites and affected-account verification, without adding HTTP or environment configuration.

## 10. Targeted Validation

- [x] 10.1 Format touched Elixir/HEEx files and run focused migration, Games, controller, module/token, Session/channel, Workspace, Backpex, and AsyncAPI tests.
- [x] 10.2 Run focused frontend tests for slug-based home/detail/Lobby navigation and TypeID-based Workspace behavior.
- [x] 10.3 Run `mix assets.lint`, `mix assets.test`, `mix typecheck`, and `mix assets.build` for revised catalog and iframe contracts.
- [x] 10.4 Run `openspec validate --all --strict --no-interactive` and `mix openspec.check` after artifact and contract updates.

## 11. Browser, Migration, and Operational Verification

- [x] 11.1 Verify slug-based home/detail navigation, Session creation, redirects, Lobby cleanup, metadata fallback, and responsive behavior through the configured browser tooling.
- [x] 11.2 Verify administrator game creation with required slug, post-create immutability, mutable edits, validation errors, keyboard operation, visible focus, and Backpex responsiveness.
- [x] 11.3 Start a Session, then disable the game and edit BGG id, stage, and engine; verify the existing Session, slug iframe, channel, Workspace, TypeID claim, and captured engine remain unchanged while new launch is blocked.
- [x] 11.4 Verify existing `koala-rescue-club.localhost` and `next-station-london.localhost` Traefik aliases load their iframe clients with no external client-repository changes.
- [ ] 11.5 In a separately authorized Infra repository change, retain the explicit canonical slug set, remove `static_pages_game_id_aliases` and their exact Pages/DNS resources, apply Terraform validation, and verify all implemented slug domains before removing TypeID aliases in production.
- [x] 11.6 Record backend-query-driven Infra reconciliation as a linked follow-up requiring an authenticated versioned registry contract, failure-closed snapshot validation, deletion protection, and bootstrap strategy; do not implement it in this change.
- [ ] 11.7 Rehearse application rollback with the additive slug column and slug infrastructure retained; restore TypeID aliases only if the preceding application release must temporarily serve TypeID iframe hosts.

## 12. Broad Completion and Archive

- [x] 12.1 Run `just check` and resolve every failure attributable to the corrected catalog identity contract.
- [x] 12.2 Review the final diff against issue #223 and every delta requirement, confirming required immutable slug, environment-local TypeID runtime identity, no persisted BGG presentation data, no dynamic engine loading, and no backend-driven Infra query entered current scope.
- [ ] 12.3 Reconcile issue acceptance criteria, OpenSpec tasks, external Infra evidence, and rollback notes; then archive `manage-persisted-game-catalog` only after all required implementation and verification work is complete.
- [ ] 12.4 Run strict OpenSpec validation after archive and confirm the change no longer appears in `openspec list --json` before reporting issue #223 complete.

## Detail contract reconciliation with #272 (2026-09-09)

The game-detail delta now preserves the later internal provider fallback and context-supplied string route slugs from `unify-game-listing-options`, including its combined local/provider requirement name. #272 owns that rename, implementation, and validation. Historical tasks 4.2 and 6.2 describe the earlier persisted-only state; their detail-routing and catalog-envelope assumptions are superseded by #272 while local TypeID/Session authority remains unchanged. This reconciliation does not complete or change the pending Infra, rollback, and archival tasks above.

The #272 raw-string continuation inlines context fallback and delegates unmatched string values to the provider without input regex/parsing; returned BGG identity determines the detail slug. The shared game-detail delta follows this refinement.
