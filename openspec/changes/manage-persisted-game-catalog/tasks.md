## 1. Persistence and Backfill

- [x] 1.1 Revise the reversible migration so `games` uses an explicit string primary key with canonical `game` TypeID constraint, plus `bgg_id`, string-backed `stage`, `enabled`, integer-backed `engine`, UTC timestamps, the unique BGG index, and named BGG/stage/engine constraints from the design.
- [x] 1.2 Backfill every current `config/config.exs` entry with a generated environment-local `game` TypeID, exact BGG, `planned`/`in_development`/`released` stage, enabled, and engine values; generate strictly increasing TypeID timestamps in former registry order and remove bigint sequence handling.
- [x] 1.3 Add a reversible string-backed `users.role` enum migration with `user` and `admin`, a `user` default, and a named database domain constraint without promoting any existing account.
- [x] 1.4 Add migration/data assertions that query all nineteen bindings, canonical and unique `game` TypeIDs, preserved K-sort order, id-prefix constraint, unique BGG behavior, engine integers, enabled defaults, and a later autogenerated TypeID.
- [x] 1.5 Verify the account role enum values and default, trusted administrator assignment, forged-role non-escalation, named database constraint, and reversible migration.

## 2. Persisted Game Domain

- [x] 2.1 Convert `D20.Games.Game` to the canonical string-backed TypeID primary key with prefix `game`, make `Game.id()` the owning id type without context proxy aliases, retain the exact permanent engine enum mappings, and keep BGG presentation fields in `D20.Games.Metadata`.
- [x] 2.2 Implement Game update validation and database constraint mapping for positive unique BGG ids, required stage/enabled, supported engines, and the released/in-development engine invariant.
- [x] 2.3 Refactor `D20.Games` into the TypeID-based persistence and enrichment interface, relying on TypeID/Ecto primary-key casting and Phoenix.Ecto HTTP exception mapping, persisted listing, update/change operations, stage/TypeID ordering, runtime BGG enrichment, and fallback metadata without public game slug derivation.
- [x] 2.4 Implement the single launch predicate combining enabled, implementation stage, environment policy, supported engine, and `D20.Game.ensure_engine/1` semantics.
- [x] 2.5 Remove `D20.Games.Registry`, its application game configuration, compatibility lookups, and registry-specific tests; replace callers and fixtures with persisted game records.
- [x] 2.6 Cover context behavior for canonical, missing, malformed, and wrong-prefix TypeID lookup; all stage/order cases; BGG id edits; metadata fallback without slug fields; launch predicate combinations; and engine enum load/dump/validation.
- [x] 2.7 Expose the deployed engine modules as `D20.Games.Game.engines/0` directly from `Ecto.Enum.values/2` without embedding admin presentation labels in the persisted schema.
- [x] 2.8 Use the concise persisted field and catalog-entry key `stage` consistently across the Game schema, context, controllers, migration constraints, and focused tests.
- [x] 2.9 Rename lifecycle atoms to `planned`, `in_development`, and `released` across persistence, launch policy, catalog contracts, UI labels, and focused tests while keeping `enabled` as the independent new-Session gate.

## 3. Stable Game Identity in Session Runtime

- [x] 3.1 Change `D20.Sessions` create/state/list specifications and functions to carry `Game.id()` TypeIDs directly, rely on persisted lookup instead of a duplicate create guard, and continue capturing the selected engine at process creation.
- [x] 3.2 Change `D20.Sessions.Server` options, state tuples, callbacks, get replies, publications, and helper paths to carry caller-validated `game` TypeIDs without repeating TypeID guards or re-reading current game launch or engine fields.
- [x] 3.3 Update Koala Rescue Club, Next Station: London, and shared test custom servers and fixtures to carry `game` TypeIDs through custom state-machine callbacks without altering gameplay, timers, Presence, or idle behavior.
- [x] 3.4 Change `D20.Accounts.Scope.game` and `put_game/2` to use the Game schema's TypeID type supplied by verified token or Session boundaries, and update SessionChannel joins to authorize full TypeID claims and ordinary socket scope against the id captured by the Session.
- [x] 3.5 Update Session, server, channel, attachment-registry, and game-specific tests to prove TypeID state/get/list shapes, wrong-prefix and id mismatch rejection, attachment lifecycle, and captured-engine behavior after an operator edits the persisted row.

## 4. Id-Only HTTP Routes

- [x] 4.1 Replace public detail routes with only `GET /games/:game_id`, replace page Session creation with `POST /games/:game_id/sessions`, and remove both slug-only and id-plus-slug route variants.
- [x] 4.2 Refactor `PageController` to fetch by full `game` TypeID, enrich metadata, emit string id and implementation stage without a slug prop, preserve Session query matching by game id, return 404 for a missing valid TypeID, and use the standard Phoenix.Ecto 400 response for malformed or wrong-prefix TypeIDs.
- [x] 4.3 Remove cosmetic slug canonicalization and keep all success, error, Session-query, and lobby navigation on the single `/games/:game_id` detail route.
- [x] 4.4 Enforce the complete launch predicate before engine resolution and process creation in `PageController`, preserve forbidden/validation response semantics, and redirect success and errors through id-based detail navigation.
- [x] 4.5 Extend controller tests for TypeID-only links, rejection of removed slug variants, missing valid and malformed/wrong-prefix ids, migrated catalog string props without slug, TypeID-based Session matching, disabled launch, and existing Session access after all supported game edits.

## 5. Module and Workspace Contracts

- [x] 5.1 Keep ModuleController routes at `OPTIONS` and `POST /modules/:game_id`, resolve only persisted `game` TypeIDs, reject malformed/wrong-prefix ids, enforce launch policy only in the new-Session branch, and preserve existing matching Session bootstrap regardless of current enabled/stage/engine/BGG fields.
- [x] 5.2 Change `D20Web.Module` framing and connection interfaces to trust caller-resolved `game` TypeIDs and generate stable DNS-safe `game-<typeid-suffix>.<shell-host>` iframe hosts while preserving scheme, port, CORS, sandbox, endpoint, and topic behavior.
- [x] 5.3 Replace signed module `slug` with the full string TypeID `game_id`, update prefix validation, ModuleSocket scope/socket ids, and SessionChannel mismatch authorization without changing actor or expiry semantics.
- [x] 5.4 Change `D20Web.Workspace` and WorkspaceChannel snapshots to resolve persisted games by captured TypeID, emit `{id, game_id, phase, module, connection}`, and never filter an existing Session by enabled, stage, BGG, or current engine.
- [x] 5.5 Update module, token, ModuleController, SessionChannel, Workspace, and WorkspaceChannel tests for full TypeID claims, malformed/wrong-prefix and id mismatch rejection, DNS-safe suffix host, disabled existing bootstrap, edit-insensitive discovery, CORS, monitoring, close, reconnect, and finished results.
- [x] 5.6 Decouple `D20Web.Plugs.AsyncApi` and pending developer-spec discovery from the removed Registry so validated static game-spec identifiers remain document identifiers and `workspace.yaml` remains non-public.

## 6. Public AsyncAPI and Frontend Navigation

- [x] 6.1 Update `priv/specs/workspace.yaml` to require string `game_id` matching the canonical `game` TypeID pattern instead of `slug`, retain Session id/phase/module/connection/close shapes, and update focused contract assertions.
- [x] 6.2 Update shared frontend game types and page props to expose stable game identity as string `id` and implementation stage as `stage`; remove public game slug fields, the optional status, `gameId`/`implementationStage` page naming, numeric-id assumptions, and barrel proxies.
- [x] 6.3 Use direct id-only URLs for home tiles, lobby cleanup, and launch-form submission; remove the cosmetic game-route helper rather than retaining a one-line abstraction.
- [x] 6.4 Update Workspace TypeScript descriptors and state tests from `slug` to string TypeID `gameId` without adding normalization, presentation names, browser persistence, or a second state owner.
- [x] 6.5 Update home, game, launch form, lobby, Workspace model/component, and browser fixtures/tests for TypeID-only links, an `In development` badge without a redundant `Planned` badge, disabled Play hiding, and unchanged Session/window interactions.

## 7. Backpex Dependency and Asset Integration

- [x] 7.1 Add compatible Backpex `~> 0.20.0`, resolve and review Mix lock changes, configure `D20.PubSub`, and import the Backpex formatter configuration without changing Phoenix, LiveView, Tailwind, daisyUI, or Heroicons major versions.
- [x] 7.2 Add Backpex as a Bun local file dependency, update `bun.lock`, merge Backpex hooks and `backpexParams` into the existing shared LiveSocket, and keep the Inertia/Svelte bootstrap and colocated hooks intact.
- [x] 7.3 Add Backpex Elixir/JS Tailwind sources to `assets/css/app.css` and verify the existing Tailwind 4, daisyUI 5, Heroicons, light/dark themes, and Vite production build include the admin styles.
- [x] 7.4 Add a small Backpex admin layout that uses the existing root document, flash rendering, D20/home and logout navigation, responsive shell, accessible labels/focus, and no separate asset or LiveSocket stack.

## 8. Admin Resource and Authorization

- [x] 8.1 Update `D20Web.Admin.GameLive` as an index/show/edit-only Backpex resource with read-only textual `game` TypeID and inline-editable BGG id, stage, enabled, and nullable engine fields.
- [x] 8.2 Build humanized implementation-stage options from Ecto.Enum metadata, pass the module atoms from `D20.Games.Game.engines/0` directly to the engine Select, provide a null engine choice, and deny new/delete actions defensively.
- [x] 8.3 Wrap the protected `/dashboard` scope in `live_session :dashboard`, add the Backpex preferences route, mount the game LiveResource directly at the dashboard root without a proxy controller or redirect, and run the `D20Web.Auth` `:admin` on-mount hook before `Backpex.InitAssigns`.
- [x] 8.4 Add the shared `D20Web.Auth` HTTP function Plug and on-mount administrator checks, apply the function Plug directly to the protected scope, re-resolve the persisted account during LiveView mount, and keep `D20.Games.Policy/:manage_games` only at the game resource capability boundary.
- [ ] 8.5 Ensure every account registration, completion, settings, identity, and ordinary update changeset ignores `role`, and add tests proving forged `role = admin` parameters retain the default `user` role.
- [x] 8.6 Add focused HTTP, LiveView, route, and resource-policy tests for anonymous, non-admin, and administrator access; path-agnostic full-page redirect after successful Inertia authentication; Backpex preferences plus list/show/edit authorization; textual TypeID display and routing; enum options; valid inline and form updates; invalid constraints; and absent create/delete actions.

## 9. Trusted Administrator Assignment

- [x] 9.1 Keep administrator assignment and revocation outside application and release interfaces; remove role-assignment helpers, email environment input, overlay commands, and dedicated application bootstrap tests.
- [ ] 9.2 Document trusted production and development database assignment and revocation, including prerequisites and affected-account verification, without adding an HTTP path or environment configuration.

## 10. Targeted Validation

- [x] 10.1 If the undeployed draft bigint games migration ran locally, roll back that migration only; then run the revised migration and repository asset setup without resetting or dropping unrelated development data.
- [x] 10.2 Format every touched Elixir/HEEx file with `mix format <files>` and rerun focused TypeID migration, Games, Accounts/Auth, Session/server, controller, module/token, channel, Workspace, Backpex LiveView, release, and AsyncAPI tests with `mix test <test-paths>`.
- [x] 10.3 From `assets/`, rerun focused Vitest suites for TypeID-based home, game, lobby, Workspace model/UI/browser behavior with `bun run test -- <paths>`.
- [x] 10.4 Rerun `mix assets.lint`, `mix assets.test`, `mix typecheck`, and `mix assets.build` to validate string TypeID contracts across Backpex and the existing Inertia/Svelte application.
- [ ] 10.5 Run `openspec validate --all --strict --no-interactive` and `mix openspec.check` after TypeID contract and artifact updates.

## 11. Browser, Migration, and Operational Verification

- [ ] 11.1 With migrations applied and BGG available, use the configured browser tooling to verify home/detail id-only navigation, rejection of removed slug variants, Session creation, and lobby cleanup at supported desktop and narrow viewports.
- [ ] 11.2 Verify anonymous login redirection, ordinary-user denial, administrator list/edit flows with textual `game` TypeIDs, enum and null-engine controls, validation errors, keyboard operation, visible focus, readable light/dark styling, and Backpex responsiveness under `/dashboard`.
- [ ] 11.3 Start a Session, then disable the game and edit its BGG id, stage, and engine; verify the existing Session, iframe, channel, Workspace, and captured engine remain unchanged while both HTTP boundaries reject a new disabled Session and later enabled creation uses the edited engine.
- [ ] 11.4 Provision and verify wildcard `game-<typeid-suffix>` host routing and coordinate full-TypeID `/modules/:game_id` consumption for each deployed iframe module before application cutover; record any external repository work separately rather than editing it under this change.
- [ ] 11.5 Rehearse the non-destructive application rollback with additive tables retained, export the persisted game/admin assignments before any schema rollback, and verify the reversible down migrations only with new-code traffic stopped.

## 12. Broad Completion and Archive

- [ ] 12.1 Run `just check` and record backend, frontend, contract, formatting, lint, typecheck, Storybook, and build results; resolve every failure attributable to the change.
- [ ] 12.2 Review the final diff against issue #223 and every delta requirement, confirm no persisted BGG metadata, public game slug, dynamic code loading, audit/reason fields, BasicAuth, self-escalation, or channel disable checks entered scope, and confirm no staged files remain unexpectedly.
- [ ] 12.3 After implementation, tests, browser checks, migration/cutover prerequisites, rollback evidence, and review all pass, reconcile every task and acceptance criterion and use the repository OpenSpec archive workflow for `manage-persisted-game-catalog`.
- [ ] 12.4 Run `openspec validate --all --strict --no-interactive` after archive and confirm `manage-persisted-game-catalog` no longer appears in `openspec list --json` before reporting issue #223 complete.
