# Local Implementation Evidence

Updated: 2026-09-08. Owner: #268. Worktree: `worktree/informative-home-footer`.

## Implemented locally

- Every App-layout page uses one shared footer composition. Compact/informative selection and page-level footer configuration are removed; existing narrow/wide shell geometry only controls alignment.
- The footer starts directly with Explore and Help, without a repeated D20 brand/tagline introduction. Storybook exposes only Default and Mobile expanded under Widgets/Footer; the ordinary Home stories cover page integration.
- Explore and Help use the same link nodes in desktop columns and independent mobile disclosures at 48rem. Privacy and Terms stay outside disclosures.
- Breakpoint changes reset mobile state, preserve focused links, and transfer focused triggers to desktop headings and back. Same-mode resizing preserves state.
- Unenhanced markup exposes every link; the implementation has no disclosure animation, remote content fetch, storage persistence, or social integration.
- `/about` uses the existing public Inertia boundary and factual product copy, one reading column, and the same shared footer. Guest and authenticated route tests preserve authentication behavior.
- The `site-footer` block name avoids a confirmed collision with daisyUI's global `.footer` child-grid styles. Screenshots were inspected after the correction.
- The Home page component, catalog props, workspace implementation, game behavior, and other worktrees are unchanged.

## Initial implementation verification

Historical evidence below predates the September 7 simplification. Removed modes and story URLs describe that earlier revision, not the current implementation.

- Baseline: 2 existing Layout unit tests and the existing Developers route test passed before their respective changes.
- Footer browser suite: 26 checks passed across Chromium and Firefox. Covers exact link contracts, 320/390/767/768/769 widths, independent toggles, ARIA controls, hidden-link exclusion, focus transitions, keyboard activation, no fetch/navigation on toggles, and unavailable-enhancement fallback. Closing a group also restores focus when activation does not natively move focus away from a child link.
- Page controller suite: 39 tests passed, including both new About access cases and existing routes. The initial development-dependency compilation was interrupted by SIGTERM; the subsequent run completed successfully.
- Focused unit suite: 9 tests passed for App layout selection, About copy/links, and matching Storybook page context.
- `mix assets.format.check`, `mix assets.lint`, `mix typecheck`, and `mix assets.build` passed. The build retains the existing large-bundle advisory; no bundle-policy change was introduced.
- Updated visual references: all 54 story cases passed a normal comparison run across the existing desktop/tablet/mobile presets, including informative and narrow/wide compact footers, both themes, open mobile groups, focus, About, public/authenticated Home, and empty Home.
- The full frontend run exposed expected compact-footer screenshot changes on account/confirmation/registration pages and the existing layout browser fixtures. Only those affected baselines were updated. An isolated Player Count glyph comparison failed once and passed all 12 cases on an unchanged rerun; its component and references remain untouched.
- Final `mix assets.test`: all 263 tests in 50 files passed after the affected references were reviewed and updated. Final frontend formatting, lint, type checks, and production asset build also passed. This includes the new close-with-focused-link regression coverage.
- Full-document Storybook captures of content taller than the viewport include blank area beyond the visible viewport. These are not evidence that lower page sections were visually reviewed; direct scrolling/manual inspection remains pending.
- `just check` was retried through `nix shell --inputs-from . nixpkgs#just --command just check` because Just was absent from PATH. All 792 backend tests passed, but the gate stopped at three existing Credo nesting violations in `lib/d20/koala_rescue_club/game.ex` at lines 250, 345, and 383. Its Git blob is identical to HEAD (`a060bb624dd76329d42a079d4ac3fdb6aa693db0`). Later broad-gate steps did not run.
- `mix openspec.check` passed for 9 active changes; `openspec validate --all --strict --no-interactive` passed all 80 items. The footer change intentionally remains active.

## Review refinement on 2026-09-06

- Isolated stories moved from `App/Footer` to `Widgets/Footer`, including their existing visual references. Runtime ownership remains in App UI; this is a Storybook navigation change, not a new widget slice.
- The informative footer now uses Home's `--color-base-100` surface, preserving light/dark themes. Desktop columns are capped at 12rem each with a 1rem gap; block spacing and link padding follow the tightened layout reference. Mobile disclosure triggers and links retain their 44px minimum height.
- The footer link is now `About` at `/about`; the About page title and copy are unchanged.
- Public and signed-in Home previews assert exactly one informative footer from the actual Home layout selection. The new `home--footer` story scrolls to that footer without duplicating the component. Review URLs are `http://localhost:6100/?path=/story/widgets-footer--informative` and `http://localhost:6100/?path=/story/home--footer`; both entries were verified in the running Storybook index.
- `bun run test:unit -- tests/app/layout.test.ts tests/stories/page_layout.test.ts`: 8 tests passed. `bun run test:browser -- tests/app/ui/footer.browser.test.ts`: 26 Chromium/Firefox checks passed with the revised About label.
- `bun run lint`, `bun run typecheck`, `bun run format.check`, and `mix assets.build` passed. No dependency, runtime configuration, backend, or other worktree changes were needed for this refinement. The build's existing bundle-size advisory remains.
- `bun run test:visual -- stories/widgets/footer.stories.ts stories/pages/public/home.stories.ts stories/pages/authenticated/home.stories.ts --update` passed 51 cases across desktop, tablet, and mobile. Desktop/tablet light, desktop dark, mobile expanded, and desktop/mobile full Home screenshots were inspected. The same command without `--update` then passed all 51 visual comparisons. The earlier full-suite counts above predate this refinement.
- No matching Storybook page was available in the connected Chrome session. The user was asked to open `http://localhost:6100/?path=/story/home--index`; unrelated tabs and the carousel server on port 5000 were left untouched. Screenshot review and automated tests do not claim completion of the remaining manual DevTools matrix.

## Simplification on 2026-09-07

- Removed compact/informative selection from Footer, Layout, Home, and Storybook decorators. Layout, Home's module exports, and the layout decorator now match their original shared-shell contracts; narrow/wide still controls alignment only.
- Removed the repeated D20 brand/tagline introduction. Every App-layout page renders the same Explore/Help directory and legal strip; mobile disclosures and focus behavior remain intact.
- Reduced eight isolated footer stories to Default and Mobile expanded under Widgets/Footer. Theme and viewport are existing toolbar controls, not separate stories. Removed the redundant footer-focused Home story; ordinary public/authenticated Home stories assert integration.
- Verified the running Storybook index contains exactly `widgets-footer--default` and `widgets-footer--mobile-expanded`. Preview URLs are `http://localhost:6100/?path=/story/widgets-footer--default` and `http://localhost:6100/?path=/story/widgets-footer--mobile-expanded`; the ordinary Home preview remains `http://localhost:6100/?path=/story/home--index`.
- `bun run test:unit -- tests/app/layout.test.ts tests/stories/page_layout.test.ts`: 8 tests passed. `bun run test:browser -- tests/app/ui/footer.browser.test.ts`: 26 Chromium/Firefox checks passed, including identical link content under both shell widths and the absent introduction.
- `bun run test:browser -- tests/app/layout.browser.test.ts --update`: 6 checks passed with affected page references updated. `bun run test:visual -- stories/widgets/footer.stories.ts stories/pages --update`: 57 cases passed across the existing three viewport presets. Inspected desktop Default, mobile Mobile expanded, and full desktop Home references; the footer is visible and aligned with Home's content.
- Old generated references for removed footer stories were moved recoverably to `/tmp/d20-footer-story-cleanup.COc9BE`, including the three removed Home footer-story images. Only references affected by the shared footer were updated; Workspace and Player Count sources/references remain unchanged.
- `bun run lint`, `bun run typecheck`, `bun run format.check`, and `mix assets.build` passed. The existing large-bundle advisory remains. No backend, dependency, runtime configuration, or other worktree edits were needed for this simplification.
- Final `mix assets.test` passed all 245 tests in 50 files without updating references, including full frontend unit/browser coverage and normal story screenshot comparisons. This supersedes the earlier revision's frontend counts.
- `mix openspec.check`, `openspec validate --all --strict --no-interactive`, and `git diff --check` passed after reconciliation. All 80 specification items remain valid; this change remains active because the publication and manual verification gates below are still open.
- No matching Storybook tab was open in connected Chrome. The user was asked to open the Default preview; unrelated tabs and the carousel server on port 5000 were left untouched. Automated tests and screenshot inspection do not close the remaining manual DevTools matrix.

## Not ready for publication

`/help`, `/contact`, `/privacy`, and `/terms` are not implemented by this local increment. The footer's corresponding links remain part of the required final contract, not accepted working destinations. Do not merge/deploy this increment as a completed footer release.

- #247 is open: the deletion action, security/retention decisions, and usable public instructions are not supplied. No fictitious deletion action or unfinished answer has been rendered.
- #248 is open: approved Privacy/Terms, actual operator identity, monitored private support address, processing/retention decisions, and revision dates are missing. The user was asked for these inputs.
- Publication, anonymous production document GETs, real deletion verification, and #249 Meta handoff remain unperformed.
- DevTools inspection requires the user to open the footer worktree's preview. The existing `http://localhost:5000/` belongs to the separate carousel worktree and was not navigated or modified. An isolated Storybook preview was started at `http://localhost:6100/`; no existing matching tab was available at selection time.
- Manual zoom, safe-area/Workspace overlap, reduced-motion settings, and supplementary viewport review remain pending. Automated responsive assertions are not a substitute for this evidence.

The OpenSpec change and #268 stay open, with 6 of 26 tasks checked and the Project item in In Progress. Checkbox groups that include missing pages or manual/production evidence stay unchecked even where their footer-only portion is implemented. Implementation changes remain uncommitted in the owning worktree; no branch integration, push, deployment, or Meta dashboard mutation was performed. The worktree and isolated Storybook preview are retained for continuation.

## Legal-row and rights-holder review increment

- Removed only the copyright font-size override and the <=48rem forced column from `footer.svelte`. Copyright and legal links now inherit identical typography; existing flex wrapping handles content pressure. Directory disclosures are unchanged.
- Added deterministic footer screenshots at 320px and 446px (single line) and 220px (content-pressure wrapping), in Chromium and Firefox. Focused browser run passed 32 tests, including normal comparisons after baseline generation. The two existing Widgets/Footer stories passed 6 normal visual comparisons after their six references were refreshed. Existing shared-page references elsewhere were not regenerated in this narrow increment and may need reconciliation in the owning delivery's broad visual run.
- Focused ESLint and oxfmt passed; frontend typecheck reported 0 errors and 0 warnings.
- DevTools reused the existing `http://localhost:6100/?path=/story/widgets-footer--default&globals=viewport.value:mobile` page, backed by this worktree's Storybook process. Verified at 320px and 446px; reviewed `/tmp/d20-footer-446.png`. Computed copyright, Privacy and Terms all use `13px / 19.5px "Noto Sans Mono Variable", "Noto Sans Symbols 2", monospace`. No console warnings/errors. Restored the original story URL/mobile viewport; unrelated tabs were untouched.
- The requested exact label is `For Publishers and Rightholders`; owning issue #268 and specs record `/rights-holders` and both game-proposal/rights-concern scenarios. Runtime replacement remains pending by supervisor direction because no useful public page or confirmed monitored contact exists. Games remains unchanged in runtime rather than introducing a 404. No page, invented contact, route, catalog, or legal text was added.
- Existing dirty work was preserved; no files staged, commit, push, integration, deployment or archive performed. Delivery and external publication gates remain open.


## Footer component consolidation on 2026-09-07

- Moved directory data, markup, styles, and disclosure state into `assets/js/app/ui/footer.svelte`; removed the single-use `footer_group.svelte`. Runtime ownership remains in App UI with the existing Widgets/Footer previews.
- One matchMedia subscription now handles both groups. CSS controls heading/button visibility from the shared mobile class, after focus is captured, and rotates one static chevron path from `aria-expanded`. Shared link styles replace duplicate group styling. Native buttons, anchors, and the hidden attribute retain keyboard activation and collapsed-link semantics.
- `bun run test:browser -- tests/app/ui/footer.browser.test.ts`: 32 Chromium/Firefox tests passed, including independent toggles, 768/769 boundary transitions, focused links/headings, pointer-focus fallback, and enhancement failure.
- `bun run test:unit -- tests/app/layout.test.ts tests/stories/page_layout.test.ts`: 8 tests passed. `bun run test:visual -- stories/widgets/footer.stories.ts`: 6 normal screenshot comparisons passed across desktop, tablet, and mobile without updating references.
- Scoped `bunx --no-install eslint js/app/ui/footer.svelte`, `bunx --no-install oxfmt --check js/app/ui/footer.svelte`, and `bun run typecheck` passed; svelte-check reported no errors or warnings.
- DevTools initially had no matching preview tab; the user was asked to open the existing preview. A subsequent attempt could not connect to Chrome at port 9222. No unrelated tab was navigated, and manual DevTools verification remains pending.
- This review increment remains uncommitted with the owning delivery. Existing content, manual-review, and publication gates remain open, so #268 and its OpenSpec change are not closed or archived.


## CSS responsive presentation and shorter classes on 2026-09-07

- Removed the reactive mobile flag and mobile CSS class. The existing viewport media query now selects heading/button presentation. `footer--enhanced` is independent of width and only enables working disclosure controls; initial state keeps all links readable. The single matchMedia listener synchronizes open state and focus, not presentation classes.
- Replaced `site-footer` class prefixes with `footer`. The root uses a scoped `footer` element selector, avoiding DaisyUI's existing global `.footer` component rules. All markup, styles, and state remain in `footer.svelte`.
- The first direct media-query conversion reproduced focus loss in Chromium and Firefox: CSS hid the active trigger before the matchMedia callback captured focus. A `:focus` display rule retains that trigger until the callback transfers focus to its heading. DOM binding arrays are reactive to avoid Svelte binding warnings.
- Final `bun run test:browser -- tests/app/ui/footer.browser.test.ts` passed all 32 Chromium/Firefox checks without those warnings. Scoped ESLint, oxfmt checking, and `bun run typecheck` passed with no errors or warnings.
- DevTools could not connect to the existing Chrome instance at port 9222; the previous request to open the prepared preview remains unresolved. No unrelated tab was opened or changed. Existing publication/manual-review gates remain open and this refinement is uncommitted.
- Final visual comparisons passed all 6 Widgets/Footer cases without reference updates; the two focused layout/unit files passed all 8 tests. Strict OpenSpec validation passed all 80 items; `git diff --check` passed.


## Single CSS breakpoint on 2026-09-07

- Removed `matchMedia` and its API-availability guard. The 48rem threshold now appears only in CSS. The directory uses native Flexbox: capped equal groups in a row on desktop and full-width groups in a column on mobile, with the same spacing and link geometry.
- A single ResizeObserver reads the directory's computed `flex-direction` and updates disclosure state/focus only when that mode changes. Initialization runs synchronously before exposing controls, avoiding a first-click race. The existing focus guard and Svelte tick remain for the required focus handoff. All behavior stays in `footer.svelte`.
- Replaced the obsolete matchMedia-unavailability fallback test with a real-browser check that mobile toggling, desktop links and focus transfer work when matchMedia is absent. Added a local ResizeObserver stub to the existing jsdom layout test, where browser layout observation is unavailable; runtime code has no compatibility shim.
- `bun run test:browser -- tests/app/ui/footer.browser.test.ts`: 32 Chromium/Firefox tests passed. `bun run test:visual -- stories/widgets/footer.stories.ts`: 6 unchanged visual comparisons passed. The two focused layout/unit files passed all 8 tests.
- Compiled Footer with Svelte's server compiler and rendered initial HTML without executing client hooks: all 8 anchors and 2 lists are present, neither list is hidden, and there is no enhancement class or client script. This characterizes the component fallback; it does not claim the application has gained server rendering.
- Scoped ESLint and formatting passed for the component and touched tests; `bun run typecheck` reported no errors or warnings. DevTools still cannot connect to Chrome at port 9222. No unrelated browser page was opened or changed.
- Changes remain uncommitted in the same delivery; existing public-content, manual-review and publication gates remain open. No archive, commit, integration, or deployment was performed.
- Full `bun run test:unit` passed all 82 tests in 14 files.


## Native disclosures without synchronization on 2026-09-07

- The user explicitly accepted omitting automatic focus handling to prioritize simple native behavior. Replaced reactive disclosure state, layout reads, observers, media-query APIs, mount hooks and focus calls with mobile `details`/`summary` and CSS-selected static desktop lists. Both representations use one link inventory and one local template in `footer.svelte`; only one set is accessible at a time. Mobile open state now survives width changes while mounted instead of resetting.
- Removed the temporary ResizeObserver stub from the jsdom layout test and replaced obsolete focus/state-reset tests with native open-state and unique-accessible-link checks. The jsdom test checks both rendered link targets; actual CSS visibility is verified in browser tests. The existing Mobile expanded story activates visible heading labels inside summary. Native expanded state is asserted through `details.open`, not manually assigned ARIA attributes.
- `bun run test:browser -- tests/app/ui/footer.browser.test.ts`: 24 Chromium/Firefox tests passed. `bun run test:unit`: all 82 tests in 14 files passed. Scoped ESLint/oxfmt and frontend typecheck passed with no errors or warnings.
- Separately compiled the component with Svelte's server compiler and supplied its markup and CSS to Playwright Chromium and Firefox contexts with `javaScriptEnabled: false`. Native Enter/Space toggling, desktop's 8 unique accessible links, and retained open state across viewport changes passed in both engines. This verifies disclosure behavior from markup without client hooks; it does not add application SSR.
- Reviewed actual and reference mobile-expanded screenshots. Geometry and content are unchanged; native activation no longer leaves the former synthetic button-focus outline after the story's click. Updated only affected Mobile expanded references across the three presets; Default comparisons already passed. The update run passed all 6 cases.
- DevTools remains unavailable because Chrome cannot be reached on port 9222; no unrelated tab was opened or modified. The existing manual-review and public-content/publication gates remain open. This local increment is uncommitted and does not close or archive the broader delivery.
- Final normal visual run passed all 6 cases without updating references.


## Inline footer list markup on 2026-09-07

- Removed the local `linkList` snippet, then completed the requested inlining by replacing the group array, link loops, and `clientNavigation` flag with two explicit Explore and Help navigation blocks. Each representation contains literal links. Applicable anchors use bare `use:inertia`, which reads their href from the anchor; Help/FAQ and legal links remain ordinary anchors. CSS and native disclosure behavior are unchanged.
- Repeated the focused checks below after this final static-markup refinement; all passed without changing tests or screenshot references.
- `bun run test:browser -- tests/app/ui/footer.browser.test.ts` passed all 24 Chromium/Firefox checks. `bun run test:visual -- stories/widgets/footer.stories.ts` passed all 6 comparisons without reference updates. Scoped ESLint/oxfmt and `bun run typecheck` passed with no errors or warnings.
- This local refinement remains uncommitted with the same delivery; existing manual-review and public-content/publication gates remain open.


## Layout test simplification on 2026-09-07

- Removed `assets/tests/app/layout.test.ts`, including checks for page layout exports and duplicate developer-link assertions. Existing Storybook Home screenshots cover shell presentation, and footer browser tests cover link targets.
- Retained the nonvisual contracts in the existing layout browser scrolling scenario: main is not an Inertia scroll region and can actually receive programmatic focus. The latter replaces the unit assertion about its tabindex attribute. No application code changed in this refinement.
- The first verification exposed stale screenshot references from earlier footer refinements. Reviewed before/after images: seven Home references differ only in footer copyright typography or the mobile legal-row layout; the compact mobile browser reference adds 13 pixels of blank space. Refreshed only those eight references and preserved existing staged state.
- Final `bun run test:browser -- tests/app/layout.browser.test.ts tests/app/ui/footer.browser.test.ts` passed all 30 Chromium/Firefox checks. `bun run test:visual -- stories/pages/public/home.stories.ts stories/pages/authenticated/home.stories.ts -t "Index|Empty"` passed all 9 selected Home comparisons without updating references. Scoped ESLint/oxfmt and `bun run typecheck` passed with no errors or warnings.
- This refinement remains uncommitted with the owning delivery. Existing public-content, manual-review, and publication gates remain open.


## Approved support and rights addresses on 2026-09-07

- The user supplied `support@d20.ravecat.io` for support/feedback and `rights@d20.ravecat.io` for game proposals/rights concerns, explicitly authorizing preparation while provisioning is in progress. Used the existing ASCII production domain and stated the visually similar Cyrillic-character correction in the working update. No mailbox, DNS, forwarding, deployment, or email-send operation was performed.
- Added anonymous Inertia routes and static Svelte pages at `/contact` and `/rights-holders`, selectable addresses with ordinary mailto links, practical inquiry guidance, and cross-links between the pages. Inertia applies only to the site-page links. No forms, clipboard controls, new packages, reactive contact state, response-time promises, or legal/licensing claims were introduced.
- Replaced Games in both explicit footer representations with exact text `For Publishers and Rightholders` at `/rights-holders`. The existing Games route and About catalog entry remain intact. Updated footer browser assertions for the new target, including the mobile disclosures.
- Added Storybook examples for both new pages in light/dark with exact mailto/cross-link assertions. Reviewed representative desktop/tablet/mobile images and the expanded mobile footer. Refreshed 53 existing page/footer references for the new label and previous approved legal-row changes; created 12 new-page references. The update run passed all 69 affected visual cases. No unrelated Player Count Label reference was changed.
- Repository-wide frontend formatting, ESLint, and type checks passed with no errors or warnings. `bun run test:browser -- tests/app/ui/footer.browser.test.ts` passed all 24 Chromium/Firefox checks. The final normal `bun run test` passed 250 of 253 tests, including every changed page/footer case; three Maximum Only screenshot comparisons in the unchanged Player Count Label story differed at the ≤ glyph. Focused reruns of Maximum Only passed at all three Storybook viewports without reference changes; the broad suite is not reported as fully green.
- Recovered from a build-cache conflict between the default Elixir and the repository Nix toolchain by forcing test dependency compilation in Nix and using that environment for remaining Mix checks. `CI=true MIX_ENV=test nix develop --command ... just check` passed compilation, Elixir formatting, and all 794 backend tests, including direct guest access and signed-in Inertia requests with the actual asset version. It then stopped on the same three existing Credo nesting findings in `lib/d20/koala_rescue_club/game.ex` at lines 250, 345, and 383. That unrelated code was not changed.
- Separately ran `mix openspec.check`, `mix assets.build`, and `mix assets.storybook` in the same Nix test environment; all passed. Strict OpenSpec validation passed all 80 items, and `git diff --check` passed.
- DevTools could list browser pages but none matched the local development/Storybook origin. No unrelated tab was opened or navigated. Existing manual full-page/zoom/reflow review and mailbox receipt/monitoring gates remain open. Help, Privacy, Terms, and the deletion answer still depend on their separate approved content and implementation; this address input does not complete them.
- The contact-page preparation is complete locally and remains uncommitted with the owning delivery. #268 is not closed, and its broader OpenSpec change is not archived because publication and independent content work remain incomplete.


## About collaboration copy on 2026-09-08

- Replaced the catalog promotion and technical session explanation with a warm introduction to D20 as a fan project creating digital versions of well-known board games. Added invitations for players, publishers, game designers, developers, and interface designers. About now links to `/contact`, `/rights-holders`, and `/developers`, with no `/games` promotion or link. Retained static inline markup and existing article styling.
- Updated the page brief, proposal, design, product-information requirement, page index, task 2.5, and #268 acceptance scope before reconciling this increment. Existing Help/legal/deletion and mailbox publication gates remain open; the broader change is not ready to archive or close.
- `bun run test:unit -- tests/pages/about/ui/about.test.ts` passed (1 test), verifying the collaboration destinations and absence of the Games link. Scoped ESLint and `oxfmt --check` passed; `bun run typecheck` reported zero errors and warnings.
- Updated only the six About screenshot references with `bun run test:visual -- stories/pages/public/about.stories.ts --update` and inspected every light/dark desktop/tablet/mobile reference. The normal run of `bun run test:visual -- stories/pages/public/about.stories.ts` then passed all six comparisons. The existing screenshot harness captures the visible iframe viewport with blank space below it, so these images do not establish full-page or scrolled-content review; the link test covers all article destinations. No manual zoom or full-page browser check is claimed.
- `openspec validate --all --strict --no-interactive` passed all 80 items. `git diff --check` passed. No backend code or routes changed in this copy refinement; broad cross-stack checks were not repeated. Changes remain local and uncommitted with the owning footer delivery.


## Local master transfer on 2026-09-08

The user requested a commit and transfer to local `master` for the implemented footer, About, Contact, and rights-holder pages. The candidate includes the reviewed screenshot references and the owning specifications. This is an implementation increment under #268, not final acceptance of its missing Help/legal/deletion content or external publication checks. Keep the issue and OpenSpec change active. No push or deployment is requested. Fresh candidate validation is recorded below. The owning issue records the resulting commit, local master transfer, and integrated-target checks.

- Pinned local master at `e12178085eff62d636d519ea4c4a7c869bbc3390`; the candidate rebase was a no-op and the target checkout was clean. Committed implementation, tests, references, and active delivery artifacts together with `Refs: #268` rather than closing the broader issue.
- Fresh `CI=true MIX_ENV=test nix develop --command just check` passed compilation, Elixir formatting, and all 794 backend tests, then reproduced the same three Credo nesting findings in `lib/d20/koala_rescue_club/game.ex` at lines 250, 345, and 383. That source and Credo configuration match the pinned master.
- Ran the checks after Credo separately in the same Nix environment: `mix dialyzer`, `mix ex_dna lib --max-clones 14`, `mix reach.check --arch --smells`, `mix openspec.check`, `mix typecheck`, `mix assets.build`, and `mix assets.storybook` all passed. Frontend formatting and ESLint passed, and strict OpenSpec validation passed all 80 items.
- Fresh full `bun run test` passed 249 of 253 checks. Three unchanged Player Count Label Maximum Only references differed by eight pixels; the mobile Established Account screenshot timed out while the dependency analysis was running. A focused mobile Established Account run passed, and isolated Maximum Only checks passed at all three viewports without reference or source changes. The broad suite is not reported as fully green.
- The user-authorized local integration proceeds with these recorded pre-existing and non-reproducing check failures. Retain the worktree and local candidate branch while the broad validation gate remains non-green; do not force retirement, close #268, archive the active change, push, or deploy. No product or reference changes were made to suppress those checks.


## Spacing review - 2026-09-08

This CSS-only increment continues #268 on `worktree/informative-home-footer`. The clean owning worktree was fast-forwarded to `03b7735` to include the delivered Home carousel; the pre-edit footer/layout baseline passed 30 Chromium/Firefox checks.

- Home top padding changed from 24px to 16px at the default root size. Its former 40px bottom padding and the footer directory's additional 16px inset are removed. Main uses its intrinsic grid-row height, so short/empty pages also keep the footer adjacent.
- Desktop columns use equal 8px gaps after their headings and between entries, including the wrapped publisher label. The directory has equal 8px top/bottom padding. The legal strip has equal 8px insets plus bottom safe-area protection. Mobile lists retain 44px link/control targets with 8px gaps and equal list block padding.
- DevTools used the existing `http://localhost:5000/` page only. Measured 16px header/content spacing, 0px main/footer and main/divider gaps, 8px desktop heading/link gaps and 8px directory insets. The 320px viewport had no horizontal overflow; both expanded groups retained 44px targets and 8px list gaps/padding. Desktop light/dark and mobile expanded screenshots were inspected. Emulation and native open state were restored. The original scroll offset now exceeds the shorter document's maximum and is naturally clamped. No browser console errors remained after the development server restarted.
- The previously open page outlived its development server. Started the owning worktree with `mix serve`, then restarted it with the primary checkout's existing direnv environment. Live game metadata currently falls back because enrichment is unavailable; no metadata, data, or environment file was changed. This limits live artwork comparison, not measured spacing; Storybook retains its deterministic game fixtures.
- Svelte autofixer reported no issues in the three touched components. Scoped oxfmt/eslint and `bun run typecheck` passed with zero Svelte errors/warnings. `mix openspec.check` passed for 9 active changes; strict OpenSpec validation passed all 81 items. `mix assets.build` passed with the existing large-chunk advisory. Browser targets resolve to Chrome/Edge 121, Firefox 128, iOS/Safari 17.4 and Opera 107; this change uses supported grid, gap and logical padding.

The initial 54-case Home/footer visual update passed; its runner emitted a teardown-timeout notice after successful tests. One concurrent comparison run was terminated with exit 143 before a test summary; smaller normal comparison runs are used for the final gate. Normal footer/layout browser comparison passed all 30 checks; Home browser comparison passed 5 with its existing unsupported-feature skip. Focused Home/page-layout unit tests passed all 13. Pixel-difference inspection found information-page captures unchanged in their shared visible region, with only the expected document-height reduction; Home changes start at the content offset, and footer captures change from the removed top inset. The 39 additional story cases passed while updating affected references across all three viewports; normal selected-story comparisons then passed all 18 Home/footer cases across the three viewports and all 13 additional desktop cases. The final normal comparison runs exited 0 with the same teardown-timeout notice after successful tests; no new screenshot mismatch or assertion failure remained. Existing public-content and production verification gates remain open; this increment does not archive or close the full footer feature. Rollback reverts its styles and matching visual baselines without changing routes or persisted data.


Reference recheck: [Apple footer stylesheet](https://www.apple.com/ac/globalfooter/8/en_US/styles/ac-globalfooter.built.css) on 2026-09-08 uses the same spacing after desktop group titles and between list items, with no trailing last-item margin. D20 implements that spacing relationship with CSS grid gaps and symmetric directory padding.

Task 3.16 is verified locally. The parent change remains active because its existing content/publication tasks are incomplete. The local preview remains on `http://localhost:5000/`, served from the retained owning worktree using the primary checkout's existing direnv environment. No branch publication, target-branch integration, issue closure, or full-feature archival is part of this increment.


## Footer separation correction - 2026-09-08

The user clarified that the footer must have the same normal spacing as other catalog blocks. The initial zero-gap interpretation is superseded. Only Home bottom padding changes, from 0 to 0.6667rem, matching the existing section separation and heading-to-cards margin. The header inset, footer internals, and intrinsic main-row sizing are preserved.

- Baseline: clean owning worktree at `1e26f6c`; all 7 focused Home unit tests passed.
- DevTools used the existing `http://localhost:5000/` page with hot-reloaded styles. Desktop and 320px viewport measurements confirmed equal 10.659px rendered gaps between section boxes, heading/card boxes, and the final section/footer divider (0.6667rem before device rounding). There was no horizontal page overflow. Desktop/mobile screenshots were inspected; viewport emulation and the original scroll position were restored.
- Scoped oxfmt and eslint passed. `bun run typecheck` reported zero errors/warnings; Svelte autofixer reported no issues. Home browser checks passed 5 with the existing unsupported-feature skip while updating references.

All 48 Home story cases passed across desktop/tablet/mobile while updating references. Their runner exited 0 with the existing teardown-timeout notice after successful tests. All 12 normal Index/Empty/Dark comparisons then passed across desktop/tablet/mobile (36 unrelated story cases excluded by the name filter). Task 3.17 is verified locally. `mix openspec.check` passed for all 9 active changes, and strict OpenSpec validation passed all 81 items. This correction remains within #268; its separate content/publication gates stay open. Rollback restores the preceding Home padding and affected Home references only.

## Help and policy reference preparation - 2026-09-08

- Added the dated BGA/Tabletopia/Lichess source comparison at `pages/reference-research.md`, including retrieval limitations and official-source checks. BGA's FAQ live extraction supplied only its shell; indexed first-party content was used with that limitation recorded. Do not treat it as a complete live FAQ review.
- Enriched the existing Help, Privacy and Terms briefs with original candidate wording, a D20 implementation inventory and explicit remaining operator/legal/operational decisions. Reconciled proposal, design, public-information requirements, task 1.4 and the linked #268/#248 records. Publication and task 1.5 remain open; no jurisdiction, age, commercial model, licensing permission, retention policy or working deletion procedure is inferred.
- `openspec validate add-informative-home-footer --strict --no-interactive` passed. `openspec validate --all --strict --no-interactive` passed all 81 items. Checked 49 relative file links across nine affected preparation files, no empty adjacent sections or typographic dashes, and `git diff --check -- openspec/changes/add-informative-home-footer` passed.
- This increment changes preparation documents only and remains uncommitted in the existing footer worktree. It implements no route and supplies no production, mailbox or legal-compliance acceptance. Existing concurrent header, sentence-case and screenshot work is preserved. Runtime tests were not rerun for these documentation changes; the broader change is not archived or closed.


## Header proportions and sentence-case copy - 2026-09-08

This increment continues #268 in the owning worktree from `27abf7e`, alongside the user's sentence-case correction. The linked `refine-borderless-app-shell` contract owns the explicit header dimensions. Concurrent Help/Privacy/Terms research remains separate from this implementation and its completion commit.

- Reduced the complete brand and Log in native dimensions, typography, internal spacing and button corner radius to 75 percent. Kept explicit responsive sizes, existing focus indicators and scroll/reduced-motion behavior. Minimum header heights preserve the existing 80px desktop, 72px narrow and 48px compact surfaces and their document reserves.
- DevTools measured desktop mark height 48px to 36px, full brand 84.625px by 48px to 63.455px by 36px, and Log in 78.068px by 40px to 58.534px by 30px. At 320px, both mark and button are 30px tall; after compaction both are 24px tall. Header/main positions and shared edge alignment remain stable, with no horizontal overflow. Login opens, Escape closes it, and focus returns to the trigger.
- Source text now uses `For publishers and rightholders` and `Contact / support` throughout the footer and corresponding titles, headings and cross-links. The rights page also says `contact support` in prose. First words, proper names and acronyms retain their appropriate capitalization. Updated existing label assertions and the owning copy-writing requirements; no CSS text transform or navigation behavior was introduced.
- In the existing browser tab, followed Home footer -> Contact -> rights-holder page and verified the labels, document titles and links. Inspected desktop and 320px rendering, including the wrapped rights-page heading and expanded mobile footer. Restored the Home route, desktop viewport and top scroll position; the console has no warnings/errors. Live game metadata still uses the previously recorded fallback, so deterministic Storybook fixtures cover artwork comparisons.
- Scoped oxfmt and ESLint passed for all seven source/test/story files; `bun run typecheck` reported zero errors/warnings. Svelte autofixer found no issues in the four components; its suggestion about the existing header effect concerns unchanged imperative authentication behavior. `mix assets.build` passed with the pre-existing large-chunk advisory.
- The final header/footer/layout browser suites passed all 80 Chromium/Firefox checks both while updating references and in a normal comparison run. The complete affected page/footer story update passed all 93 cases across desktop/tablet/mobile. Reviewed 94 changed references, with no image-size changes and differences confined to the header or changed text. Earlier header-only validation passed 56 browser checks and 48 Home stories; one partial visual run ended with exit 143, and later superseded runs were stopped when the user added copy changes. These are superseded by the final checks. The successful story update exited 0 after the runner's existing teardown-timeout notice.

All 18 final normal Contact/rights-page/footer visual comparisons passed at the three viewports, exiting 0 with the same existing teardown notice. `mix openspec.check` passed for 9 active changes; strict OpenSpec validation passed all 81 items. Tasks 3.18/3.19 and the linked shell tasks 15.1/15.2 are verified locally. The broader changes remain active because their existing independent content/publication and lifecycle tasks are unfinished. The local preview remains in the retained owning worktree; no publication, integration, issue closure or full-feature archival is part of this increment. Rollback restores these header declarations, copy strings and matching assertions/references.

## Operator and launch clarification - 2026-09-08

Recorded the user's partial answers in the shared page-input table: personal EU operator without a company, intended worldwide audience, some publicly available source, current initial stage without a paid-access offer, possible future paid access after catalog/demand exploration, and undecided voluntary support. Exact member state, public identity/contact and minors' policy remain unresolved. No support provider or destination was supplied. The transcribed Fofa wording is not treated as a rename.

Updated Help and Terms candidate access/payment wording, Privacy's current-versus-future data boundary, reference applicability, proposal/design, the public-information scenario and task 1.5. The same #268/#248 records carry the confirmed facts and remaining criteria. Restored the two prematurely checked #268 policy/operational criteria to incomplete because final policies, exact country, age handling, retention and deletion are not verified. Preserved the issues' and Project items' statuses.

Rechecked BGA's current Privacy minimum-age section and EDPB consent guidance. The BGA reference has age/parental conditions; no D20 all-ages approval, borrowed national threshold or implemented parental workflow is inferred. Paid access and support remain future/optional preparation, not billing implementation. Checked nine preparation files, 52 relative links and the new launch-input anchors, and ran scoped strict OpenSpec and whitespace validation successfully. No product code, public page, deployment or payment link was changed; the preparation remains uncommitted.


## Header vertical spacing correction - 2026-09-08

This correction continues #268 from `197fd09` in the existing owning worktree. It supersedes the earlier choice to preserve the header's larger height after reducing its contents. Existing uncommitted Help/Privacy/Terms research is preserved separately.

- Reduced the centered header minimum rows from 80px/72px/48px to 60px/54px/36px at a 16px root size. Removed additional block padding instead of stacking it on the explicit row height. Brand, Log in and account-control dimensions remain intact. The expanded mark now has equal 12px visual insets; compact brand/login have equal 6px insets.
- Matched both layout block-start reserve and document scroll padding to 60px desktop and 54px narrow. Main begins immediately after the expanded header and is 20px/18px higher, while existing catalog/footer intervals remain unchanged. Updated the existing browser test's root-alignment contract to the smaller reserve.
- DevTools used the existing Home page at `http://localhost:5000/`. Measured 60px desktop, 54px at 320px, and 36px compact header heights, centered unchanged controls, matching main/reserve offsets and no horizontal overflow. Catalog section and final-section/footer intervals both remain 10.659px after device rounding. Inspected desktop/mobile screenshots and restored desktop emulation/top scroll state. Console warnings/errors are absent; the previously recorded live metadata fallback remains unrelated.
- Baseline and final header/layout browser comparisons each passed all 56 Chromium/Firefox checks, including root scrolling, compaction, root-aligned targets, focus and dialog behavior. Scoped oxfmt/eslint and `bun run typecheck` passed with zero errors/warnings. Svelte autofixer found no issues in Header/Layout; its header-effect suggestion concerns unchanged imperative dialog behavior. `mix assets.build` passed with the existing large-chunk advisory.

All 87 affected page story cases passed across desktop/tablet/mobile while refreshing 89 references, including the two compact-layout browser references. Reviewed desktop/mobile Home, signed-in account controls and representative page geometry; image widths are unchanged. Height changes follow the reduced reserve, viewport minimum or compact screenshot framing. The visual runner exited 0 with its existing teardown-timeout notice. `mix openspec.check` passed for 9 active changes, and strict OpenSpec validation passed all 81 items. Task 3.20 and linked shell tasks 16.1/16.2 are verified locally. The broader changes stay active for their independent content/publication and lifecycle tasks; no integration, publication, issue closure or archival is part of this correction. Rollback restores the preceding header heights, layout reserve/scroll padding, alignment assertion and matching references.


## Final shell spacing and compact footer review - 2026-09-08

This local styling increment continues #268 and the linked shell dimensional contract. It folds the four unpublished styling commits and the latest corrections into one `style(shell)` commit. Independent Help/Privacy/Terms research remains uncommitted and outside this increment.

- Home now uses one `padding-block: 0.6667rem` declaration. DevTools measured 10.6672px computed padding on both ends and equal 10.6591px rendered catalog/footer intervals, with no stacked footer gap or stretched main row. Expanded header reserves remain 60px desktop and 54px narrow; compact height is 37.8px around the unchanged 24px controls, producing equal 6.9px insets, 15 percent above the prior 6px.
- Compact footer items have no list margin, padding or gap. Links use 6px block padding and render 31.5px tall with existing 13px/1.5 typography. Following the additional density review, summary rows use the same 6px block padding and a 32px minimum height instead of 44px. The intermediate 4px list inset was removed. These are explicit CSS declarations, not accumulated browser defaults. Desktop spacing and native disclosure behavior remain intact.
- Inspected the [Apple global-footer stylesheet](https://www.apple.com/ac/globalfooter/8/en_US/styles/ac-globalfooter.built.css): its compact links use contiguous items and 6px block padding. D20 adopts that spacing relationship with its existing typography and the user's additional compact-summary refinement.
- Prepared-page checks at 697x807 and 320x900 CSS pixels verified both independently expanded groups, 32px summaries, 31.5px links, symmetric Home padding and no horizontal page overflow. Reviewed expanded-footer, desktop Home and empty mobile Home references. Existing live metadata fallback remains outside this styling increment.
- Final Home/header/layout/footer browser reference update passed 85 checks with one existing unsupported-feature skip. A normal comparison run before the last summary reduction also passed 85 with the same skip; the final footer normal comparison passed all 24 Chromium/Firefox checks, including native keyboard toggles, retained state, hidden-link exclusion and legal-row reflow.
- The Home/footer visual update passed 54 stories before the last summary reduction. The final reduction then passed all 31 mobile page/footer stories and four desktop/tablet footer stories. All six final footer visual comparisons passed without updating references. Reviewed 83 changed references: height differences correspond to reduced Home padding, two shorter summary rows and compact scrolling geometry. Successful story runs exited 0, with the existing teardown-timeout notice on the final footer runs.
- Scoped oxfmt and ESLint passed. `bun run typecheck` reported zero errors/warnings. `mix assets.build` passed with the existing large-chunk advisory; inspected its generated CSS to confirm the final 32px summary and 6px padding. The build used the current direnv Elixir 1.20 environment while the existing preview still used 1.19.5, causing a temporary compiled-cache incompatibility. Restarted the task-owned preview in the same current environment and restored the Home view. No runtime configuration or toolchain files changed.

The owning changes remain active because their independent content/publication and lifecycle gates are unfinished. This increment does not publish, integrate, close the issue or archive the broader changes. Rollback restores the corresponding shell styles, copy, contracts and visual references together.

Final reconciliation: `mix openspec.check` passed for 9 active changes and `openspec validate --all --strict --no-interactive` passed all 81 items. Restored the prepared 697x807 viewport with both footer groups open; the prior scroll offset now clamps naturally to 121.4545px because the page is shorter. The final console contains no warnings/errors.


## Header reset after removing document overflow - 2026-09-08

The additional intermediate-width report was reproduced at 697x807: open both footer groups, scroll to compact the header, then close the groups. Chromium clamped `scrollY` to zero but retained CSS animation progress at one. The header stayed 37.8px high while main content still began at the correct 60px expanded reserve, leaving a 22.2px mismatch.

- Added a derived at-top guard using [Svelte's reactive window scroll offset](https://svelte.dev/docs/svelte/svelte-reactivity-window). It disables the existing animations only at zero or negative root scroll offsets. The CSS timeline still owns interpolation while scrolling, and expanded reserves, dimensions and viewport breakpoints are unchanged. Reconciled the linked shell contract's former unconditional no-scroll-state wording with this bounded reset.
- The failing characterization observed 37.8px instead of the initial 60px after collapse. Added a focused footer-collapse regression using existing browser interactions and two reviewed header screenshot references; removed the old expectation that a zero-scroll animation remains attached. The final suite passed all 87 Chromium/Firefox checks with one existing unsupported-feature skip, including the normal 12px/24px progress, authentication, reduced-motion fallback and footer checks. Existing references remain unchanged by this reset.
- DevTools verified the complete transition: 60px expanded header and main start, 37.8px compact header around the 24px mark while scrolled, then 60px header/main start and 36px mark after collapse returns to zero. No horizontal overflow or console warnings/errors. The prepared intermediate viewport is left at the top with both groups closed to show the corrected state.
- Scoped oxfmt/ESLint and `bun run typecheck` passed with zero errors/warnings. Svelte autofixer reported no issues; its existing auth-effect suggestion concerns unchanged imperative dialog behavior. `mix assets.build` passed with the existing large-chunk advisory. The retained preview and final Mix checks now use the owning worktree's matching environment after resolving the earlier mixed-shell cache issue.


## Privacy deferral and local-master transfer - 2026-09-08

The operator explicitly deferred Privacy publication and authorized integration of the current footer work and preparation into local master. Removed the `/privacy` anchor from the shared footer; Terms and native disclosure behavior remain unchanged. There was no Privacy route or published page to remove. The retained policy brief records unresolved operator, data-processing, retention, rights-contact and minors decisions; link removal does not establish compliance or close publication work. Earlier entries describing uncommitted preparation are historical and are superseded by this transfer record.

Reused explicit account-deletion feature #247. A search across open/closed issues and the account/runtime boundaries found no existing automatic account-deletion feature; created #270 for inactive-account deletion and expired-data cleanup, with durations, notice/grace periods and safe rollout left as explicit implementation inputs. Added it to D20 Project 5 as a Medium-priority Todo feature under outcome #12. Verified native blocked-by dependencies from #248 to #247 and #270 and moved #248 to Blocked. Automation is an operator-selected prerequisite, not an asserted universal legal requirement. The current increment does not implement deletion, launch scheduled erasure or publish policies.

Candidate checks before incorporating the separately delivered footer placement in master:

- Existing footer browser suite: 24 Chromium/Firefox checks passed; refreshed six narrow legal-row references and verified hidden-link absence, link counts and keyboard order.
- App/Home browser suites: 87 passed, one existing unsupported-feature skip.
- Affected page and footer stories: 93 passed across desktop/tablet/mobile while refreshing 43 affected references in total with the six browser references. The successful story runner exited 0 with its existing teardown-timeout notice. Reviewed representative footer images and pixel-change regions; Privacy disappears and the 220px legal row no longer needs its former wrap.
- Scoped source/test/story oxfmt and ESLint passed; `bun run typecheck` reported zero errors/warnings. `mix assets.build` passed with the existing large-chunk advisory. `mix openspec.check` passed for 9 active changes; strict OpenSpec validation passed all 81 then-current items. Relative-file checks and `git diff --check` passed.

Master independently received footer-bottom placement #271 in `14da501`; its layout and authoritative specification must be preserved when rebasing this increment. Candidate and integrated-target evidence is recorded below after that reconciliation. The full #268 change remains active for unfinished Help, Terms, publication and lifecycle criteria; it must not be archived or closed by this increment. Rollback restores the footer anchor, its assertions and references together only after reconsidering the operator's deferral; the blocker record and unpublished briefs remain independently useful.

Anonymous local HTTP checks confirm `/about`, `/contact`, `/rights-holders` and `/developers` return 200. `/help`, `/terms` and `/privacy` return 404; the router has no corresponding declarations. The Privacy anchor removal prevents navigation to that deferred destination. Help/FAQ and Terms remain unfinished under #268/#248 and are not represented as working pages.

Rebased the unpublished increment onto pinned master `14da501` without conflicts. Reconciled the active footer spacing requirement with the already authoritative `app-footer-placement` capability from #271; preserved its executable layout unchanged. The rebased App/Home browser suites again passed 87 checks with one existing unsupported-feature skip. All 87 affected page stories passed across desktop/tablet/mobile; reviewed 36 additional page-reference adjustments for footer placement with unchanged dimensions, including empty desktop/mobile Home and footer movement on short populated pages. Scoped formatting/ESLint, type checks, production assets, all 82 strict specification items and the 9-active-change lifecycle check passed. All 62 relative file links resolve. Candidate checks are complete; integrated-target verification follows under task 6.4.


### Integrated-target verification

Fast-forwarded clean local master from pinned `14da501` to `7564d0d` after candidate validation, preserving the independently integrated footer-bottom layout and its archived #271 specification. The earlier styling commit `5548458` was already in master. This transfer includes the previously uncommitted Help/Privacy/Terms preparation and the current footer/public-information delta specifications.

- `bun run test -- --project browser tests/app tests/pages/home`: 87 passed, one existing unsupported-feature skip on integrated master.
- `bun run test:visual -- stories/pages/public/home.stories.ts stories/widgets/footer.stories.ts -t 'Empty|Default|Mobile Expanded'`: all nine normal comparisons passed across desktop/tablet/mobile; 39 unrelated cases were excluded by the filter. The successful runner exited 0 with its existing teardown-timeout notice.
- `bun run typecheck`: zero errors/warnings. `openspec validate --all --strict --no-interactive`: 82 passed. `mix openspec.check`: 9 active changes passed. `mix assets.build`: passed after rebuilding the main checkout's dependency cache and briefly waiting for the existing development process's build lock; dependency warnings and the existing large-chunk advisory do not change the successful result.
- DevTools reused the existing Home tab at `http://localhost:5000/`. At the prepared 697x807 viewport, the integrated DOM contains no Privacy anchor, retains one Terms anchor, preserves the naturally sized bottom footer and has no horizontal overflow. Checked both observed collapsed and expanded disclosure states without changing route, authentication or viewport. Console warnings/errors were absent. Current public-page HTTP results are recorded above; this is local verification, not production publication.

Task 6.4 is complete for the authorized local transfer. #247 and #270 remain Todo, #248 remains Blocked by both native dependencies, and #268 remains In Progress. The wider footer and shell changes remain active because their content/publication and other recorded criteria are unfinished. No remote push, deployment, account deletion or policy publication occurred. This linked documentation finalization records the post-integration checks without rewriting the integrated implementation commit.


## Permanent columns and guest account actions - 2026-09-08

The operator requested the Zed footer organization, direct implementation in master, lowercase `d20 © <current year>`, and `Sign up · Have an account? Sign in`. Both master and the registered footer worktree were clean at `f844175`; this increment was authored directly in master as explicitly requested. It continues #268 and task 3.26.

- Footer now contains one explicit navigation tree with h2 headings and direct anchors. Details/summary, ul/li wrappers, chevrons, duplicate desktop/mobile markup and their styles are removed. CSS places the service block left above 48rem and above Explore/Help below it. Existing theme tokens, shell widths, hrefs, Inertia actions and safe-area padding remain in use.
- Guest account actions are native buttons styled as links. Sign up opens the shared dialog in registration mode; Sign in opens login. They are absent for authenticated visitors. Copyright uses the requested order and casing with a dynamic year.
- The existing layout overflow-removal regression now collapses main-content disclosure content instead of removed footer disclosures. Footer tests cover all destinations, keyboard order, resize focus and authenticated visibility. Only Default remains under Widgets/Footer. The metadata test stubs the reactive window module because its static imports do not require browser measurements; without that isolation this jsdom suite failed on missing matchMedia.
- Normal Chromium/Firefox `bun run test:browser -- tests/app tests/pages/home`: 89 passed, one existing unsupported-feature skip. The focused Footer/Layout run passed all 34 checks. Metadata `bun run test:unit -- tests/stories/page_layout.test.ts`: 6 passed.
- Scoped oxfmt and ESLint passed; `bun run typecheck` reported zero errors/warnings and Svelte autofixer returned no findings. `direnv exec . mix assets.build` passed. The first Mix invocation used a different shell toolchain and caused cache recompilation and a transient local module error; rerunning in the repository direnv environment completed successfully and the prepared page recovered. Dependency warnings and the existing chunk-size advisory remain.
- `bun run test:visual -- stories/pages stories/widgets/footer.stories.ts --update`: all 90 checks passed across 30 desktop/tablet/mobile suites. Reviewed changed references, including standalone footer and short/empty Home; screenshot widths remain unchanged. The runner exited 0 with its existing teardown-timeout notice. Existing long-page captures leave regions below the initial viewport blank; they do not establish off-screen footer appearance, which is covered by the standalone footer screenshots and prepared-page inspection.
- Final normal visual comparisons for Footer Default and Home Index/Empty/Dark passed all 12 desktop/tablet/mobile cases with no baseline updates; 33 unrelated stories were excluded by the filter.
- Strict OpenSpec validation passed all 82 items; `direnv exec . mix openspec.check` passed for 9 active changes.
- DevTools inspected the existing `http://localhost:5000/` page. Initial and updated layouts had no horizontal overflow at 320 CSS pixels. After the original tab was closed, the operator reopened it and validation selected that matching page. Final guest actions opened the correct dialogs without changing the URL, Escape dismissed them, and native focus returned to Sign up. The final 400px page retained all nine controls, including both guest actions, with dark theme and footer text doubled from 13px to 26px. Restored the light theme and original font styling. No console warnings/errors were reported. Authentication and route were preserved; no form was submitted.

The linked content and publication work is unchanged: Help/FAQ and Terms are unfinished, Privacy stays deferred, and mailbox/deletion/production gates remain open. This is a local presentation increment, not completion of #268 or the broader OpenSpec change. Keep the change active rather than archiving incomplete delivery tasks. Rollback reverts this commit's footer, tests and references together while retaining existing routes and policy records.

Task 3.26 is complete for this verified local increment and is included with its implementation, tests and reconciled artifacts in the same semantic commit. The issue remains In Progress and the broader change remains active for its existing unfinished delivery work.
