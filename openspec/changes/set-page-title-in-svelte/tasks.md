## 1. Page Title Ownership

- [x] 1.1 Remove the duplicate developers page title assign from `D20Web.PageController` while preserving the existing Inertia component response.
- [x] 1.2 Update focused Phoenix and Svelte tests so the controller no longer requires title metadata and the page verifies `document.title`.

## 2. Validation

- [x] 2.1 Format the touched Elixir and frontend files with the repository-native formatters.
- [x] 2.2 Run the focused developers controller and Svelte page tests.
