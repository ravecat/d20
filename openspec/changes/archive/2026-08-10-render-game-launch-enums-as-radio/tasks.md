## 1. Shared SJSF Theme

- [x] 1.1 Resolve the installed basic-theme radio component for direct enum widgets while retaining the basic theme fallback for other selectors.
- [x] 1.2 Confirm existing shared radio styles preserve visible focus, disabled state, shell colors, and native input semantics.
- [x] 1.3 Give radio and multi-checkbox groups separate full-width rows matching the submit action height, with wrapping labels.

## 2. Frontend Coverage

- [x] 2.1 Update the focused game page component test to select a radio choice and verify the typed enum payload posted through Inertia.
- [x] 2.2 Update browser coverage to verify radio roles, accessible option names, the schema default selection, and responsive launch layout.
- [x] 2.3 Cover equal full-width option rows and submit-height parity through semantic radio queries in browser mode.

## 3. Validation

- [x] 3.1 Run targeted game page unit and browser tests, formatting, linting, and frontend type checking.
- [x] 3.2 Run strict OpenSpec validation and archive the completed change after all checks pass.
- [x] 3.3 Re-run focused browser, formatting, linting, and strict OpenSpec validation after the visual follow-up.
