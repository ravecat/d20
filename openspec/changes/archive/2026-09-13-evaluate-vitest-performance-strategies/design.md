## Context

At the experiment baseline, the existing `assets/package.json` command is `test: vitest run`; `mix assets.test` delegates to it and `just check` includes that boundary. `assets/vite.config.mjs` defines a jsdom unit project, a Chromium/Firefox browser project, and six independent Chromium Storybook projects for light/dark and desktop/tablet/mobile. The visual projects already progress concurrently, with `fileParallelism: false` inside each project and separate ignored Vite caches. Browser isolation is currently enabled by the installed defaults.

Visual projects use `retain-on-failure` tracing. This records successful executions too, then discards their traces. `assets/.storybook/vitest.setup.ts` loads every declared font and runs the native screenshot assertion against the document root with a 15-second budget after each story. Storybook play functions and accessibility checks also execute in each matrix cell.

The historical audit in [evidence.md](evidence.md) used Vitest 4.1.7 and a dirty favorites checkout at base `577c5c34`. Its 585 tests included 81 unit, 126 browser, and 378 visual cases. Those counts and timings are historical; the clean strategy worktree does not include the uncommitted favorites implementation. Full and browser measurements had failures, some runs competed with other processes, and one full run created missing references. There is no established clean, successful full-suite baseline.

`.github/workflows/release.yml` currently builds and publishes the application image without executing frontend tests. A complete release test gate is desired future behavior, not an existing guarantee.

## Goals / Non-Goals

**Goals:**

- Find the greatest repeatable reduction in end-to-end feedback time on the existing hardware and OS, preferring configuration or small supported dependency changes.
- Use only documented configuration, native CLI flags, and public tool APIs. A documented package-manager patch mechanism does not make edits to a dependency's internal implementation an admissible optimization.
- Preserve reviewed screenshots, all meaningful test assertions, Chromium/Firefox behavior coverage, and all six visual combinations.
- Keep one familiar native command surface without new aliases, scripts that choose tests, manually maintained impact lists, or selection based on a contributor's interpretation.
- Separate execution optimizations that run the same suite from dependency selection that runs a smaller suite.
- Record measured adoption and explicit rejection/deferment decisions without treating estimates as commitments.

**Non-Goals:**

- Do not patch, fork, monkey-patch, or replace internal dependency code, or rely on private internal APIs. Restore the rejected first candidate and retain only its evidence; subsequent candidates must satisfy the public-interface constraint.
- Do not reduce assertions, skip themes, viewports, browsers, or accessibility checks, weaken comparison thresholds, or reduce timeout budgets to obtain faster numbers.
- Do not require extra machines, cloud visual services, a new runner, or a custom orchestration layer.
- Do not absorb the separate browser-migration outcome tracked in [issue #280](https://github.com/ravecat/d20/issues/280).

## Decisions

### Experiment 1 decision: reject internal screenshot timeout patch

The user authorized one measured Vitest experiment at a time, followed by review before the next candidate. Experiment 1 evaluated tasks 2.2 and 2.3 from committed source `592f1403`. During review the user clarified that only documented settings, CLI flags, and public APIs are acceptable. The measured dependency patch is rejected and rolled back, regardless of its local timing benefit. No implementation commit was created.

The small native PlayerCountLabel probe passes with a trailing 10-second close timeout. Upstream 4.1.7 and 4.1.11 retain the uncancelled screenshot timeout race; no supported released fix or public setting eliminating it without changing timeout semantics was verified. The internal patch's observed 47.23% focused reduction remains diagnostic evidence only, not an adopted saving. This direction is deferred until a suitable upstream release or public API exists.

Future comparisons preserve source and reference hashes, warm caches, and all settings other than the selected documented option. The user limits research to a small file/project scope, such as PlayerCountLabel in visual-light-mobile (four tests). Broad validation belongs to eventual consolidated delivery. The [experiment 1 record](experiment-01-shutdown.md) preserves measurements, rejection, and verified restoration. The subsequent user request authorizes an autonomous sweep of all compatible public-interface candidates. The sweep compared against the restored unpatched dependency and retained all changes uncommitted for review, recording a decision for every candidate. Following that review and its corrections, the user authorizes two semantic commits and local `master` integration; this supersedes the temporary uncommitted-review hold. Small experiments remain sequential; only read-only analysis and research may run alongside measurements.

### 1. Use controlled comparisons before selecting an optimization

Every later experiment starts from a named committed source state with stable reviewed references and a recorded test/project inventory. Preserve the historical audit as exploratory evidence; do not reuse its failing and contended full runs as a causal comparison.

Record the native command and flags, source and reference hashes, lockfile, Node/Bun/Vitest/Playwright/browser versions, OS, logical CPU count, memory, cache condition, concurrent workloads, test totals, skipped and failed cases, and exit status. Measure wall time from command startup through process exit separately from Vitest's internal duration and summed test time. Concurrent project durations are not additive wall time.

For each candidate, run a baseline and candidate with one intentional difference, then repeat at least three times per condition in alternating order. Compare equivalent warm-cache samples; measure a separate cold-cache series when startup or transform caching is the proposed saving. Do not destroy shared caches or stop another contributor's processes to arrange measurements. Coordinate non-overlapping runs instead.

Exploratory comparisons use the smallest representative passing file/project scope, with native Vitest flags through `bun run test` and `--update none` to preserve references. Keep the selected scope identical before and after; defer the complete suite to validation of the selected combination before final delivery. Extend with a tiny deterministic failure probe when diagnostic or failure behavior changes. A candidate is not successful merely because it times out sooner, skips work within the selected scope, or turns a previous failure into a pass. Existing failures must be characterized and resolved by their owning outcome before claiming successful-suite performance.

Report medians and ranges with sample counts, absolute seconds saved, and relative improvement. Accept a candidate only when the improvement is repeatable beyond the observed run-to-run spread, coverage and results remain equivalent, and no new instability appears. If measurements are inconclusive, retain the current setting. Do not sum savings from separate experiments without measuring their combination.

### 2. Keep an explicit strategy and gain ledger

The order below prioritizes low-cost investigation and then larger structural opportunities. It is an evaluation order, not a commitment to ship every candidate.

| Candidate                                                                   | Purpose and potential gain                                                                                                                                                                                                                                                                                                                                         | Environment impact                                                                                           | Adoption evidence and main limitation                                                                                                                                                                                                                                                                                                         |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Coordinate independent runners                                              | Avoid several complete browser matrices competing for the same CPUs and memory. Gain is unmeasured; earlier contended runs cannot quantify it.                                                                                                                                                                                                                     | No environment change; coordinate existing invocations.                                                      | Record competing workloads. Six visual projects already run concurrently. Do not introduce a global lock or terminate unrelated work.                                                                                                                                                                                                         |
| Disable routine tracing                                                     | Reduce recording, serialization, and trace-file work. The current three-pair four-story comparison measured 16.49 to 14.87 seconds, 9.82%; the fullscreen story requires trusted native input before adoption.                                                                                                                                                     | Config-only candidate. Native tracing remains available for diagnosis.                                       | Repeat with identical references and outcomes. Screenshot comparisons and actual/diff artifacts remain. Full-run 22-minute versus 3-minute observations do not prove a tracing multiplier.                                                                                                                                                    |
| Fix pending screenshot timeout cleanup                                      | Experiment 1 observed a focused median reduction of 16.62 to 8.77 seconds with an internal patch. The user rejected the method; it is not an adopted gain.                                                                                                                                                                                                         | Only a verified supported upstream release or public API is eligible; local dependency patches are excluded. | Restore the original dependency and defer this direction until an eligible fix exists. Preserve the 15-second screenshot budget and failure semantics.                                                                                                                                                                                        |
| Disable visual file isolation                                               | Old configuration measured 8.02%, but the selected combination screen improved only 0.09 seconds (0.57%). Keep isolation enabled; the older gain is not additive.                                                                                                                                                                                                  | Config-only change to execution semantics.                                                                   | Compare all cases in normal and altered file orders, including repeated runs. Verify cleanup of DOM, globals, mocks, subscriptions, timers, transport fixtures, theme, viewport, focus, and storage. Reject state leakage.                                                                                                                    |
| Consolidate six visual projects into one project with six browser instances | Share Vite preparation while retaining all six instances. Three matched 24-case pairs measured 23.95 to 17.29 seconds with trace-off fixed, 27.81%.                                                                                                                                                                                                                | Modest configuration and setup restructuring.                                                                | Prove theme and viewport initialization before render/play, unique report identities, unchanged baseline and diff paths, cache correctness, and matrix parity. Public instance provide/inject and explicit public project annotations preserve globals; the corrected interactive 66-case matrix passes. Private injection keys are not used. |
| Tune native concurrency                                                     | Two workers per visual instance measured 17.00 to 15.94 seconds on 11 stories, 6.24%. Four did not improve the screening result. This is not a claimed global hardware optimum.                                                                                                                                                                                    | Native settings only.                                                                                        | Compare a small controlled range against defaults and record resources and failures. `fileParallelism: false` is per project; a low `maxWorkers` value does not establish a global cap across projects. Avoid oversubscription from separately invoked suites.                                                                                |
| Wait for fonts actually needed by rendering                                 | Rejected after three pairs: 14.33 to 14.31 seconds, overlapping ranges. Keep eager all-face loading and its error behavior unchanged.                                                                                                                                                                                                                              | Small Storybook setup change.                                                                                | Preserve production font imports and supported languages, mathematical glyphs such as U+2264, and exact reference pixels across all six cells. `document.fonts.ready` is a candidate readiness mechanism, not accepted proof that every required glyph is ready.                                                                              |
| Reduce repeated story or accessibility work where equivalent                | Investigate whether setup or invariant work is duplicated across matrix cells. No safe saving is established.                                                                                                                                                                                                                                                      | Potential harness or test restructuring.                                                                     | Play functions can establish the screenshot state; theme-specific contrast and viewport-dependent accessibility differ. Skipping repeated play/a11y by default changes coverage and is rejected. Any finer split must prove assertion and state equivalence and preserve every meaningful check.                                              |
| Native dependency-graph selection                                           | Avoid unrelated test files during development. The dirty audit selected 97 of 116 file/project combinations, retaining 83.6%; omitting 16.4% of combinations is not a measured wall-time reduction. Localized future edits may select far less.                                                                                                                    | No environment change, but changes which tests run.                                                          | Evaluate native selection across unit, browser, and generated stories. Preserve all visual cells for every selected story. A clean checkout and non-imported dependencies need the explicit command and coverage decision described below.                                                                                                    |
| Native watch mode                                                           | Reuse a persistent process and automatically rerun affected graph dependencies after the initial complete run. Startup amortization and selection gains are unmeasured.                                                                                                                                                                                            | Requires a long-running process and clear process ownership.                                                 | This is a workflow alternative, not a one-shot fallback. Avoid multiple watchers and compare edited-module behavior across all projects before claiming savings.                                                                                                                                                                              |
| Unit worker pool, environment, and filesystem module cache                  | Investigate unit startup only after the visual bottleneck. The historical isolated unit suite took 12.03 seconds, much less than the observed full suite; optimizing it alone has no demonstrated large full-suite benefit. Current 79-case unit screening retains default forks: threads, worker caps and warm filesystem module cache did not beat 6.74 seconds. | Pool/cache settings are small changes; jsdom replacement changes the environment and migration scope.        | Do not generalize the Node/SSR module cache to browser transform reuse. The previous contended worker samples establish no optimal setting. An experimental option needs repeatability and maintenance justification.                                                                                                                         |
| Move standalone browser tests into Storybook projects, issue #280           | Could reduce duplicated harness preparation and project overhead. Gain is unmeasured and can reverse if long flows are multiplied into all six visual cells.                                                                                                                                                                                                       | Separately owned harness/project migration.                                                                  | Preserve Chromium/Firefox, keyboard/focus, form payload/pending/error/retry, scrolling, reduced motion, and screenshots. Remove the standalone browser boundary only after parity. This does not migrate jsdom unit tests and is not a prerequisite for the other candidates.                                                                 |
| Native sharding or project partitioning across extra machines               | Reduce elapsed CI time by adding compute. Potential gain is unmeasured and bounded by startup and the slowest partition.                                                                                                                                                                                                                                           | More machines and CI coordination.                                                                           | Deferred under the current environment constraint. Splitting into extra processes on the same saturated machine does not create extra CPU capacity.                                                                                                                                                                                           |

### 3. Preserve one native command and document the fallback limitation

The default command remains `bun run test`, whose existing script invokes `vitest run`. No new alias or custom wrapper is proposed. Existing project-filter scripts need not be expanded. Native flags used for experiments do not become additional permanent command names.

Vitest supports the following distinct behaviors:

| Native operation                      | Source of selection                                                                                                                                                | Clean checkout / initial behavior                                                                        |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| `vitest run`                          | Every configured project and discovered test unless filtered.                                                                                                      | Runs the full suite.                                                                                     |
| `vitest run --changed`                | Git identifies uncommitted files; Vitest follows their dependency graphs to test files. Staging is not required and selecting lines or functions is not supported. | With no changes, no tests are selected; this is not a full-suite fallback.                               |
| `vitest run --changed <ref>`          | Vitest derives a Git comparison against the supplied reference and follows related imports.                                                                        | Can select committed branch changes on a clean checkout; a suitable base is an explicit workflow choice. |
| `vitest --watch`                      | Initial discovery, then observed file changes and the live graph.                                                                                                  | Starts with a full run, keeps running, and reruns affected tests on edits.                               |
| `vitest related <source-files> --run` | Explicit source paths plus automatic graph analysis.                                                                                                               | Requires source-file input; recorded for completeness, not chosen as the user's normal workflow.         |

There is no verified native Vitest 4.1.7 one-shot option for "run affected tests, otherwise run everything". `passWithNoTests` affects exit behavior, not fallback selection. Combining two shell commands, a Git-reading configuration function, a custom plugin, or separate developer/release aliases would be a policy/orchestration change and is not silently substituted for the user's native-only requirement.

The desired fallback has two cases: no changed source files, and changed files that produce an empty related-test set. Neither case should be confused with a full verification. The statically discoverable module graph includes analyzable dynamic imports, but runtime-computed imports and external effects are not guaranteed. Shared source imports can correctly select nearly every test. Shared CSS, URL-loaded assets, Storybook-generated tests, setup, dependency manifests, build configuration, and backend contracts require explicit validation of graph coverage. The audit also found a `forceRerunTriggers` matching caveat for absolute paths under `.worktrees`; configuration-change coverage cannot be assumed to work there.

Before adopting a default reduced policy, verify the pinned native behavior and selection against representative component, common module, CSS, font/asset, story/setup, manifest/config, backend-contract, new-file, clean-tree, and empty-selection cases. If the desired single-command fallback remains unavailable under the accepted constraints, record the candidate as deferred and retain the current full command. Native watch remains an optional alternative requiring a later workflow decision; its existence does not satisfy the one-shot request.

### 4. Full release validation is a separate correctness gate

Any future release validation must execute the complete unit, both-browser, and six-cell visual suite without a changed/related filter, including on a clean checkout. Image publication must depend on its successful completion. Reusing the same existing package command without selection satisfies the current command contract; an eventual reduced default must explicitly resolve how full release execution is preserved using supported native configuration.

The strategy records this missing gate without implementing it. Before adopting it, establish the existing locked toolchain and Linux/browser/font dependencies in the release environment, resolve baseline validity, and decide the minimal required setup and cache steps. Do not claim that the current release workflow tests the image or application merely because it builds successfully.

### 5. Amend authoritative contracts only for selected candidates

At the experiment baseline, the [storybook-visual-regression specification](../../../specs/storybook-visual-regression/spec.md) required retained failure traces, loading every declared font, six native project identities, sequential files within each project, separate project caches, and an unfiltered complete frontend command. The [project-command-interface specification](../../../specs/project-command-interface/spec.md) preserves native dispatch and composed validation behavior. These baseline constraints informed the adoption gates.

The accepted delta now changes only the measured trace, shared preparation and visual scheduling contracts, and adds the trusted fullscreen prerequisite. It preserves font loading, native project filters, screenshot references and the unfiltered command. The new frontend performance specification captures evaluation constraints and explicit rejected/deferred outcomes. Synchronize these deltas before the completion commit; the unmeasured alternatives do not become delivered requirements.

## Risks / Trade-offs

- [Historical failures and competing work distort timings] - Use the historical table only for prioritization. Establish a successful controlled baseline before promising an overall speedup.
- [Removing traces reduces post-failure evidence] - Preserve ordinary errors and screenshot differences; validate the existing native trace flag as an explicit diagnostic escape hatch before changing its default.
- [Timeout cleanup is an incomplete diagnosis] - Compare hanging-process diagnostics before and after the smallest supported fix. Preserve timeout behavior for a real stalled screenshot.
- [Shared browser state changes test semantics] - Exercise different file orders and repeated runs. Reject candidates requiring broad state-reset infrastructure just to recover the prior guarantees.
- [Manual Storybook annotations add maintenance] - The selected public annotation API disables automatic annotation registration, so the explicit renderer/addon/preview imports must remain aligned with Storybook configuration. Private injection keys are excluded. Public provide/inject preserves per-instance globals without claiming that setup execution itself is faster.
- [Graph selection can miss non-imported effects] - Prove the affected areas and full-run triggers on the actual `.worktrees` path. Preserve full completion and release validation; defer unsupported fallback rather than label no work as successful full verification.
- [Parallelism adds instability] - Judge median wall time together with memory, failures, screenshot stability, and the slowest project. Preserve sequential interactions.
- [A faster benchmark hides less coverage] - Compare selected case identities, project cells, skip counts, reference hashes, and assertions as well as time. Do not accept regenerated references as a performance fix.
- [Current screenshots can omit content below the viewport] - [Issue #281](https://github.com/ravecat/d20/issues/281) owns the capture-contract correction. A matching existing reference is not proof of full-document coverage; no candidate may accelerate comparison by clipping content. Re-establish timings and references under that outcome when its capture contract changes.
- [Many small optimizations create maintenance cost] - Retain only reproducible useful gains, combine candidates incrementally, and reject inconclusive or fragile changes.

## Migration Plan

1. Preserve the completed planning delivery and its historical evidence. The later experiment authorization and accepted candidate decisions supersede the initial documentation-only scope.
2. After a separate experiment/implementation request, pin the current source and reference baseline, account for the favorites and #280 outcomes, and characterize any failures under their existing ownership.
3. Evaluate coordination, trace recording, and shutdown handles, then isolate the larger initialization, project-consolidation, concurrency, and font hypotheses. Record lower-priority or coverage-reducing candidates as rejected or deferred when their prerequisites are unmet.
4. Reconcile selected candidates with affected authoritative contracts before making lasting implementation changes. Preserve the same source/reference baseline while measuring each candidate.
5. Validate the accepted combination with the full native suite and the relevant existing `mix assets.lint`, `mix typecheck`, Storybook build, and `just check` gates according to touched scope. Release-gate changes also require validation that failure blocks publication.
6. Revert an unsuccessful candidate's configuration or supported dependency change and rerun its focused validation. No reference regeneration, database rollback, or unrelated worktree rollback is needed for performance configuration alone.
7. After final source validation, synchronize accepted deltas, reconcile all adopted, rejected and deliberately deferred tasks, archive the change, and run `openspec validate --all --strict --no-interactive`. Confirm the change is absent from the active list.
8. Deliver the trusted fullscreen story and its public test environment marker as the first semantic test commit. Deliver the environment optimization, public annotation setup/type declaration and finalized artifacts as the second performance commit. Validate the linear candidate and integrate it into local `master`; do not push or publish. This ordering keeps tracing changes dependent on their tested interaction prerequisite.

## Remaining limitations and deferred work

- The original screenshot shutdown delay remains; no eligible public API or verified compatible release fix was found. The rejected internal patch is not delivered.
- Trace-off, shared preparation and two isolated visual files are retained. Disabling isolation and changing font readiness gave no useful additional combined gain; unit alternatives were screened and rejected.
- Separate profiling of individual transform, lifecycle and cache costs was not performed. The measured gain belongs to the tested configuration change, not to every internal phase independently.
- Native watch is optional feedback only. Reduced default selection and an affected-or-full fallback remain deferred; the existing full command is unchanged.
- Browser migration #280 and screenshot clipping #281 remain independently owned. No release setup, publication gate or deploy-speed improvement is delivered by this change.

No blocking implementation question remains after review. The detailed ledger in experiment-02-public-api-sweep.md records evidence and dispositions. Experiment 1 remains rejected and no internal patch gain is included.

## Authorized implementation sweep

Evaluate native trace recording, browser isolation, bounded file concurrency, rendered-font readiness, and unit execution settings with equivalent focused workloads. Adopt only repeatable gains with preserved outcomes. Reconcile affected visual requirements before any lasting config/setup change. Investigate project consolidation and selection through public APIs; defer unsupported or independently owned changes. Preserve the unfiltered default command and existing deployment behavior. Full-suite checks of the selected combination are validation, not per-candidate research. The sweep originally left code and artifacts uncommitted for review. The later user request now authorizes committing and integrating the accepted result into local `master` after final validation and artifact reconciliation.

### Public consolidation candidate

A supported candidate uses one `storybookTest` plugin and six named `browser.instances`. Each instance supplies serializable `visualGlobals` through Vitest `provide`; the existing setup reads it with `inject` and uses the framework public `setProjectAnnotations` API with explicit registered addon previews and the repository preview. The plugin continues to own its setup, story transformation, lifecycle and assertions. Do not import addon internals or reproduce its private injection keys. Do not register duplicate beforeAll hooks. Preserve each native instance name and its screenshot/diff paths using public resolver arguments. Prove all six cells and addon checks before adoption. This replaces six mutable Vite caches with one cache owned by one server.

Trace-off is a candidate default after three passing equivalent pairs (16.49 to 14.87 seconds median); validate native trace re-enablement and failure screenshots before retaining it. Any consolidation, isolation, worker, and font candidates remain conditional on their own measurements and correctness checks.

Consolidation smoke testing exposed a public-provider limitation: concurrent instances sharing a trace directory failed in Playwright `tracing.stopChunk` with an unexpected byte count. Per-instance trace directories are not supported by the installed public instance options. The consolidation comparison therefore holds independently measured trace-off constant in both baseline and candidate. Do not use invalid trace-enabled smoke timings as speed evidence. A retained combination must verify an explicit single-instance, single-file `--browser.trace on` diagnostic rerun; multi-instance tracing is not claimed reliable.

### Rendered-font readiness candidate

Replace eager loading of all declared Unicode subsets with public `document.fonts.ready`, then propagate any attempted face failure by awaiting `font.loaded` for faces whose status is `error`. CSS Font Loading readiness includes required font loads and font-dependent layout; current Chromium flushes pending layout in its ready getter. Keep all production font imports and the original screenshot budget. The explicit contract change is that never-used font resources are no longer fetched or validated in every story. Retain only after unchanged-reference comparisons, Latin/Cyrillic/math readiness and required-load failure probes, and equivalent timing evidence. No manual glyph lists or font matching implementation.

### Trusted fullscreen activation prerequisite

Broader validation found the same Workspace fullscreen-entry failure with trace-off in both original and consolidated configurations. The story's synthetic Testing Library click does not provide the trusted user activation required by Fullscreen API. Trace-dependent success is not a valid interaction guarantee. Characterize activation and test the public Vitest Browser native click for only fullscreen entry, preserving every assertion, keyboard step and exit action. Finish the native pointer action with public `unhover` so an unintended hover does not alter the accepted capture. The visual project's documented `test.env` supplies `VITEST: 'true'`; the story checks `import.meta.env.VITEST === 'true'` before dynamically importing `vitest/browser`. Ordinary Storybook keeps its existing click path. This small story-harness correction is part of making trace-off valid, not a production or fullscreen-policy change. Verify default and custom Vitest modes, all six visual cells, trusted click evidence, and normal production Storybook build. The runner marker is independent of Vite mode; review rejected the initial mode-based predicate. Supply the same marker in both comparison configurations so the corrected interaction harness stays fixed. If the public fix does not establish parity, reject tracing/consolidation rather than suppressing the failed assertion.

Rendered-font readiness verdict: rejected as inconclusive after three matched four-story pairs, median 14.33 to 14.31 seconds (0.02 seconds), overlapping ranges. The eager all-face hook is restored. The proposed font contract delta is removed; adoption-only multilingual/failure probes are unnecessary because no font change is retained.

### Worker-group compatibility

The first unfiltered final attempt exited before any tests because Vitest 4.1.7 rejects projects with different explicit maxWorkers in the same sequence.groupOrder. The visual project therefore uses documented `sequence.groupOrder: 1`, preserving the original unit/browser worker settings in group 0. This ordering is a compatibility prerequisite for the measured two-worker setting, not an independently measured speedup. A small mixed-pool scope validates grouping before the full retry. The single-visual-project paired measurements remain scoped evidence; full-run ordering effects are reported separately.

## Accepted result for local delivery

The measured combination retains shared preparation for six named visual instances, trace-off with focused diagnostic tracing, and two parallel isolated files per instance. Public globals and complete renderer/addon annotations preserve the matrix; the trusted fullscreen story correction holds assertions and references fixed. Fonts, unit configuration and default isolation remain unchanged because their screened alternatives did not establish a useful combined gain.

Three matched 11-case pairs give median wall time 23.90 to 15.98 seconds, a 7.92-second / 33.14% reduction. The earlier sweep full validation passes 481 cases with two existing skips in 114.49 seconds across all 100 files. After the inline review correction, the final commit candidate passes the same full inventory in 151.783 seconds wall (143.56 seconds internal). That single delivery verification is not a controlled regression or speed comparison. The historical failed full run is not a controlled comparator. All 322 references and dependency hashes remain unchanged. Native watch separately yields 1.056-second median warm four-case feedback; changed/related probes do not establish a safe full fallback, so reduced default selection and release orchestration remain deferred.

The detailed evidence, invalid probes, diagnostic checks and candidate ledger live in experiment-02-public-api-sweep.md. The user has accepted local delivery in two semantic commits. Final validation passes and accepted deltas are synchronized and archived for the environment completion commit; local integration follows validation of the linear candidate and is not yet claimed complete. No push is authorized.

README files keep general development and story-authoring guidance. Sweep-specific tuning advice, scheduling and annotation details, diagnostic recipes, limitations and timing evidence belong in this change and its experiment report, rather than additional README instructions.

Review correction: keep the visual project literal inside the main `test.projects` array and construct its six instances inline under `browser.instances`. Remove the detached matrix/project variables and name-to-globals map. Screenshot and diff resolvers read the same per-instance `visualGlobals` through public [`TestProject.getProvidedContext()`](https://v4.vitest.dev/api/advanced/test-project#getprovidedcontext). Preserve all runtime settings, instance identities and artifact paths; validate the refactor with one story file across all six cells.
