The public-API sweep and review corrections are accepted for local delivery. The user now authorizes two semantic commits and integration into `master`, superseding the earlier uncommitted-review hold. Commit the trusted fullscreen story prerequisite first, then the Vitest environment optimization with synchronized and archived delivery records. No push or publication is requested. Retain the small-scope research rule, measure each viable candidate sequentially without competing test runners, and validate the final combination. Experiment 1 remains rejected. Record unsupported, inconclusive, coverage-reducing, and independently owned candidates explicitly instead of forcing adoption.

## 1. Establish an equivalent measurement baseline

- [x] 1.1 After an experiment request, record its scope in issue #282 and these artifacts; pin the current committed source, lockfile, reference hashes, runtime/browser versions, machine, cache conditions, and case/project inventory without importing uncommitted favorites work.
- [x] 1.2 Review current failures and the ownership of #280 browser migration and #281 screenshot clipping; record their impact on baseline validity without absorbing those outcomes or accepting changed references for speed.
- [x] 1.3 Coordinate non-overlapping runs and establish a passing small visual probe with existing `bun run test` plus native diagnostic/project flags and `--update none`; record at least three baseline samples and distinguish wall time, Vitest duration, test execution, and shutdown. Per the user's research-scope correction, do not repeat the full suite for each candidate; task 5.3 owns eventual complete validation.

## 2. Evaluate the lowest-cost execution candidates

- [x] 2.1 Compare `--browser.trace off` with the current trace mode in alternating paired runs; record median/range, identical results, screenshot failure evidence, and native trace re-enablement before deciding adopted, rejected, or deferred.
- [x] 2.2 Reproduce the shutdown handles with the native `hanging-process` reporter and identify a narrowly supported timer cleanup fix or compatible dependency update; record its source, scope, and maintenance tradeoff. The internal patch is rejected; no eligible released fix or public API was verified. See experiment-01-shutdown.md.
- [x] 2.3 Verify successful exit and real stalled-capture timeout behavior without reducing the screenshot or teardown budgets; measure actual focused-run savings rather than subtracting 10 seconds as a result. Recorded reduction: 7.85 seconds (47.23%), all measured tests pass, but the implementation method is rejected and the gain is not adopted. See experiment-01-shutdown.md.
- [x] 2.4 Reconcile user rejection of the internal patch, verify restoration with the focused native tests and runtime hash, and record the public-interface-only constraint before another candidate begins. Original runtime restored; 4/4 tests pass in 17.48 seconds, assets diff empty. See experiment-01-shutdown.md.

## 3. Select larger opportunities using the measured bottleneck

- [x] 3.1 Profile repeated initialization, transforms, story lifecycle, fonts, and resource contention; selected trace/isolation/consolidation/font/concurrency experiments are recorded in experiment-02-public-api-sweep.md. Unit changes were screened and rejected; remaining directions are reconciled in the ledger.
- [x] 3.2 Evaluate visual isolation in the original configuration (three passing pairs, 8.02%); screen it again in the combined configuration (0.09 seconds, inconclusive). Retain default isolation rather than shared-state risk; final paired runs use changed file order. No cleanup replacement is introduced.
- [x] 3.3 Establish and benchmark a public six-instance Storybook configuration: matched 24-case medians 23.95 to 17.29 seconds; corrected interactive 66-case matrix passes with unchanged references. Per-instance provide/inject plus public annotations preserve globals, paths, names and addons; trace limitation and trusted fullscreen prerequisite are documented.
- [x] 3.4 If selected, compare a small native worker/file-concurrency range and font-readiness alternatives separately; preserve sequential interactions, all glyphs and languages, full capture contracts, and exact accepted images while recording memory, failures, and wall time.
- [x] 3.5 Record decisions for repeated play/a11y work, persistent watch, unit pools/environment/cache, separately owned #280 migration, and extra-machine sharding; reject coverage reduction and keep unsupported or out-of-scope options deferred without implementing them.

## 4. Resolve automatic selection and full delivery semantics

- [x] 4.1 Screen native changed/watch behavior: changed discovery selected seven files rather than the full suite, and three four-case watch reruns had median 1.056 seconds. Record the `.worktrees` full-rerun trigger caveat. The exhaustive source/setup/asset/config/backend/reference selection matrix is deliberately deferred because reduced default selection is not adopted.
- [x] 4.2 Resolve selection disposition: the README related-set probe selected no tests and exited zero without fallback. Clean-tree behavior is documented/source-verified, not empirically rerun by hiding the review diff. No supported affected-or-full fallback was verified; preserve `bun run test` and defer reduced selection without wrappers or manual impact lists.
- [x] 4.3 Record release-policy disposition: reduced default selection is not adopted, so release setup/publication-gate implementation is deliberately deferred. The existing command remains full; any future reduced policy still requires the documented complete release gate before adoption.

## 5. Adopt only verified candidates and reconcile delivery

- [x] 5.1 Before lasting implementation, update issue #282 and every affected proposal, design, task, and authoritative requirement delta for the selected candidates, including trace/font/project/command contracts where applicable; record explicit rejected/deferred outcomes for other candidates.
- [x] 5.2 Apply only candidates covered by the later implementation request, preserving source and reference ownership; if reduced development selection is adopted, implement and verify the required full release gate before treating the policy as delivered.
- [x] 5.3 Measure the selected combination against the fixed baseline with three alternating pairs on the same small representative scope; run the complete native final suite once for inventory/outcome validation. Respect the user's fast-feedback constraint rather than repeating the eleven-minute full baseline per candidate. Report scoped wall-time savings, full-run outcomes, diagnostic behavior, remaining limitations, and rollback verification without adding independent estimates or presenting historical failing runs as a controlled speed comparison.
- [x] 5.4 Validate the touched frontend scope: full `bun run test --update none`, `mix assets.lint`, `mix typecheck`, `bun run storybook:build`, `bun run lint`, and `bun run format.check` pass. OpenSpec strict validation passes all 86 items; `git diff --check` passes. No backend, CI or release behavior changes, so cross-stack `just check` and publication-failure validation are not applicable to this sweep.
- [x] 5.5 Reconcile the accepted README simplification and inline configuration after final source validation; synchronize the adopted deltas, archive through OpenSpec, run `openspec validate --all --strict --no-interactive`, and confirm absence from `openspec list --json`. Final native validation passes all 100 files with 481 passed and two existing skips in 151.783 seconds wall; formatting, lint, types and Storybook build pass and reference/manifest/lock hashes are unchanged. Rejected/deferred candidate decisions remain explicit. The story prerequisite is committed as `f3c7015a`; the environment completion commit and validated local `master` integration are authorized next delivery operations, without push or publication.
