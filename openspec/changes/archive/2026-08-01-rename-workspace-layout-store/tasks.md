## 1. Naming

- [x] 1.1 Rename the local `createStore` binding to `layout` and update every read, subscription, and trigger reference.

## 2. Validation

- [x] 2.1 Format and lint the workspace model, run focused workspace model tests, and run frontend type checking.
- [x] 2.2 Run strict OpenSpec validation for `rename-workspace-layout-store`.
