## 1. Lock the presentation regression boundary

- [x] 1.1 Cover 0/1/2/3/4/5/8/31/32 Games in Home Storybook scenarios with screenshot comparison and semantic play assertions for exact hero-prefix/compact-suffix membership, order, empty lanes, singletons and one canonical link per input. Preserve the recorded failing-base characterization as historical evidence.
- [x] 1.2 Cover local/numeric routes, named/empty-title fallback, image/thumbnail metadata, nullable stage and allowed Playable overlap in Storybook. Remove Home unit assertions of loop-copy/inert/tabIndex mechanics and delete the redundant unit suite after migration.

## 2. Implement disjoint lanes with the existing CSS machinery

- [x] 2.1 Update `assets/js/pages/home/ui/home.svelte` with inferred derived contiguous lane arrays, inline canonical links in both lanes, pointer-activatable loop-copy links with per-slide `aria-hidden` and anchor `tabindex="-1"` (no Games `inert`), lane-local count/singleton inputs and conditional compact rendering. Preserve server props, selection/order, routes, card geometry and fallback behavior; add no library or extracted carousel framework.
- [x] 2.2 Apply lane-local logical counts to stepped loops, short-loop tail fractions, singleton selectors and compact centering while retaining shared Games pause handling and limiting displacement/overflow reveal to keyboard-visible focus so pointer clicks do not move their targets. Preserve Playable selection/geometry and its independent pause scope. Set the shared CSS motion to a five-second dwell plus a 1.1-second `ease-in-out` movement per 6.1-second cycle, with no JavaScript carousel state or timers.
- [x] 2.3 Run Home Storybook visual comparisons with semantic play checks at desktop/tablet/mobile and verify the migrated application scenarios. Keep separate browser coverage only for native focus/reveal, pointer/Enter delivery and reduced-motion presentation; remove the redundant singleton browser scenario/baselines.

- [x] 2.4 Rename the carousel CSS custom property `--n` to `--slide-count` in all three lane inputs, CSS formulas and explanatory comments. Preserve counts and behavior; verify existing Home Storybook coverage and scoped formatting/lint without adding implementation-detail tests.

- [x] 2.5 Simplify all three card loops to `aria-label={title || "Open game"}` and remove per-card `titleId`, heading IDs and card `aria-labelledby`. Preserve section naming, loop-copy accessibility and rendering keys; preserve overlap/name coverage in Home stories and run focused visual checks plus scoped formatting/lint.

## 3. Verify actual behavior, presentation and motion

- [x] 3.1 Update `assets/tests/pages/home/ui/home.browser.test.ts` for canonical links across both Games lanes, hero/compact link activation including currently visible loop copies, and narrow focus screenshots. Cover complete 32-link forward/reverse reachability and reduced-motion access, including the final compact entry; retain existing Playable regressions. Run `cd /home/max/apps/d20/.worktrees/work3/assets && env -u BASH_ENV bun run test:browser -- tests/pages/home/ui/home.browser.test.ts` for Chromium/Firefox, refreshing only affected Home screenshots when needed. Record the existing Firefox reduced-motion skip separately. Do not inspect/seek animations or assert timing/geometry.
- [x] 3.2 Reuse/update deterministic Home fixtures and public/authenticated Home stories for disjoint composition, 0/1/2/3/5/32 Games, small compact loops, fallback/long-name/badge content and reduced motion where appropriate. Run `cd /home/max/apps/d20/.worktrees/work3/assets && env -u BASH_ENV bun run test:visual -- stories/pages/public/home.stories.ts stories/pages/authenticated/home.stories.ts --update`, review only affected Home baselines, then rerun without `--update`. Confirm desktop/tablet/mobile centering, symmetric neighbors, hero proportions and singleton geometry. Do not refresh unrelated screenshots.
- [x] 3.3 Reverify existing unchanged backend sample contracts with `cd /home/max/apps/d20/.worktrees/work3 && mix test test/d20/games/sources/board_game_geek_test.exs test/d20/games_test.exs test/d20_web/controllers/page_controller_test.exs`. Use native work3 setup if required; do not reset a database or alter shared runtime resources. Confirm default 32 bound, Hot uniqueness, small/empty results, metadata/error handling and permitted Playable overlap without changing provider/controller behavior.
- [x] 3.4 Run `cd /home/max/apps/d20/.worktrees/work3/assets && env -u BASH_ENV bun run format.check && env -u BASH_ENV bun run lint && env -u BASH_ENV bun run typecheck`, then `cd /home/max/apps/d20/.worktrees/work3 && env -u BASH_ENV mix assets.build` to check compatibility and CSS compilation. Record exact failures instead of fixing unrelated baseline issues; broaden to `just check` only if implementation scope/risk expands.
- [x] 3.5 Perform and record the design's natural-playback review on actual work3-rendered markup/CSS: 5-card 2/3 independent wraps, 3-card 1/2 compact wrap and a full 32-card 8/24 compact loop with intervening hero wraps. Sample natural movement and wrapping, responsive compact centering, shared hover pause, focus after advancement and movement between lanes. Combine this manual presentation evidence with the native browser suite for pointer/Enter delivery, exit/resume and complete reduced-motion keyboard access. Record URL/build provenance, viewport, motion setting, observations/recording and relevant console results in these owning artifacts and #272. Use the task-owned work3 Storybook server in the existing D20 development tab under the latest completion authorization; restore its original URL afterward and preserve existing servers. Static screenshots or CSS-only master previews cannot complete this task.

## 4. Reconcile the new delivery only

- [x] 4.1 Record final review/validation evidence and update this follow-up in issue #272. Verify the scoped diff and reconcile every artifact. The latest user request supersedes the earlier no-commit/no-transfer restriction and authorizes one semantic local completion commit and transfer to master.
- [x] 4.2 Synchronize and archive split-home-carousel-lanes through the native OpenSpec lifecycle after all required validation, run openspec validate --all --strict --no-interactive and mix openspec.check, and verify absence from openspec list --json before committing. Do not publish remotely or close the broader issue solely for this step. Local transfer and source retirement are post-commit lifecycle operations recorded in issue #272, not prerequisites for creating this completion commit.

## Historical implementation progress (2026-09-09)

These entries record intermediate states; the final review below supersedes old test allocations, pending approvals and branch positions.

- User authorized implementation in the existing work3 workspace and clarified that every visible Games card must open its internal game detail page, including large/small loop copies. Copy accessibility treatment was revised consistently before dependent implementation.
- Initial work3 Home unit run characterized the unfinished component: 9 passed and 10 failed, caused by missing compact anchor attributes. This is an in-scope implementation regression, not a clean-base failure.
- Issue #272 is Open/In Progress in D20 Project 5. Work3 stays at original base `64f6e824`; master now contains the same requested motion cadence in amended commit `d9335ba6`.

- Component/unit implementation verified: all 19 Home unit tests pass. The split matrix failed against the committed repeated-row base (8 failed, 1 passed); with semantic lane labels alone applied to that base it still failed (7 failed, 2 passed). The exact working component was restored after each characterization. Scoped ESLint, Oxfmt and `git diff --check` pass.

- `mix test test/d20/games/sources/board_game_geek_test.exs test/d20/games_test.exs test/d20_web/controllers/page_controller_test.exs` passed 133 tests with no failures. Expected malformed XML and metadata fallback scenarios logged errors/warnings; no backend files changed.
- Issue #272 now records the clickable-copy clarification, verified by re-reading its body; state remains Open/In Progress.

- Browser verification reproduced a pointer regression in both Chromium and Firefox: the existing `:focus-within` displacement reset moved visible compact copies between pointer down/up and prevented navigation. The root correction keeps shared focus pause but scopes track/overflow reveal to `:has(:focus-visible)`. The click regression remains part of browser acceptance.

- Full frontend `format.check` and `lint` passed. Final `typecheck` passed with zero Svelte errors/warnings after replacing unsupported test-only `toReversed()` usage with a copied-array reverse. `env -u BASH_ENV mix assets.build` passed; Vite emitted its existing large-chunk advisory.
- Parent visually inspected the 32-game desktop layout and three-game mobile layout: disjoint row content, centered compact middle card and symmetric clipped neighbors remain intact. Requested a revised compact-metadata fixture to center the long title/badge for direct narrow-screen review.

- Browser click regression was corrected using keyboard-visible-focus-only reveal; visible-copy pointer navigation passes Chromium and Firefox. Initial complete browser run passed 12 cases with two Firefox reduced-motion skips. Full Home visual comparison passed 63 cases. Screenshot inspection then exposed the final compact canonical card partially clipped after Tab; acceptance remains pending until its full focus reveal is corrected and reverified.
- `mix openspec.check` passed strict validation and lifecycle checks for ten active changes. No archival has occurred while browser delivery obligations remain outstanding.

- Full final-card keyboard reveal is now verified in Chromium/Firefox screenshots, including Chromium reduced motion: native card scroll margins plus keyboard-focus-only viewport scroll padding keep the final compact card and outline fully visible. No scroll snap or scripted scrolling was introduced. The updated narrow Compact Metadata story centers its long title and badge; three viewport comparisons pass after visual review.

## Historical verification before final review

- Card-label simplification: removed the three `titleId` constants, card `aria-labelledby` attributes and `h3` IDs. All card links now use `aria-label={title || "Open game"}`. Existing overlap tests assert accessible names, routes and canonical link counts instead of generated IDs. All 19 Home unit tests and scoped Oxfmt/ESLint checks pass. Section naming and loop-copy accessibility remain intact; task 2.5 is verified without completing the prior natural-playback/finalization obligations.
- Readability refinement: renamed all 11 `--n` occurrences in `home.svelte` to `--slide-count`, with only formatter wrapping beyond the rename. Existing Home unit tests passed 19/19 before and after; scoped Oxfmt and ESLint passed. No test or screenshot changes were needed. This completes task 2.4 only and leaves the existing natural-playback and finalization obligations pending.
- Automated obligations complete: 19 Home unit tests; 12 Chromium/Firefox browser tests with two Firefox CDP reduced-motion skips; 133 focused backend tests; 63 Home visual scenarios plus the corrected Compact Metadata scenario in three viewports. Browser and visual comparisons passed without baseline update flags after review.
- Final frontend format, lint, typecheck (zero Svelte errors/warnings), and production asset build passed. Strict OpenSpec validation passed all 83 items; lifecycle validation passed ten active changes. Diff whitespace checks pass and the index is empty. Vite retains the large-chunk advisory; visual runners exited successfully with the existing shutdown-timeout warning. No unrelated baseline changes or failure screenshot artifacts remain.
- Natural-playback task 3.5 is still pending. Chrome DevTools exposes `http://localhost:5000/` from master, with no prepared work3 page. The user was asked to approve a separate work3 Storybook/browser target; no approval has arrived, and no alternate page or persistent server was created. Actual 3/5/32-game loop playback, shared hover/resume after advancement, and hero-copy pointer navigation during playback are not claimed as observed.
- Tasks 4.1 and 4.2 remain incomplete because natural playback has not been verified. Keep this change active and all edits unstaged/uncommitted in work3. After the approved browser target is available, finish 3.5, record evidence, reconcile the issue and synchronize/archive through OpenSpec.

## Final review and integration authorization

The user requested auditing Home tests for screenshot-first coverage, retaining separate tests only for behavior not faithfully represented in Storybook, then committing and transferring the functionality to master. The subsequent naming clarification selects `--slide-count`. Review found no animation-engine assertions but identified redundant jsdom coverage and copy/inert/tabIndex implementation assertions. The tasks above now describe the final allocation; their earlier unit counts are historical, not the final suite. The completion request authorizes necessary temporary work3 browser validation and supersedes prior no-commit/no-transfer notes.

- Final automated review: 78 Home Storybook scenarios pass across desktop/tablet/mobile on both update and non-update runs. Native browser coverage passes 10 checks with two expected Firefox reduced-motion skips. Full TypeScript/Svelte validation and scoped Oxfmt/ESLint pass. Fifteen new Home screenshot baselines were visually inspected; two obsolete singleton browser baselines were removed. The successful visual runner retains its existing teardown-timeout warning.
- Source reconciliation: rebased onto master `d9335ba6`; resolved a comment-only autostash conflict and verified the entire component and final story/browser-test sources match the pre-rebase tested content.

- Final source/test review found no product defects or lost semantic coverage. Corrected two stale delta statements so Storybook and native browser coverage have explicit, consistent ownership; no extra presentation requests are established by source review rather than request-mock assertions.
- Final full frontend `bun run check` (Oxfmt and ESLint) and `bun run typecheck` pass after rebasing; Svelte reports zero errors/warnings. The production asset build also passed on the identical final product source.
- Broad `CI=true nix develop --command env -u BASH_ENV just check` reached 871/872 backend tests, then reported the existing `games_stage_domain` versus `games_launch_stage_requires_engine` constraint-name failure at `test/d20/games/game_test.exs:66`, and unchanged Credo nesting findings at `lib/d20/koala_rescue_club/game.ex:250,345,383`. The focused constraint test reproduces on clean master `d9335ba6`; both affected files match master exactly. These unrelated baseline failures do not block this scoped frontend delivery. The broad command stopped before subsequent checks; standalone passing frontend and OpenSpec commands are recorded separately.

## Final natural-playback evidence (2026-09-09 UTC)

- Used the task-owned work3 Storybook on port 6006 in the existing D20 tab. Its Vite stylesheet path identifies `assets/js/pages/home/ui/home.svelte` under work3. The global `.storybook/preview.css` reset suppresses animations even in manual stories; disabled that one stylesheet in the inspected page only, leaving product CSS intact. Earlier screenshots with the reset enabled are static evidence only. No playback seeking, timers or source changes were introduced.
- Motion-enabled `home--thirty-two-browse-games` ran from 17:26:57 through 17:30:31, longer than the 146.4-second compact loop and intervening 48.8-second hero loops. Sampled frames show both lanes advancing, including intermediate translation, with populated neighboring cards and no observed empty runway. This is sampled visual review, not a frame-by-frame proof of interpolation.
- Motion-enabled `home--five-browse-games` ran from 17:31:20 through subsequent focus review, spanning its independent 12.2/18.3-second loops. The sampled wrapped layout retained separate hero and compact content. Native Tab moved from the header into the hero and then the compact lane after advancement; the focused Deep Sea Adventure card and its outline were fully visible.
- Motion-enabled `home--three-browse-games` ran from 17:34:31 through repeated 12.2-second compact loops. Voyages remained stationary in the hero while Death Valley and Deep Sea Adventure exchanged compact positions. Hovering the Games heading held the visible composition across successive captures; the pointer was then moved back to the header. The final sampled frame showed the same card, so that frame alone is not evidence of resumed displacement. No blank gap appeared in inspected states.
- The browser window changed from 1397x808 to 1118x646 CSS pixels during review; reduced motion was false. Dedicated desktop/tablet/mobile comparisons cover responsive geometry; the native suite supplies complete 32-link forward/reverse access, reduced-motion screenshots/access and real pointer/Enter delivery for visible copies. Those interactions are not claimed as separately repeated manually.
- Temporary visual evidence was inspected at `/tmp/d20-work3-32-running.jpeg`, `/tmp/d20-work3-32-after-loop.jpeg`, `/tmp/d20-work3-five-after-wrap.jpeg`, `/tmp/d20-work3-five-compact-focus.jpeg`, and `/tmp/d20-work3-three-{running,paused,paused-later,resumed}.jpeg`. The original page URL `http://localhost:5000/games/357873` was restored afterward, which also removes the temporary harness-style override. The existing master server was preserved.
- Console inspection showed Storybook's expected missing Phoenix WebSocket endpoint on port 6006, a browser `runtime.lastError` message-channel warning and one unattributed `Uncaught (in promise)` without a source/stack. No component error was identified; the unattributed message is not asserted to be a product defect or a clean-console pass. Successful browser/story runs provide the executable application-error checks.

Issue #272 now records the final test allocation, authorization and baseline failures; it remains Open/In Progress for its broader scope. The final semantic completion commit includes this reconciled change and its synchronized authoritative specification. Local master transfer uses the already-reconciled d9335ba6 base and fast-forward only; its result and source retirement are recorded in the issue after target verification.

- Native `openspec archive split-home-carousel-lanes --yes` synchronized eight modified requirements and archived this change. Its sole unchecked task at invocation was archival itself, now complete. Strict validation passes all 82 remaining items; the change is absent from `openspec list --json`. The task-owned Storybook process was stopped after restoring the original browser page.
- `mix openspec.check` passed the lifecycle check for nine active changes.
