## 1. Persistence and authoritative identity

- [x] 1.1 Extend the resolved detail in `lib/d20/games.ex` with its authoritative BGG ID; cover local metadata fallback, provider-only identity, raw-string delegation, and numeric-local-slug precedence in `test/d20/games_test.exs` without changing route association.
- [x] 1.2 Generate the additive migration with `mix ecto.gen.migration create_game_interests`; add an autogenerated interest-prefixed TypeID primary key and account/game unique index, positive BGG constraint, user foreign key with delete cascade, UTC first-request timestamp, and BGG index. Validate forward and reverse migration in a disposable test database without resetting shared data.
- [x] 1.3 Add `D20.Games.Interest` and expose interest operations through `D20.Games.Interests`, with insert-if-absent inside the request operation, account membership, and internal grouped counts. Test duplicate/concurrent insertion, independent accounts/games, first timestamp preservation, foreign-key/check constraints, account deletion, and non-duplicate failure propagation in `test/d20/games/interests_test.exs`.

- [x] 1.4 Replace the composite primary key with an autogenerated interest-prefixed TypeID and preserve account/game uniqueness. Verify persisted typed identity, duplicate ID preservation, and migration up/down/up in an isolated database.

## 2. Page props and authenticated submission

- [x] 2.1 Rename the detail-only `can_launch_game` / `canLaunchGame` contract to required `playable` across `PageController`, page consumers, and affected fixtures/tests; preserve the existing predicate and both Session-creation boundaries. Add `interest` with the original-route action URL and account membership; retain the resolved BGG ID only in the server detail result. Inline the single-use `render_game/4` body into the local `render_game_detail/3` clause and verify unchanged page behavior.
- [x] 2.2 Add `POST /games/:slug/interest` and `GameInterestController` using existing Inertia, CSRF, and authenticated-user protections without sudo. Enforce ordinary detail visibility and fresh non-playability before inserting under the server account.
- [x] 2.3 Return 303 redirects to the original detail route with persisted membership on success and scoped recoverable persistence errors on failure. Silently redirect already playable submissions without writing or assigning messages; verify no-op behavior and real persistence error feedback. Preserve current 404 behavior for missing/provider-failed/hidden details and prevent a supplied Session ID or user ID from bypassing checks.
- [x] 2.4 Add `test/d20_web/controllers/game_interest_controller_test.exs` for authentication, no replay, idempotency, numeric normalization/local-slug collisions, changed BGG binding, newly playable state, provider failure, local metadata fallback, and safe original-route redirects. Update `page_controller_test.exs` for new props and existing-Session precedence.

- [x] 2.5 Expose `D20.Games.Interests.request/2`, `requested?/2`, and `counts/0` directly to controllers and other callers. Remove the redundant Games interest delegates and their unused aliases; move public operation documentation to Interests. Preserve route resolution, visibility/playability checks, inline insertion, result handling, and HTTP behavior. Update all callers and focused context/controller/page tests.

- [x] 2.6 Remove the expected-identity helper, error, request argument, form payload, and page prop. Use current route resolution through `Interests.request/2`; verify empty-payload submission, ignored client identities, original-route selection, and refreshed bindings. Update frontend fixtures and run focused backend/frontend tests, lint, and type checks.

- [x] 2.7 Add `Games.visible?/1` for configured local-game stage visibility and reuse it in PageController, Games.Interests, and session launch eligibility. Keep SQL list scopes and ordinary fetch behavior intact. Test stage configuration independently of enabled/engine state and validate dependent page, interest, and module/session-launch paths.

- [x] 2.8 Rename the general interest-error field to `message` inside the `interest` error bag across server assignment, both page consumers, and the scoped callback. Update existing HTTP/browser expectations, verify a distinct server message is preserved, and run focused tests, formatting, lint, type checks, and strict OpenSpec validation.

## 3. Alternative action and account flow

- [x] 3.1 Rename `LaunchForm` / `assets/js/pages/game/ui/launch_form.svelte` to `SessionForm` / `assets/js/pages/game/ui/session_form.svelte`; preserve Session creation behavior and update imports, exports, and existing test/story consumers without a compatibility alias. Update `game.svelte` to select existing Lobby first, then `SessionForm` for playable with schema, then `InterestForm` only for `playable: false`. Preserve metadata/layout and reject schema absence as a reason to infer non-playability.
- [x] 3.2 Add `InterestForm` in `assets/js/pages/game/ui/interest_form.svelte` as the alternative submission form with the initial single visible button `I want this game!`, full-width primary treatment, no game setup fields or `BasicForm`, original-route submission, pending state, persisted saved state, and retryable errors. Use the Inertia `Form` component directly and keep `SessionForm` dedicated to Session creation.
- [x] 3.3 Reuse the shared auth store/dialog for guests with the original safe return path; verify cancellation and successful login produce no request until explicit authenticated submission. Preserve provider-only account support.
- [x] 3.4 Update `assets/tests/pages/game/ui/game.test.ts` and focused `SessionForm` / `InterestForm` tests for local/provider/disabled/engine-less cases, form selection, Session precedence, missing schema, saved reload state, pending/error/retry feedback, initial single-button content, exact button copy, keyboard semantics, and auth behavior.

- [x] 3.5 Add `/games/:slug` Storybook page stories for playable without a Session, a waiting Session, unavailable without a Session, and saved interest. Reuse the production wide layout and deterministic transport mocks with cleanup; validate semantic activation assertions, nearby layout tests, reviewed desktop/tablet/mobile references and a normal comparison, lint/type checks, Storybook build, and the existing Storybook browser tab.

- [x] 3.6 Add the authoritative per-game `interest.count` through Interests and page props. Show count, five-point star, then the action label with an accessible count description; preserve pending/saved/error behavior. Cover zero, distinct accounts, duplicate requests, shared route identity, account deletion, refreshed props, and narrow layouts. Update related fixtures/stories and review changed visual references; run focused backend/frontend tests, formatting, lint/type checks, and strict OpenSpec validation.

- [x] 3.7 Confirm saved interest through the disabled `Requested` button without a separate success message below it. Preserve pending and retryable-error behavior and suppress stale errors after confirmed membership. Update existing component and Storybook assertions, review affected Requested screenshots, and run focused frontend checks and strict OpenSpec validation.

- [x] 3.9 Replace manual interest submission with the direct Svelte Inertia `Form` component, using its processing state and scoped server errors. Preserve guest cancellation/no replay, server membership, count/star, and Requested-only confirmation. Use standard Inertia transport/HTTP failure handling without custom callbacks. Keep the existing adapter version unless a supported API needed here requires an upgrade. Update focused browser tests and shared Form mocks only as required; validate related Form consumers, normal game-story comparisons, formatting/lint/type checks, real adapter behavior, and strict OpenSpec validation.

## 4. Verification and delivery reconciliation

- [x] 4.1 Format touched Elixir files with `mix format <files>`, check touched frontend files through existing formatter/linter tooling, and run `mix test test/d20/games_test.exs test/d20/games/interests_test.exs test/d20_web/controllers/page_controller_test.exs test/d20_web/controllers/game_interest_controller_test.exs`.
- [x] 4.2 From `assets/`, run `bun run test -- tests/pages/game/ui/game.test.ts` and any new interest test path; run `mix assets.lint` and `mix typecheck` for the changed page contract.
- [x] 4.3 Use `$devtools-validations` for an actual local and provider-only detail: authenticated save/reload/duplicate, guest login/cancel/no replay, visible pending/error feedback, keyboard and narrow viewport access, and an unchanged existing Session. Verify internal grouped counts against persisted requests.
- [x] 4.4 Run `just check` for this cross-stack persistence change; record migration validation and application rollback notes, retaining collected data during ordinary rollback.
- [x] 4.5 Reconcile issue #275 acceptance criteria and affected specifications after feature checks pass and the user accepts continuation with the recorded unrelated baseline findings. Synchronize and archive this OpenSpec change through the native lifecycle, run `openspec validate --all --strict --no-interactive`, and verify `openspec list --json` no longer lists it. Keep each behavior with its relevant tests and artifacts in the logical commit sequence authorized by the user; record post-integration reconciliation in a documentation-only finalization commit.
- [x] 4.6 Create separate commits for reusable mocks, the interest feature with its tests, and game-page stories with their screenshot baselines. Rebase the unpublished candidate onto pinned local master, validate it, fast-forward master, and validate the integrated target. Record final worktree retirement in issue #275 after transferring the archival commit, so cleanup is confirmed after execution rather than claimed in advance.

## Validation record - 2026-09-10

Implementation is present in `worktree/game-interest`, based on `5a28c182`. Task 4.5 remains pending: the repository completion gate requires passing broad checks before synchronization, archival, and tracker completion. This implementation checkpoint does not close issue #275 or claim delivery finalization.

- Final focused backend command from 4.1 passed: 120 tests, including independent-connection concurrency, timestamp preservation, changed binding, scoped errors, CSRF, and account deletion. Tests used isolated `MIX_TEST_PARTITION=_interest`.
- Final focused frontend command passed: 29 tests across the page unit suite and Chromium/Firefox InterestForm browser suites. Pending, saved reload, retry, guest cancellation/sign-in without replay, and keyboard submission are covered.
- `mix assets.format.check`, `mix assets.lint`, and `mix typecheck` passed; Svelte reported zero errors and warnings. `mix assets.storybook` built successfully.
- `just check` ran using the repository's Nix-provided Just executable. Its full backend suite passed (887 tests), then Credo stopped the workflow on three pre-existing nesting findings in `lib/d20/koala_rescue_club/game.ex` at lines 250, 345, and 383. Running `MIX_ENV=test mix credo --strict` in the clean primary checkout reproduced exactly those findings.
- Remaining checks were run separately. `mix dialyzer` reported six findings with one filtered; the remaining findings concern unchanged `accounts/anonymous.ex`, `koala_rescue_club/rules.ex`, `koala_rescue_club/ruleset.ex`, and `d20_web/module.ex`. No interest implementation path was reported. These findings have not been suppressed or fixed by this feature.
- `mix ex_dna lib --max-clones 14` passed at 11/14; `mix reach.check --arch --smells` passed.
- Full `mix assets.test` recorded 325 passed, 3 failed, and 2 skipped tests. All three failures were the unchanged `PlayerCountLabel` Maximum Only screenshot (8 differing pixels) across desktop/tablet/mobile. A focused retry passed all 12 tests without updating references; the original checkout's desktop run also passed. The full-suite result remains recorded as failed, not relabeled green.
- `openspec validate --all --strict --no-interactive` passed all 83 items. This change remains active while task 4.5 is blocked by the broad validation gate.
- Migration `20260910103223` migrated up, rolled back one step, and migrated up again in disposable `d20_test_interest_migration`. No shared database reset or primary development migration occurred. Ordinary application rollback must retain `game_interests`; reversing this migration drops collected data.
- Chrome DevTools reused the existing game-detail tab against isolated `http://127.0.0.1:5001`: provider-only 356080, local Voyages, and playable Qwinto. It verified guest dialog/cancel/no replay after actual password login, offline error and explicit retry, throttled Saving state, saved reload and duplicate POST, keyboard submission, narrow viewport access without horizontal overflow, and existing Lobby precedence after disabling new launches. Grouped SQL counts were one for each of the two requested games; deleting the fixture account cascaded both to zero.
- The browser validation server used deterministic BGG response fixtures because its environment had no BGG API key. Live external BGG integration was not revalidated. The temporary server stopped, the fixture account was removed, and the original guest tab at `http://localhost:5000/games/356080` was restored.

## Typed interest identity correction - 2026-09-10

- Each interest now has an autogenerated interest-prefixed TypeID primary key; the account/game pair remains unique through its own index. Duplicate requests preserve the original ID and first timestamp.
- The regression reproduced the missing `id` before implementation. `mix format lib/d20/games/interest.ex priv/repo/migrations/20260910103223_create_game_interests.exs test/d20/games/interests_test.exs` passed.
- `MIX_TEST_PARTITION=_interest_typeid_20260910 mix test test/d20/games/interests_test.exs test/d20_web/controllers/game_interest_controller_test.exs` passed all 16 tests, including independent-connection concurrency, before and after corrected migration up/down/up in disposable `d20_test_interest_typeid_20260910`.
- The uncommitted creation migration was updated in place. Previously migrated `_interest` test databases retain their old schema and were not changed; shared databases were not reset.
- `git diff --check` and `openspec validate --all --strict --no-interactive` passed (83 items). Task 4.5 remains pending due to the previously recorded broad validation blockers; this correction does not finalize the overall delivery.

## Games context boundary correction - 2026-09-10

- `Games.request_interest/3` now owns fresh route resolution, visibility, expected BGG identity casting/matching, playability checks, and private idempotent insertion. `Games.interested?/2` and `Games.interest_counts/0` own reads; `Games.Interests` was removed.
- The interest controller only maps authenticated request values and context outcomes to the existing HTTP status, redirect, and scoped error messages. Its sole Ecto reference is matching a returned changeset error.
- Direct context tests cover rejection order, identity changes, provider failures, metadata fallback, numeric-route collisions, and implemented games accepting demand after disabling, retaining original interest identity across enable/disable transitions.
- `MIX_ENV=test mix format lib/d20/games.ex lib/d20_web/controllers/game_interest_controller.ex lib/d20_web/controllers/page_controller.ex test/d20/games/interests_test.exs test/d20_web/controllers/game_interest_controller_test.exs` passed.
- `MIX_TEST_PARTITION=_interest_typeid_20260910 mix test test/d20/games/interests_test.exs test/d20_web/controllers/game_interest_controller_test.exs test/d20_web/controllers/page_controller_test.exs test/d20/games_test.exs` passed all 126 tests, including independent-connection concurrency. Existing frontend selection tests also passed all 17 tests with `bun run test -- tests/pages/game/ui/game.test.ts` before this backend-only refactor.
- Code review found no behavior regressions; `git diff --check` and strict OpenSpec validation passed. No migration or frontend change was required by this correction. Existing task 4.5 delivery blockers remain; the change stays active and uncommitted.

- Inline-predicate refinement: removed `visible_for_interest/1` and `unavailable_for_interest/1`; `request_interest/3` checks both conditions directly in its `with`, preserving nil-entry handling, guard order, and tagged errors. `MIX_ENV=test mix format lib/d20/games.ex` and `MIX_TEST_PARTITION=_interest_typeid_20260910 mix test test/d20/games/interests_test.exs test/d20_web/controllers/game_interest_controller_test.exs` passed (22 tests); `git diff --check` passed.

- Inline-insertion refinement: moved the existing `Repo.insert` case into `request_interest/3` and removed `insert_interest/2`. Conflict options, result normalization, and identity checks are unchanged. `MIX_ENV=test mix format lib/d20/games.ex`, `MIX_TEST_PARTITION=_interest_typeid_20260910 mix test test/d20/games/interests_test.exs test/d20_web/controllers/game_interest_controller_test.exs` (22 tests), and `git diff --check` passed.

## Route-only submission correction - 2026-09-10

The user removed the stale-page expected-identity requirement. Earlier validation records above describe prior checkpoints; the authoritative current contract is `Games.request_interest/2` with no client BGG identity, as recorded in the updated design and delta specifications. Verification passed as recorded below.

- Removed `matching_interest_identity/2`, its mismatch error branch, the expected request argument, `GameInterest.bggId`, the serialized interest BGG prop, and form BGG payload. `Games.request_interest/2` uses the current route identity; submitted BGG IDs are ignored.
- `MIX_ENV=test mix format lib/d20/games.ex lib/d20_web/controllers/game_interest_controller.ex lib/d20_web/controllers/page_controller.ex test/d20/games/interests_test.exs test/d20_web/controllers/game_interest_controller_test.exs` passed.
- `MIX_TEST_PARTITION=_interest_typeid_20260910 mix test test/d20/games/interests_test.exs test/d20_web/controllers/game_interest_controller_test.exs test/d20_web/controllers/page_controller_test.exs test/d20/games_test.exs` passed all 126 tests. Updated tests verify no-payload submission, ignored client identities, current binding resolution, historical request preservation, and original numeric routes.
- From `assets/`, `bun run test -- tests/pages/game/ui/game.test.ts tests/pages/game/ui/interest_form.browser.test.ts` passed 29 tests across unit, Chromium, and Firefox. `bun run lint`, `bun run typecheck` (zero Svelte errors/warnings), and scoped `bun x oxfmt --check` on the four affected frontend files passed.
- Code review and `git diff --check` passed. Strict OpenSpec validation passed all 83 items. Task 4.5 remains pending; this correction does not resolve the previously recorded broad delivery blockers or finalize the change.

## Internal interest implementation extraction - 2026-09-10

- Restored `D20.Games.Interests` as the internal implementation module with `request/2`, `requested?/2`, and `counts/0`. `D20.Games` retains its documented public functions through three delegates; controllers and caller contracts are unchanged. This supersedes the earlier module-removal checkpoint.
- Moved the existing operation bodies without changing route resolution, inline visibility/playability checks, insertion, TypeID deduplication, or errors. No expected-ID check or payload was restored.
- `MIX_ENV=test mix format lib/d20/games.ex lib/d20/games/interests.ex` and `git diff --check` passed. `MIX_TEST_PARTITION=_interest_typeid_20260910 mix test test/d20/games/interests_test.exs test/d20_web/controllers/game_interest_controller_test.exs test/d20_web/controllers/page_controller_test.exs test/d20/games_test.exs` passed all 126 tests. Strict OpenSpec validation passed all 83 items.
- Task 4.5 remains pending under the previously recorded delivery blockers; this extraction remains uncommitted in the owning worktree.

## Silent playable submission - 2026-09-10

- The interest controller handles `:ok` and `{:error, :game_available}` with the same plain 303 redirect to the original game route. Already playable games receive no new interest and no availability, error, or success message. The context eligibility result remains unchanged.
- Persistence failures retain a scoped save-error message. The controller test provokes an actual foreign-key failure by deleting the authenticated account during provider resolution and verifies the error bag and absence of saved interest. The redirect pipeline is inline after removing its now-single-use helper.
- `MIX_ENV=test mix format lib/d20_web/controllers/game_interest_controller.ex test/d20_web/controllers/game_interest_controller_test.exs` passed. `MIX_TEST_PARTITION=_interest_typeid_20260910 mix test test/d20/games/interests_test.exs test/d20_web/controllers/game_interest_controller_test.exs` passed 22 tests. After the final helper cleanup, the controller suite passed all 11 tests again. `git diff --check` and strict OpenSpec validation (83 items) passed.
- Task 4.5 remains pending under the previously recorded broad validation blockers; this correction stays uncommitted in the owning worktree.

## Inline local detail rendering - 2026-09-10

- Moved the sole `render_game/4` body into the existing local-entry `render_game_detail/3` success branch and removed the helper. Playability, schema, props, visibility/session handling, provider-only clauses, and redirects are unchanged.
- `MIX_ENV=test mix format lib/d20_web/controllers/page_controller.ex`, `MIX_TEST_PARTITION=_interest_typeid_20260910 mix test test/d20_web/controllers/page_controller_test.exs` (60 tests), and `git diff --check` passed. Strict OpenSpec validation passed all 83 items. Task 4.5 remains pending under the recorded broad validation blockers.

## Shared configured visibility predicate - 2026-09-10

- Added `Games.visible?/1` and reused it in PageController, Games.Interests, and `Games.session_launch_available?/1`. A runtime-source search confirms visible-stage configuration is read only inside Games; its two SQL list scopes remain unchanged.
- Added focused coverage showing visibility responds to stage configuration independently of enabled state and engine presence. Provider-only bypass, existing Session access, fetch behavior, and SQL filtering retain their previous semantics.
- `MIX_ENV=test mix format lib/d20/games.ex lib/d20/games/interests.ex lib/d20_web/controllers/page_controller.ex test/d20/games_test.exs` passed. `MIX_TEST_PARTITION=_interest_typeid_20260910 mix test test/d20/games_test.exs test/d20/games/interests_test.exs test/d20_web/controllers/page_controller_test.exs test/d20_web/controllers/game_interest_controller_test.exs test/d20_web/controllers/module_controller_test.exs` passed all 138 tests. `git diff --check` and strict OpenSpec validation (83 items) passed.
- Task 4.5 remains pending under the recorded broad validation blockers; no commit or tracker completion was performed.

## Game-detail Storybook coverage - 2026-09-10

- Added four production GamePage stories under `Pages/Public/∕games∕:slug` with stable `pages-game` IDs: Without Session (`Play`), With Session (`Start` and joined players), Unavailable Game (`I want this game!`), and authenticated Requested (`Requested`, disabled, with `Request saved.`). The wide layout uses each original route, including the waiting Session query parameter.
- Extended the existing Session mock with the projected Session type and inert detach lifecycle; isolated the socket module in Storybook. Added focused SJSF dependency optimizer exclusions and nested jsonpointer inclusion so the real SessionForm renders in development and browser tests. Production code and dependencies are unchanged by this addition.
- From `assets/`, `bun run test:unit -- tests/stories/page_layout.test.ts` passed 7 tests. `bun run test:visual -- stories/pages/public/game.stories.ts --update` passed 12 tests; all desktop/tablet/mobile references were reviewed. A normal comparison with `bun run test:visual -- stories/pages/public/game.stories.ts stories/widgets/workspace.stories.ts` passed all 24 tests. Vitest printed a shutdown timeout warning after successful test completion and exited zero.
- `bun run typecheck` passed with zero errors/warnings; targeted ESLint, oxfmt, and `git diff --check` passed. `bun run storybook:build` passed with the final Storybook configuration.
- Chrome DevTools reused the existing `http://localhost:6006` Storybook tab, verified all four activation states in the dark theme, confirmed the mocked socket module was loaded, and observed no console errors or warnings on the final checked stories. The original home-story route and theme were restored. The existing dev server was restarted in this worktree to apply Storybook configuration.
- Focused code review passed after correcting the waiting Session query parameter. This completes story task 3.5 only; pending visibility work and the existing broad delivery gate remain tracked in this active, uncommitted change.

## Direct Interests context correction - 2026-09-10

The user removed the redundant Games interest delegation layer. The authoritative API is now `D20.Games.Interests.request/2`, `requested?/2`, and `counts/0`, called directly by controllers and other consumers. Earlier delegation checkpoints are superseded; task 2.5 is verified for the direct-call boundary. Persistence, visibility, launch policy, and HTTP behavior are unchanged.

- Removed all three interest delegates and unused aliases from Games; both controllers and existing interest tests now call Interests directly. Public operation documentation moved to Interests. Existing request, membership, and count bodies are unchanged.
- `MIX_ENV=test mix format lib/d20/games.ex lib/d20/games/interests.ex lib/d20_web/controllers/game_interest_controller.ex lib/d20_web/controllers/page_controller.ex test/d20/games/interests_test.exs test/d20_web/controllers/game_interest_controller_test.exs` passed.
- `MIX_ENV=test MIX_TEST_PARTITION=_interest_typeid_20260910 mix test test/d20/games/interests_test.exs test/d20_web/controllers/game_interest_controller_test.exs test/d20_web/controllers/page_controller_test.exs test/d20/games_test.exs` passed all 127 tests. No references to the removed Games interest APIs remain under lib or test.
- Focused diff review, `git diff --check`, and `openspec validate --all --strict --no-interactive` passed (83 items). The existing task 4.5 finalization blockers remain; the change stays active and uncommitted.


## Saved button confirmation correction - 2026-09-10

- Removed the separate `Request saved.` paragraph. Confirmed membership uses the disabled `Requested` button; pending behavior, retryable errors, and suppression of stale errors after confirmation remain unchanged. This supersedes the earlier Storybook checkpoint that included a separate success status.
- Updated existing component and Requested story assertions and reviewed only Requested screenshot references at desktop, tablet, and mobile. `bun run test:browser -- tests/pages/game/ui/interest_form.browser.test.ts` passed 18 tests across Chromium and Firefox. A concurrent first run timed out in unchanged count coverage; the isolated rerun passed.
- `bun run test:visual -- stories/pages/public/game.stories.ts -t Requested --update` passed all three cases; the same command without `--update` passed all three comparisons. The runner emitted its existing shutdown warning after success and exited zero.
- Scoped oxfmt and ESLint, `bun run typecheck` (zero Svelte errors/warnings), Svelte autofixer, `git diff --check`, and `openspec validate --all --strict --no-interactive` (83 items) passed. Live Storybook had moved to the unavailable-game route during inspection; its prepared state was preserved, and the saved presentation was verified through the browser tests and reviewed screenshots.
- Task 3.7 is verified. This correction remains part of the active, uncommitted delivery; task 4.5 and the existing finalization blockers remain pending.

## Request count and star - 2026-09-10

- Added filtered `Interests.count/1` and required `interest.count` from the resolved BGG identity. The button shows count, the existing metadata five-point SVG geometry, then its current label. Its accessible description includes the count while the action name stays unchanged. Refreshed server props determine the count; pending/error states do not invent contributions.
- `MIX_ENV=test mix format` on the four touched backend files and `MIX_TEST_PARTITION=_interest_typeid_20260910 mix test test/d20/games/interests_test.exs test/d20_web/controllers/page_controller_test.exs test/d20_web/controllers/game_interest_controller_test.exs` passed, with 83 tests covering zero, duplicates, distinct accounts/games, shared local/provider identity, deleted accounts, guest reads, and redirected saved props.
- From `assets/`, `bun run test -- tests/pages/game/ui/game.test.ts tests/pages/game/ui/interest_form.browser.test.ts` passed 35 tests across unit, Chromium, and Firefox. `bun run lint`, `bun run typecheck`, and scoped `bun x oxfmt --check` passed. The initial lint finding from a disallowed geometry assertion was removed; responsive geometry is verified through browser inspection and visual tests.
- `bun run test:visual -- stories/pages/public/game.stories.ts --update` passed 12 tests; the six changed Unavailable Game/Requested references were reviewed across desktop/tablet/mobile. An interrupted comparison exited 143 after a tablet failure; a fresh `bun run test:visual -- stories/pages/public/game.stories.ts --reporter=verbose` passed all 12 comparisons. Both successful visual runs reported the existing shutdown timeout warning and exited zero.
- Chrome DevTools verified the current count description and the 320 px layout with no horizontal overflow. The original Requested story and light theme were restored on the surviving worktree server at localhost:6006 after the duplicate localhost:6008 server stopped. No server was started or stopped by this task. Read-only code review found no defects. `git diff --check` and strict OpenSpec validation passed (83 items).
- Task 3.6 is verified. Task 4.5 retains its previously recorded broad delivery blockers; this addition does not archive the overall feature or close issue #275.

## General interest error field - 2026-09-10

- General errors now use `errors.interest.message` in both page consumers and `errors.message` in the bag-scoped callback. The controller assigns `%{message: text}`. The error bag, displayed copy, retry behavior, count/star, and saved presentation remain unchanged. The existing browser failure test uses a distinct server message to verify extraction rather than matching the fallback.
- `MIX_ENV=test mix format lib/d20_web/controllers/game_interest_controller.ex test/d20_web/controllers/game_interest_controller_test.exs` and `MIX_TEST_PARTITION=_interest_typeid_20260910 mix test test/d20_web/controllers/game_interest_controller_test.exs` passed (12 tests). From `assets/`, `bun run test -- tests/pages/game/ui/game.test.ts tests/pages/game/ui/interest_form.browser.test.ts` passed 35 tests; scoped formatting, `bun run lint`, and `bun run typecheck` passed with zero Svelte errors/warnings. Runtime/test searches found no remaining `interest.interest` key, and diff checks and strict OpenSpec validation passed (83 items).
- Task 2.8 is verified. The existing overall delivery blockers and pending task 4.5 remain unchanged.


## Direct Inertia Form migration - 2026-09-10

- `InterestForm` now imports the Svelte Inertia `Form` component directly. The component owns submission, processing, and scoped `errors.message`; the existing page error fallback, original action, guest auth guard, count/star, and disabled Requested confirmation remain. Removed manual router, processing/error state, and custom HTTP/network callbacks. Standard Inertia transport/HTTP handling replaces the custom inline transport message as authorized.
- Kept `@inertiajs/svelte` 3.6.0. Published stable 3.7.0 still lacks Form forwarding for the two custom failure callbacks, so upgrading is unnecessary for this supported Form migration.
- From `assets/`, `bun run test -- tests/pages/game/ui/interest_form.browser.test.ts tests/pages/game/ui/game.test.ts tests/pages/registration_completion/ui/registration_completion.test.ts tests/pages/auth_confirmation/ui/auth_confirmation.test.ts tests/pages/account_settings/ui/account_settings.test.ts tests/app/ui/header.browser.test.ts` passed all 103 tests. Shared Form mocks gained only the required before-submit cancellation behavior.
- `bun run test:visual -- stories/pages/public/game.stories.ts` passed all 12 desktop/tablet/mobile comparisons without changing references. The existing shutdown warning occurred after success with exit zero. `bun run typecheck`, scoped oxfmt/ESLint, Svelte checks, and `git diff --check` passed.
- Chrome DevTools reused the existing Requested Storybook page at `http://localhost:6006/?path=/story/pages-game--requested&globals=backgrounds.value:light;theme:light`: disabled Requested, count 13, no success paragraph, and no console warnings/errors. A detached mount of the actual installed Form additionally verified onBefore cancellation before processing, processing start/finish, interest error bag, and default transport callbacks with a temporary router transport stub. The stub was restored and component unmounted; no actual network submission or page navigation occurred.
- Strict OpenSpec validation passed all 83 items. Task 3.9 is verified; the overall change remains active and uncommitted under the existing task 4.5 finalization blockers.

## Integration preparation - 2026-09-10

- The user authorized three logically ordered commits: reusable mocks, feature with its tests and domain specifications, then game-page stories with screenshot baselines. The target is local master; remote publication was not requested.
- Current full backend validation passed all 897 tests, compilation, and formatting. `just check` then failed on the same three Credo nesting findings at Koala Rescue Club game lines 250, 345, and 383. That source file and `.credo.exs` have identical Git blob hashes in the candidate and pinned master `1d98be8c2ed550d42f26cef745034cfa78d7f4ff`.
- Frontend formatting, lint, type checks, and Storybook build passed. Clone budget remains 11/14, architecture checks passed, and the native OpenSpec lifecycle check passed. Broad frontend execution and clean-master compilation exhausted available memory/swap; those task-owned checks were stopped and frontend validation was restarted with two workers and file parallelism disabled. The interrupted run is not counted as passing; no screenshots were updated.
- Feature delta requirements were synchronized into authoritative game-detail, game-session-launch-policy, game-detail-activation-layout, and game-interest specifications. Storybook coverage will be committed with its own requirement. The change remains active while final validation and integration tasks 4.5/4.6 are pending.

## Final integration verification - 2026-09-10

- This record supersedes the earlier uncommitted and blocked checkpoints above. After the existing broad-check failures were reported, the user instructed continuation of the local master transfer. Those failures remain visible and were not suppressed or fixed as part of this feature.
- Three unpublished commits were rebased onto pinned master `1d98be8c2ed550d42f26cef745034cfa78d7f4ff` without conflicts: `a2559aae` reusable Form/session/socket mocks, `bd5118d7` interest behavior with tests and domain specifications, and `1cf5aaf1` game-page stories with 12 reviewed screenshot references. Master was fast-forwarded to `1cf5aaf1`; no remote publication or deployment was requested.
- Rebased-candidate verification passed 128 focused backend tests, all 897 backend tests through `just check`, and 122 focused frontend/shared-mock/story checks across 12 test files. Frontend formatting, lint and type checks passed. Strict OpenSpec validation passed all 85 items; its native lifecycle check passed for 10 active changes.
- Integrated-master verification repeated all 897 backend tests and the same 122 frontend checks, including all 12 game-page screenshot comparisons without reference updates. Formatting, lint, type checks and Storybook build passed in the primary checkout.
- `just check` on both the candidate and integrated master stopped after successful compilation, formatting and tests on the same three Credo nesting findings in unchanged Koala Rescue Club code at lines 250, 345 and 383. Clean-master validation before transfer reproduced them. Source and Credo configuration were identical to the pinned base.
- Previously recorded Dialyzer findings concern unchanged modules outside this feature. The later PLT rebuild was interrupted and is not counted as a successful check. Two broad frontend attempts were interrupted with exit 143; the limited run observed the existing Maximum Only screenshot failures before interruption. The earlier completed broad suite also recorded those three screenshot failures. None of these broad checks is represented as green. Focused candidate and integrated-master checks completed successfully afterward.
- All 12 changed requirements across four capabilities exactly match their synchronized canonical specifications. Native archival completed as `2026-09-10-add-game-interest`; strict validation passed all 84 remaining items, the lifecycle check passed for 9 active changes, and `openspec list --json` no longer includes this change. Integrated-master clone budget and architecture checks also passed at 11/14 and OK. Issue #275 owns confirmation of the subsequent archival-commit transfer and source-worktree retirement; it stays open until those operations are verified.
- Migration up/down/up and rollback evidence remain as recorded above. Ordinary application rollback retains collected interests; reversing the additive migration deletes them. No primary development database migration or live BGG verification is claimed.
