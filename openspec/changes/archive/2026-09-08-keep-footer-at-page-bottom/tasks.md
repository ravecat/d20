## 1. Footer placement

- [x] 1.1 Characterize the prepared page's footer gap and confirm master/worktree ownership.
- [x] 1.2 Use a column flex layout with main consuming spare height while preserving shell offsets and normal flow.

## 2. Validation

- [x] 2.1 Inspect short and overflowing content on narrow and wide viewports in the selected development tab, including disclosure expansion; restore prepared state.
- [x] 2.2 Run existing layout/footer browser tests, touched-file formatting and lint, and frontend type checks.
- [x] 2.3 Record verification, reconcile and archive the OpenSpec change, and run strict validation before the requested style commit.
