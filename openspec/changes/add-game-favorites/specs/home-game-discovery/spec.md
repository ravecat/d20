## ADDED Requirements

Task 21 supersedes earlier task-specific Storybook/screenshot waivers for current visual reconciliation. Existing Home, GameCard and GameCollection states SHALL be executed through the unchanged six-cell matrix with reviewed current references; affected standalone Home/favorite comparisons SHALL retain Chromium and Firefox coverage and their paths. Stale semantic assertions SHALL follow the current contract, including `Games` for the page h1, `Hot (by BGG)` for the provider h2 and no favorites status region. Historical unchanged-reference and waived-review statements below apply only to earlier amendments. Current product appearance, story IDs, meaningful coverage, configuration and performance changes SHALL remain unchanged; unrelated delivery gates require their own evidence.

### Requirement: Pages compose collection headings with snippets

GameCollection SHALL accept a required zero-argument `header` Snippet instead of `title`, `headingAttribution` or `headingId` props. Its existing h2 SHALL render that snippet, and its section SHALL refer to a unique heading ID generated by the component. Home SHALL own the Playable text and Hot text/native BGG link markup. Link presentation, heading semantics, section names, empty omission and keyboard access SHALL remain intact. Multiple compact rows SHALL use generic `Row 1` and `Row 2` labels within their named section without deriving or duplicating a title string from the snippet. Other lane behavior SHALL remain unchanged.

#### Scenario: Home supplies provider markup

- **WHEN** Home renders its provider collection
- **THEN** it supplies Hot and the native by BGG link through the header snippet
- **AND** the section and h2 retain the accessible name Hot (by BGG) without attribution-specific component props

#### Scenario: A collection receives arbitrary heading content

- **WHEN** a parent supplies a header snippet containing text and inline markup
- **THEN** the collection renders it inside its existing h2 without interpreting its content
- **AND** the generated heading ID names that collection section

#### Scenario: Compact rows retain accessible context

- **WHEN** a compact collection contains seven or eight games
- **THEN** its named section contains Row 1 and Row 2 lists with unchanged partition and canonical order
- **AND** a single compact row retains its existing unnamed list

### Requirement: Home identifies BGG as the Hot collection provider

Home's visible provider collection heading SHALL read `Hot (by BGG)` instead of `Games`. The `by BGG` text in parentheses SHALL be a visibly identifiable, keyboard-accessible native link to BGG's dedicated Hotness page at `https://boardgamegeek.com/hotness` when a dedicated URL is known, otherwise to `https://boardgamegeek.com`. This attribution SHALL identify the external selection rather than D20 editorial recommendations. The visually hidden Home h1 SHALL retain its current plain `Games` text without a hidden focusable link; `Hot (by BGG)` SHALL name the visible provider collection h2. Playable SHALL retain its separate heading without BGG selection attribution. Existing selection, order, lane partition, cards, favorites and empty-provider behavior SHALL remain unchanged; the client SHALL NOT probe provider availability to choose the link.

#### Scenario: Provider discovery contains games

- **WHEN** Home renders a nonempty BGG discovery collection
- **THEN** its visible heading and accessible section name are `Hot (by BGG)`
- **AND** exactly one `by BGG` link in that heading targets `https://boardgamegeek.com/hotness`
- **AND** Playable has no provider-attribution link

#### Scenario: A dedicated Hot URL is unavailable

- **WHEN** no dedicated Hot page URL can be established for the Home attribution
- **THEN** the heading still reads `Hot (by BGG)` and its `by BGG` link targets `https://boardgamegeek.com`
- **AND** no network probe or fabricated dedicated route is used

#### Scenario: The provider collection is inspected in isolation

- **WHEN** any browse example under Widgets/Game Collection is rendered
- **THEN** it uses the same `Hot (by BGG)` heading and `by BGG` Hotness link as Home
- **AND** canonical game link assertions exclude the provider link and Playable examples have no inherited attribution

#### Scenario: Empty collection coverage is allocated

- **WHEN** the GameCollection story catalog is reviewed
- **THEN** it contains Hot counts 1/2/3/4 and Playable counts 1/2/4/7 with no standalone Empty example or its orphaned references
- **AND** Home retains empty-provider composition coverage and production GameCollection still omits empty sections

#### Scenario: Provider discovery is empty

- **WHEN** Home receives an empty provider collection, including the existing provider-error fallback
- **THEN** no visible Hot section or associated attribution link renders
- **AND** Playable and the noninteractive page h1 retain their normal behavior

### Requirement: Home owns its collection and card components

Home SHALL compose its internal `GameCollection` and `GameCard` components at `assets/js/pages/home/ui/game-collection.svelte` and `game-card.svelte`. Card rendering and card CSS SHALL belong to GameCard; section/lane derivation, loop copies and carousel CSS SHALL belong to GameCollection. Home SHALL retain page composition and server-provided favorites membership; FavoriteButton SHALL own the current-page authentication read. Home, GameCollection and GameCard SHALL NOT accept or forward authenticated props for favorites. Home and its collection SHALL use relative default component imports; neither component SHALL be exported from the Home public API. The old `assets/js/widgets/game-collection/` production slice and barrel SHALL be removed without compatibility aliases. GameCard SHALL consume named `FavoriteButton` through `~/features/favorite-game`. Components SHALL NOT receive feedback props, bindings or result IDs; each favorite Form SHALL retain local processing. Relocation SHALL preserve current props, markup, CSS, input order, lane partition, empty/singleton states, image/metadata/title presentation, card links, favorite forms, accessible names, focus reveal, pause/looping and reduced motion without new dependencies or scripted carousel behavior.

#### Scenario: Home components are composed

- **WHEN** Home renders its collections
- **THEN** Home imports GameCollection from `./game-collection.svelte` and the collection imports GameCard from `./game-card.svelte`
- **AND** GameCard imports FavoriteButton through the feature public API without exporting these page internals for other production pages

#### Scenario: A collection is inspected independently

- **WHEN** an isolated production-component story or semantic test renders a supported Playable or Games collection
- **THEN** it imports the Home-owned component and exposes the same section naming, canonical links, order and lanes as the composed page
- **AND** no production markup is copied into a story-only implementation

#### Scenario: Both collections expose one favorite identity

- **WHEN** the same game appears in Playable and Games and a favorite request starts or completes
- **THEN** both collections use Home's authoritative membership, and only the submitting Form becomes locally busy
- **AND** confirmed prop updates synchronize every occurrence without shared pending coordination or a result announcement

#### Scenario: Existing verification identities survive relocation

- **WHEN** component imports are updated to their Home-owned paths
- **THEN** the two existing `assets/stories/widgets/game-card.stories.ts` and `game-collection.stories.ts` paths, titles, IDs, decorator and screenshot files remain unchanged
- **AND** the screenshot-free collection test moves to `assets/tests/pages/home/ui/game-collection.browser.test.ts`
- **AND** `assets/tests/shared/components/favorite_button.browser.test.ts` stays at its current path with unchanged content and screenshot references because it exercises Home
- **AND** legacy verification paths do not require a production widget/shared-component alias

### Requirement: Carousel covers have icon-only favorite controls

Each delivered canonical card in Playable and both Games lanes SHALL display a gold-outlined favorite star at the top right of its cover, with a transparent interior when absent and gold fill when saved. Both states SHALL retain the same gold outline, color, and opacity; the parent favorite button's CSS SHALL own the shared SVG's paint as specified by game-favorites. Its rendered icon box SHALL be square with sides equal to the first title line box height and SHALL share that line box's horizontal centerline, including when the title wraps. The glyph SHALL retain a single flat gold outline without shadows, a dark outer contour or glow. Hover on hover-capable devices and keyboard focus-visible SHALL temporarily fill an unsaved icon gold; leaving both states SHALL restore its confirmed appearance without changing membership or pressed state. The control SHALL have no visible button background or border. It SHALL use a native button with a hit box equal to the rendered SVG box, no extra target padding or minimum size, a state-dependent accessible name, `aria-pressed`, and visible keyboard focus. It SHALL remain usable on narrow cards, fallback covers, and both themes. The button SHALL submit an Inertia form outside the normal detail anchor within the noninteractive card wrapper, never nested inside the link. The same icon-only FavoriteButton and star paint SHALL also apply to game detail without a labeled variant or text-button box, preserving its hero placement and request behavior. Card favorites SHALL NOT render a hover/focus tooltip or native title tooltip. No JS geometry, observers, portals, library, or browser-policy change SHALL be introduced.

#### Scenario: User saves from a card

- **WHEN** an authenticated user activates the gold-outlined star with a transparent interior
- **THEN** a favorite add is requested without activating the card link
- **AND** after confirmed success all occurrences of that game display gold fill inside the same gold outline

#### Scenario: User removes from a card

- **WHEN** an authenticated user activates the saved star
- **THEN** removal is requested and confirmed success clears the interior of every same-game star while retaining its gold outline

#### Scenario: Card heading size changes with layout

- **WHEN** a compact or hero card is rendered at a supported viewport size
- **THEN** the favorite icon width and height equal the title's first line box height, with their horizontal centerlines aligned for single-line and wrapped titles
- **AND** the button wraps the SVG box without extra target padding or a fixed minimum size, preserving proportional top/right alignment

#### Scenario: Card membership has not yet been confirmed

- **WHEN** a favorite request is pending or fails
- **THEN** every occurrence retains the pressed state of the latest server-confirmed membership and its confirmed empty or filled interior outside the icon hover/focus preview
- **AND** local Form processing prevents repeat activation of its button, retains focus and permits same-control retry after failure without an associated notice

#### Scenario: Card state is inspected without relying on color

- **WHEN** unsaved and saved cards are compared without hover or focus
- **THEN** their transparent and filled star interiors distinguish membership without relying on brightness or opacity

#### Scenario: Card state remains accessible during interaction

- **WHEN** a canonical favorite receives hover or keyboard focus, or is inspected through accessibility APIs
- **THEN** its accessible action distinguishes Add from Remove and its pressed state reflects confirmed membership
- **AND** keyboard focus remains visible without introducing a persistent button box or new visible state label

#### Scenario: Icon interaction has no tooltip

- **WHEN** an Add or Remove card favorite is hovered or keyboard-focused
- **THEN** no tooltip or popover appears
- **AND** its accessible name, visible focus and normal keyboard activation remain intact

#### Scenario: Visible loop copy is activated

- **WHEN** a user clicks a favorite star on a visible loop copy in any carousel
- **THEN** it operates on the same BGG identity and confirmed membership as the canonical card through its own Form processing state
- **AND** it does not navigate or create a duplicate keyboard stop or announcement

#### Scenario: Empty metadata is displayed

- **WHEN** a delivered game has no title or cover image
- **THEN** the favorite control retains a generic accessible action name and remains visible and usable over the fallback

### Requirement: Card content shares a cover-filling grid

Playable, Games hero, and Games compact cards SHALL retain a common local CSS Grid structure with title/favorite in the upper region and bounded metadata/status below. Compact/Playable cards SHALL retain a 2:1 rendered width/height ratio and hero cards a 2.9:1 ratio through resizing, without a mobile ratio switch or content/max-content-driven lane-height growth. Each card/variant SHALL own proportional padding; visible in-card typography, glyphs, chips/status, gaps, and other content spacing SHALL scale from actual card size using native container units/queries. Metadata chips and development-status badges SHALL use `0.5em` text consistently across variants, with their existing em-based dimensions shrinking proportionally; approved heading/glyph size, card ratio, and card padding SHALL remain unchanged. Sizing calculations SHALL be inline in CSS rather than `--slide-basis` or geometry alias variables, except local shared title-font and line-height values that coordinate the first title line and favorite square. The favorite button SHALL follow its rendered SVG box without enlarged invisible hit areas, and SHALL NOT change the card layout or glyph alignment. Normal links, separate forms, images/fallbacks, native focus snapping, and existing CSS motion SHALL remain intact without JS sizing. Metadata SHALL use native flex wrapping within exactly one visible chip row and `overflow: clip`. Complete additional chips SHALL wrap below the visible row based on available footer width, without count caps or width thresholds. Only an overlong primary chip MAY ellipsize; secondary chips SHALL NOT appear partly visible or ellipsized in the first row. Visually suppressed noninteractive category text on canonical cards SHALL remain in the DOM and accessible to assistive technology, without `aria-hidden` for that suppression.

#### Scenario: Cards resize across supported widths

- **WHEN** compact/Playable and hero cards render at 320, 375, 420, 600, 768 CSS pixel viewports or the supported wide viewport
- **THEN** their visible rendered bounds excluding lane gaps retain ratios 2:1 and 2.9:1 respectively within browser rounding
- **AND** fonts, proportional padding, gaps, and visible upper/lower content insets track their actual card size
- **AND** content and the icon-sized interaction target do not increase card height
- **AND** metadata and status use the same reduced `0.5em` scale with proportionally smaller chips, preserving the approved heading and card geometry

#### Scenario: Metadata exceeds its available first-row width

- **WHEN** additional complete category chips cannot fit the available metadata row width
- **THEN** native flex wrapping places those chips below the exactly one-row visible area
- **AND** no secondary chip is partly shown or ellipsized in the visible row
- **AND** every category remains in the DOM and accessible to assistive technology without being aria-hidden because of visual clipping
- **AND** no count cap, width threshold, overflow detector, or JS decides which chips wrap

#### Scenario: Long title or primary chip text is displayed

- **WHEN** primary text exceeds its bounded visual space
- **THEN** intentional ellipsis keeps it inside the fixed-ratio card
- **AND** the full title remains in link/favorite accessible names and the primary chip retains its full accessible text

#### Scenario: The interaction target follows the SVG box

- **WHEN** the visible star scales with the heading on a small card
- **THEN** its button bounds equal the rendered SVG bounds without extra padding, while preserving the glyph's proportional top/right inset
- **AND** title top/left, star top/right, and lower metadata/status insets follow the card's proportional layout

#### Scenario: Grid content receives pointer or keyboard interaction

- **WHEN** a user activates the full-card link or separate favorite, including a pointer-visible loop copy
- **THEN** the existing navigation or favorite action occurs without nested interactions or blocked targets
- **AND** canonical focus/reveal, duplicate accessibility exclusion, native focus-only snapping, CSS pause/looping, and reduced motion remain intact

### Requirement: Favorite changes preserve discovery without notifications

Home favorite descriptors SHALL use `{bggId, action, slug}` with one query-free `/favorites/<bggId>` action shared by PUT and DELETE and each delivered original source slug. PUT forms SHALL send the original `slug` and `response_to`; DELETE forms SHALL send only `response_to`. Neither SHALL submit a runtime Session field or hidden `bgg_id`; authoritative membership SHALL determine the method only. Favorite forms SHALL redirect back through an Inertia partial response explicitly requesting favorites, auth, and errors while preserving page state and scroll. Home SHALL assign favorites and both catalog collections through lazy props in its ordinary Inertia response pipeline. A favorites/auth/errors partial response SHALL omit both collection props; application acceptance SHALL permit the adapter to evaluate excluded loaders and repeat catalog/BGG reads. Full responses SHALL retain updated master's existing collection loading, playable projection, and provider-error fallback. The controller SHALL NOT implement a favorites-specific header allowlist or partial response branch. Updated server membership SHALL change the stars without changing the visible route, scroll position, collection membership/order, or mounted carousel motion. Each Form SHALL preserve confirmed membership while processing and prevent repeat activation of its own button without disabling other forms through shared coordination. Expected favorite failures SHALL retain server-authoritative membership and allow retry through the original control after processing ends without a custom notice. Home SHALL NOT render favorite success/error notifications, result regions or feedback state. Authentication failures SHALL retain the existing dialog and standard adapter behavior SHALL govern HTTP/network failures.

#### Scenario: Favorite mutation succeeds

- **WHEN** a favorite response is confirmed
- **THEN** the original collection identities, order, lane partition, route, scroll, and carousel continuity remain intact
- **AND** repeated cards update together from the refreshed favorites prop without replacing the client's existing collections

#### Scenario: Favorite request fails

- **WHEN** a form receives an expected server error or does not complete successfully
- **THEN** no successful membership change is displayed
- **AND** the user can retry after processing ends without a custom favorite notice; standard adapter HTTP/network behavior remains intact

#### Scenario: Membership-only home response is requested

- **WHEN** the standard adapter selects favorites, auth, and errors for a home partial response
- **THEN** the response contains the current account's membership and requested shared auth/errors without another account's membership
- **AND** no collection or metadata prop is returned, regardless of whether the adapter evaluated its loader

#### Scenario: Error response returns to home

- **WHEN** a favorite form redirects to home with authentication or favorite errors and explicitly requests errors
- **THEN** the corresponding error fields survive the partial response and reach the form
- **AND** the client does not report a failed mutation as successful

#### Scenario: Full home response is requested

- **WHEN** the standard adapter renders a full home response
- **THEN** the lazy collection loaders return the existing Playable and Games projections in their established order
- **AND** provider discovery failures retain the existing empty-Games fallback

### Requirement: Favorite discovery stories use the shared theme matrix

Public and authenticated Home stories and isolated Home-owned production-component stories affected by favorite controls SHALL preserve their meaningful scenarios at the owning UI boundary and use the existing light and dark reference matrix at desktop `1280x720`, tablet `1024x640`, and mobile `320x900` through the existing native visual projects. Each scenario SHALL remain declared once without theme-only duplicates or theme overrides. Favorites identity, membership, local Form processing, failure/retry and guest-authentication fixtures SHALL remain deterministic and isolated without feedback props or favorite alert/status assertions. Isolated card/collection stories SHALL initialize and reset actual mocked page authentication through existing context APIs, without obsolete authenticated component args. Existing story IDs SHALL remain unchanged. For the current first-line/card-state, two-row Playable, notification-removal, header-snippet, page/feature ownership, single-icon-control and page-authentication ownership amendments, the user explicitly waives Storybook execution, screenshot generation/reference updates and rendered review; source and scoped static checks SHALL be recorded without claiming a visual pass. Whole-feature visual acceptance remains a separate open gate.

#### Scenario: Home references are reconciled after the master update

- **WHEN** restored favorites stories are integrated with master's visual matrix
- **THEN** each affected Home scenario has six reviewed references under `<story-path>/<theme>/<viewport>/chromium/<scenario>-1.png`
- **AND** obsolete unthemed references within those story paths are removed after replacement review
- **AND** normal comparison and existing semantic favorite assertions pass without moving standalone browser-test references or changing screenshot infrastructure

#### Scenario: Card star appearance and grid are refined after integration

- **WHEN** the heading-sized outlined/filled gold card treatment is implemented within the existing shared grid
- **THEN** affected Home and relocated card/collection scenarios receive fresh review across all six native theme/viewport cells; the former 23 public and five authenticated Home scenarios define historical coverage rather than a required page-level count
- **AND** existing semantic stories continue to verify action/pressed/pending/error behavior, while manual DevTools measurements verify inherited CSS paint, unchanged icon/heading geometry, upper-row alignment, and icon-sized hit targets and rendered review verifies transparent/gold interiors, a persistent gold outline, and a bare control surface
- **AND** manual checks at intermediate widths including 375, 420, 600, and 768 CSS pixels verify exact fixed ratios, relative typography/padding/gaps, actual upper-left/top-right/lower visual insets, one complete visible metadata row with no partial secondary chips, and bounded primary text on actual/fallback covers
- **AND** metadata semantic coverage verifies complete accessible text without using `not.toBeVisible` for overflow clipping; screenshots/manual review verify whole-chip visual suppression
- **AND** current-source manual CSS movement, focus pause, and blur resume are recorded with the observed lane coverage rather than inferred from static screenshots
- **AND** ImageMetadata scopes content through a named article and the Home browser duplicate-link check queries accessible names, without CSS-class or DOM-depth assertions
- **AND** hover and focus do not render a tooltip
- **AND** existing Home focus/loop/reduced-motion browser tests run in full; their affected browser references are reviewed and narrowly updated alongside affected page and relocated widget Storybook references before both normal comparison commands pass and exit successfully on the final implementation source
- **AND** whole-feature detail reference review covers the current icon-only star treatment; historical labeled-detail references do not establish the later single-icon-control amendment, whose fresh screenshot review remains explicitly waived

## MODIFIED Requirements

### Requirement: Playable games use a compact auto-advancing strip

The `Playable` section SHALL render one compact row for one through six delivered games and exactly two compact rows for more than six. Both rows SHALL use the existing compact landscape cards with 2:1 proportions and responsive sizing matching the compact Games row; neither row SHALL contain hero cards. For `n > 6`, the first row SHALL contain the first `ceil(n / 2)` entries and the second the remaining suffix in server order. Concatenating the canonical row sequences SHALL reproduce the input exactly without filtering, interleaving, reordering, padding or another request. The server's existing Playable limit and launch-eligibility policy SHALL remain unchanged.

Each multi-item row SHALL advance one of its own cards per shared 6.1-second cycle consisting of a five-second dwell and 1.1-second `ease-in-out` movement, using only CSS. Two moving rows SHALL share direction, cycle phase and movement progress while owning independent count-derived loop durations. They SHALL remain in one section-wide pause scope and loop without blank gaps or visible jumps. A split collection SHALL expose distinct accessible list names `Row 1` and `Row 2` within its named section; the existing single-row naming SHALL remain unchanged. Every canonical link and favorite SHALL remain accessible in first-row then second-row order; pointer-usable loop copies SHALL add no accessible duplicates.

#### Scenario: Multiple playable games render in one row

- **WHEN** two through six playable games are delivered
- **THEN** they render in one compact row in server order with no second row or hero card
- **AND** the CSS animation advances one slide per dwell and loops continuously

#### Scenario: Seven playable games require two rows

- **WHEN** seven playable games are delivered
- **THEN** the first compact row contains entries 1 through 4 and the second entries 5 through 7
- **AND** each game has exactly one canonical link and favorite in Playable and no hero row renders

#### Scenario: Eight playable games are delivered

- **WHEN** eight playable games are delivered
- **THEN** the first compact row contains entries 1 through 4 and the second entries 5 through 8
- **AND** canonical first-row then second-row order reproduces server order without a selection or eligibility change

#### Scenario: Playable dwell completes

- **WHEN** Playable is running and a dwell completes
- **THEN** every moving row settles its own next slide through the shared CSS cadence and eased movement
- **AND** no script timer participates

#### Scenario: Playable renders with application styles

- **WHEN** Storybook or the application loads daisyUI alongside the home styles
- **THEN** every Playable viewport retains the full available inline width and visibly renders compact cards
- **AND** one-, two- and three-game rows do not collapse or expose a blank gap during looping

#### Scenario: Playable rows wrap independently

- **WHEN** one of two Playable rows reaches its final game before its sibling
- **THEN** that row continues seamlessly from its own first game while the other advances normally
- **AND** both retain matched per-card phase without requiring equal full-loop durations or adding duplicate accessible links

#### Scenario: One playable game is delivered

- **WHEN** exactly one playable game is delivered
- **THEN** it renders as one static compact landscape card
- **AND** no animation, loop sentinel, Previous, Next, Play or Pause affordance is rendered

### Requirement: Auto-advancing carousels pause on hover and focus

Each rendered carousel with more than one logical item SHALL pause its CSS animation while a pointer hovers within its section or keyboard focus is within its section, and SHALL resume from the held position when the reason leaves. Pausing Games SHALL pause both moving lanes together so their per-card phases cannot drift apart despite independent loop lengths. Entering a static sibling lane SHALL retain the same Games pause scope. Playable SHALL retain its independent section pause scope, shared by both compact rows when present. Pause and resume SHALL be expressed only through CSS state such as `animation-play-state`; the implementation MUST NOT claim persistent Pause, Play controls, manual-stop persistence or document-hidden pausing that CSS cannot provide, and MUST NOT introduce script to provide them.

#### Scenario: Pointer hovers a running carousel

- **WHEN** pointer hover enters a running carousel section
- **THEN** its animation pauses at the current position
- **AND** it resumes from that position after hover leaves

#### Scenario: Focus enters a running carousel

- **WHEN** keyboard focus enters a canonical link or favorite in a running carousel section
- **THEN** its animation pauses without moving focus
- **AND** it resumes from the held animation position after focus leaves the section

#### Scenario: Games rows pause together

- **WHEN** hover or keyboard focus applies to either Games lane, including a static hero sibling
- **THEN** every moving Games lane pauses
- **AND** moving focus between lanes keeps them paused
- **AND** both resume with matched per-card phase once no pause reason remains

#### Scenario: Playable rows pause together

- **WHEN** hover or keyboard focus applies to either row of a two-row Playable collection
- **THEN** both compact rows pause and moving focus between them keeps both paused
- **AND** both resume with matched per-card phase after the final pause reason leaves Playable
- **AND** Playable and Games retain independent section pause scopes

#### Scenario: Reduced motion is requested

- **WHEN** `prefers-reduced-motion: reduce` is active
- **THEN** carousel animation is removed and leading slides render statically with the existing compact centering
- **AND** non-essential card transitions are removed and no opt-in autoplay is reintroduced
- **AND** all canonical links and favorites in both Playable rows and Games lanes remain keyboard reachable and revealable

#### Scenario: Item count changes between responses

- **WHEN** a rendered collection's logical count changes across full home responses, including Playable crossing six games
- **THEN** the new response renders the correct lane split, slide counts, singleton states and animation timing
- **AND** no stale index, timer or binding survives from the previous response

### Requirement: Carousel behavior is owned entirely by CSS

CSS SHALL own responsive hero and compact card geometry, fixed compact/hero aspect ratios and proportional card content, overflow containment, dwell and movement timing, continuous looping, seamless wrap, pause state, transition presentation and reduced-motion overrides. Carousel advancement SHALL use CSS keyframes with stepped timing over a track whose lane-local slide count is provided as a custom property. The shared carousel cadence, including Playable, SHALL use a five-second dwell followed by a 1.1-second `ease-in-out` movement; stepped sequence duration SHALL be the lane's logical count multiplied by the 6.1-second cycle. A singleton lane SHALL have no animation or loop copies. Derived-value types SHALL be inferred from typed inputs and computations without redundant annotations or comments explaining the derivation. Card markup and card CSS SHALL belong to the Home-owned GameCard component, while the Home-owned GameCollection component SHALL own section rendering, lane derivation, loop copies and carousel CSS. Home SHALL compose these internal page components without duplicating their rendering or motion logic. Svelte SHALL NOT add carousel state, timers, listeners, observers, selected-index synchronization, manual-scroll reconciliation, document-visibility tracking, dynamic carousel-position ARIA status or scripted carousel navigation controls; it MAY derive the two contiguous lossless lanes, render conditional sections and lanes, provide lane-local counts and singleton flags, and render loop copies with section-appropriate accessibility treatment: Games and Playable copies retain pointer activation with `aria-hidden` and link/button `tabindex="-1"`; neither section uses inert copies. Favorites SHALL use Inertia forms with local processing and server-confirmed membership without shared pending coordination, feedback state or result announcements, and SHALL NOT control carousel motion or selection. The primary production path MUST NOT depend on `::scroll-button()`, CSS anchor positioning, scroll snap events, container scroll-state queries or scroll-driven animations.

#### Scenario: Carousel advances without script

- **WHEN** a multi-item carousel is rendered and its dwell elapses
- **THEN** the next slide is settled by the CSS animation alone
- **AND** no timer, listener or observer participates in advancement

#### Scenario: Viewport changes

- **WHEN** home moves between supported desktop, tablet and mobile widths
- **THEN** CSS adapts hero and compact geometry within the shell without page-level inline overflow
- **AND** each moving lane retains the shared dwell/movement cadence and its own count-derived loop

#### Scenario: Experimental features are unavailable

- **WHEN** a current target lacks generated scroll buttons, anchor positioning, scroll snap events, scroll-state queries or scroll-driven animations
- **THEN** every multi-item carousel remains advancing, looping, pausable and linked through the baseline path
- **AND** singleton lanes remain static and linked

#### Scenario: Visual loop duplicate is present

- **WHEN** CSS looping uses duplicated slides for seamless wrap and focus runway
- **THEN** Games and Playable duplicate slides retain pointer-usable links and favorite controls, remain hidden from the accessibility tree, and exclude their links/buttons from sequential keyboard focus
- **AND** no real game is available solely through a duplicate
- **AND** loop travel is based on the lane's logical sequence rather than half of a rendered track with an additional tail

### Requirement: Carousel links and focus remain accessible

Each delivered game SHALL expose exactly one semantic canonical detail link in each owning section using the server-resolved route slug with a meaningful accessible name and visible focus. Within Games, this link SHALL belong to the game's assigned hero or compact lane, and both lanes' canonical content SHALL remain in the accessibility tree and tab order. Only duplicated loop content SHALL be excluded. Canonical link order through hero then compact SHALL reproduce Games response order. When Playable has two compact rows, canonical first-row then second-row order SHALL reproduce its response order, with both rows remaining in the accessibility tree and tab order. A canonical link receiving keyboard-visible focus SHALL be fully revealed with its focus outline inside its own carousel viewport by CSS overflow containment and the loop-copy runway, without script involvement. The implementation SHALL NOT provide scripted Previous, Next, Play or Pause controls or dynamic carousel-position status announcements, and canonical links SHALL carry their own accessible names. Card links in Playable, hero and compact lanes SHALL use `aria-label` with their game title or `Open game` when the title is absent or empty, without generated card-title IDs or card `aria-labelledby` references. Existing section-heading naming relationships SHALL remain intact. While a carousel viewport contains keyboard-visible focus, it SHALL use native mandatory horizontal CSS snapping with only the card containing that focus eligible for centered alignment. Computed/inherited card, preview, and link scroll margins SHALL NOT determine focus placement. Unfocused cards SHALL NOT be competing snap targets; unfocused overflow clipping/autoplay and existing focus reset/pause SHALL remain intact. No JavaScript scrolling, snap events, or hard-coded focus offsets SHALL be introduced.

#### Scenario: Canonical and duplicate content coexist

- **WHEN** a loop boundary renders duplicated content beside canonical content
- **THEN** only canonical links and their sibling favorite buttons participate in keyboard and accessibility order
- **AND** each delivered game contributes exactly one accessible detail link in its owning section

#### Scenario: Keyboard focus reaches a canonical link

- **WHEN** a canonical hero or compact link receives keyboard-visible focus
- **THEN** the link shows visible focus and its section's moving tracks pause
- **AND** the focused link is revealed within its own carousel viewport through CSS containment and the loop-copy runway

#### Scenario: Focus enters after autoplay has passed the first game

- **WHEN** keyboard-visible focus reaches an earlier canonical link after multiple advances or a lane wrap
- **THEN** CSS temporarily neutralizes paused track displacement so native focus scrolling can reveal that link
- **AND** its focus outline is visible within the card
- **AND** leaving the section clears temporary focus scroll and restores compact centering with the held CSS animation position

#### Scenario: Keyboard focus moves forward or backward between cards

- **WHEN** native Tab or Shift+Tab reaches a canonical link or favorite in Chromium or Firefox
- **THEN** only the focused card is eligible for centered native snapping and its complete target and focus outline remain inside its viewport
- **AND** a neighboring card cannot capture the snap destination
- **AND** leaving keyboard-visible focus disables the focus-only snapping and restores the existing unfocused carousel behavior

#### Scenario: Focus reaches every compact game

- **WHEN** the user tabs through a 32-entry Games section or reverses direction with Shift+Tab
- **THEN** all eight hero and 24 compact canonical links are reachable in response order or its reverse
- **AND** sequential keyboard focus never enters a loop copy or trapped within the section
- **AND** the same complete route remains available with reduced motion

#### Scenario: Fallback metadata keeps a meaningful name

- **WHEN** a delivered game has no runtime display name
- **THEN** its canonical link retains a generic accessible name
- **AND** it remains keyboard reachable in server order in its assigned lane

#### Scenario: Keyboard focus reaches a favorite control

- **WHEN** a canonical favorite button, including the last card's shared favorite control, receives native keyboard-visible focus after autoplay has advanced
- **THEN** its complete hit target and focus outline are revealed within its carousel viewport by the same CSS containment and focus treatment as the card link, including focus inside the shared button component
- **AND** the section pauses without moving focus or adding scripted carousel navigation

### Requirement: Compact carousel rows center their middle visible card

Each multi-item compact lane in either Playable row and the lower Games carousel SHALL align the middle visible card's center with its own viewport's center at every unfocused settled position. The initial middle card SHALL be the second entry in that lane's assigned sequence. Neighboring cards SHALL remain partially visible with equal clipping at the left and right viewport boundaries. CSS SHALL derive this composition from the existing responsive container and lane-local card geometry, retain it for unfocused reduced motion, and preserve continuous looping without blank gaps, including two-item lanes. Hero and singleton inline geometry SHALL retain their existing responsive behavior, while card heights SHALL retain their fixed compact or hero aspect ratio; singleton detection SHALL use lane count rather than the total Games response. Keyboard-visible focus SHALL temporarily clear compact alignment as needed to reveal canonical links and SHALL restore centered composition when focus leaves. Additional Games loop coverage SHALL remain pointer-activatable and hidden from the accessibility tree, with anchors excluded from sequential keyboard focus. Playable loop coverage SHALL remain pointer-usable and hidden from the accessibility tree, with links and favorite buttons excluded from sequential keyboard focus.

#### Scenario: Compact rows render

- **WHEN** either Playable compact row or the Games compact lane contains multiple assigned entries at a supported desktop, tablet or mobile width
- **THEN** the initial middle visible card in each compact row is centered within that row's viewport
- **AND** neighboring cards are clipped symmetrically at its boundaries
- **AND** the Games hero retains its responsive inline geometry with its fixed 2.9:1 ratio

#### Scenario: Centered compact tracks advance and wrap

- **WHEN** a multi-item compact lane advances through a complete loop, including a two-item lane
- **THEN** each settled middle card is centered and no blank gap appears during advancement or wrap
- **AND** lane order, shared per-section per-card cadence, independent loop counts and accessible link uniqueness are preserved

#### Scenario: Reduced motion keeps the centered composition

- **WHEN** reduced motion is requested and neither section contains keyboard focus
- **THEN** multi-item compact lanes show a centered static middle card with symmetrically clipped neighbors
- **AND** no animation is introduced

#### Scenario: Keyboard focus reveals the first canonical card

- **WHEN** focus enters a canonical link in a centered carousel section
- **THEN** the focused link and its focus outline are revealed through the existing CSS focus behavior
- **AND** leaving the section restores compact centering and the held animation position

#### Scenario: A lane contains one entry

- **WHEN** a Playable, hero or compact lane contains exactly one entry
- **THEN** it retains static responsive inline sizing, retains its fixed compact or hero aspect ratio, and has no loop copy
- **AND** it does not receive multi-item compact alignment solely because another lane contains games

### Requirement: Home titles use more available card space

Compact and Playable headings SHALL use a smaller card-relative font and wrap long words, with up to four visible lines and a three-line limit on the narrowest cards to preserve footer clearance. Hero headings SHALL retain their existing font scale. This title-specific fitting SHALL preserve its existing font sizes and 1.2 line height; the favorite square SHALL match the first line box height without changing card ratios, footer typography, accessible names, links or carousel behavior. Arbitrarily long text MAY still ellipsize; fitting SHALL use CSS without JavaScript measurements or title-length heuristics.

#### Scenario: Long compact title has enough room

- **WHEN** the CompactMetadata title is shown at the supplied desktop card width
- **THEN** the full title wraps into four lines above the footer instead of ellipsizing after two
- **AND** it remains clear of the favorite target and lower metadata

#### Scenario: Narrow cards cannot show the entire title

- **WHEN** the card width is below 12rem
- **THEN** the title occupies at most three lines and may ellipsize without overlapping the footer
- **AND** complete title text remains available in its existing accessible names

#### Scenario: Short or absent titles are rendered

- **WHEN** an existing short-title or untitled game is displayed in either theme
- **THEN** its title retains normal wrapping or absence, and the existing card, favorite and link remain usable

### Requirement: Home tests cover application behavior and visual states

Production GameCollection Storybook scenarios with semantic play assertions SHALL describe the disjoint Games split, exact canonical membership/order, internal local and numeric links, conditional lanes and the Playable one/two-row threshold. The bounded collection matrix SHALL contain Hot counts 1/2/3/4 and Playable 1/2/4/7; seven is the smallest distinct two-row example. Games 5/8/31/32 and Playable eight SHALL NOT remain as redundant count stories. Remove the 30 references directly orphaned by those five high-count stories and the six references of the subsequently removed standalone Empty story; retained references SHALL NOT be regenerated or updated for this task. The isolated browse examples SHALL display Home's `Hot (by BGG)` heading/link; Playable examples SHALL clear inherited attribution. Empty collection omission SHALL remain covered through Home composition instead of a standalone blank collection story. Public Home, authenticated Home and GameCollection default browse composition SHALL use four games, while explicitly distinct empty, conditional and same-game overlap cases SHALL retain their own fixtures. Shared larger fixtures used by existing browser checks SHALL remain available; this story simplification SHALL NOT change runtime selection limits.

Production GameCard stories SHALL own title, image, metadata fallback, nullable stage, status and unsaved hover/focus plus confirmed saved compact/hero states. Home stories SHALL retain default/empty composition, conditional sections, OverlappingSingletons, authentication dialogs and confirmed saved membership overlap, local Form pending state and failure/retry behavior without notifications without repeating count or card-content matrices at page level. Stories SHALL import the Home-owned production components and reuse deterministic fixtures/mocks. Retain their existing story paths, titles and IDs during the ownership relocation without republishing page internals through a production barrel.

The user explicitly waives further Storybook execution, screenshots/reference updates, rendered review and high-count natural-motion demonstrations for the current first-line/card-state, two-row Playable and page/feature ownership amendments. Acceptance for this bounded task SHALL use source review, scoped native formatting/lint, TypeScript/Svelte checks and focused non-screenshot semantic threshold/keyboard checks. Validation records SHALL distinguish actual results, prior attempted captures and unverified visual/motion behavior; static evidence SHALL NOT be reported as a visual or motion pass. The existing six theme/viewport projects and whole-feature delivery gates SHALL remain unchanged.

Separate Home browser tests SHALL continue to focus on native keyboard focus/reveal, clipped-card pointer/Enter event delivery and reduced-motion presentation. A focused non-screenshot collection test MAY additionally verify the six/seven/eight row boundary and canonical keyboard order because the waived Storybook execution otherwise leaves this new branch unexecuted; it SHALL NOT duplicate the full story matrix. Tests SHALL NOT assert visual loop-copy counts, inert ancestry, exact tabIndex or aria-hidden values, per-copy action bindings or duplicated badge counts. Tests SHALL NOT query animation objects or CSS timing properties, manipulate playback or current time, or assert browser interpolation, pause/resume, duration, geometry or looping mechanics.

#### Scenario: Keyboard navigation is verified

- **WHEN** a non-screenshot browser test tabs through a two-row Playable collection
- **THEN** canonical links and favorite buttons are reached in first-row then second-row server order and loop copies never add keyboard stops
- **AND** the test does not seek or inspect browser animations or record screenshots

#### Scenario: The Playable row boundary is verified

- **WHEN** focused non-screenshot semantic checks render six, seven and eight playable entries
- **THEN** six exposes one compact canonical sequence, seven exposes 4/3 sequences and eight exposes 4/4 sequences
- **AND** row concatenation matches the input, no game is missing or duplicated canonically, and no hero list appears

#### Scenario: One game appears in both sections

- **WHEN** the same BGG ID is delivered in Playable and Games
- **THEN** each section exposes one canonical link for that game
- **AND** both canonical links retain the game title as their accessible name and loop copies add no accessible link

#### Scenario: Collection story source is simplified

- **WHEN** collection and Home story definitions are reviewed
- **THEN** Games counts stop at four and Playable seven remains solely for its distinct two-row branch
- **AND** public Home, authenticated Home and GameCollection default browse composition uses four games
- **AND** semantic assertions describe exact lane membership and accessible ordering without CSS variable or timing assertions

#### Scenario: Storybook validation is explicitly waived

- **WHEN** the current amendment is checked under the user's instruction not to run or inspect stories
- **THEN** source review and scoped formatting/lint/type checks are recorded with any focused non-screenshot semantic results
- **AND** no screenshot capture, reference update, visual pass or natural-motion demonstration is required or claimed for this bounded task
- **AND** prior attempts remain historical evidence and separate whole-feature gates stay open

#### Scenario: Card content variants remain isolated

- **WHEN** title, image, metadata or status varies independently of Home composition
- **THEN** the corresponding production GameCard story retains its semantic assertions
- **AND** Home does not repeat the same standalone visual variation as a full-page story

#### Scenario: Visible title lines retain descenders

- **WHEN** a short or long title contains descending letters
- **THEN** each visible line retains its glyph ink and a compact shadow without a hard gray rectangular edge
- **AND** the existing three/four-line limits, full accessible name and footer clearance remain intact

#### Scenario: Saved and preview card states are reviewed independently

- **WHEN** isolated GameCard stories render unsaved hover and authenticated saved compact/hero cards
- **THEN** the hover preview retains its Add action and unpressed state while saved cards expose Remove and pressed state true
- **AND** story source preserves the filled-icon and first-line-alignment scenarios without repeated page stories or automated geometry assertions, while rendered comparisons remain waived for the current amendment
