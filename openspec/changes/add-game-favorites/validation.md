# Favorites implementation validation

Issue: [#123](https://github.com/ravecat/d20/issues/123). Owning branch: `worktree/game-favorites`, now based on `577c5c34ed1fa5dbb873f90c99b3e747d2cfb0ee`. Implementation was explicitly authorized on 2026-09-10; conflict resolution and adoption of master's six-cell visual matrix were authorized on 2026-09-11. The change remains active until the required validation and rollout gates are met.

## Current outlined/filled star amendment

On 2026-09-11 the user requested a gold outline with a transparent interior for unsaved games and the same outline with gold fill for saved games. The parent favorite button's CSS owns paint inherited by the existing SVG/path on both Home and detail. This supersedes the filled pale/bright and opacity-only distinction; detail text/box/layout and all interaction remain unchanged. Task 3.6 is implemented; task 4.7 awaits visual acceptance, bringing current progress to 15/21 including the concurrent title amendment. Scoped ESLint and formatting passed, all 8 shared favorite browser tests passed in Chromium/Firefox, all 19 game-page unit tests passed, and TypeScript/Svelte checks reported no errors or warnings. Native screenshot validation remains incomplete for the reasons recorded below. Earlier screenshot evidence remains historical for star appearance; existing behavior, geometry, tooltip, and backend evidence retain their recorded scope.

### Star amendment verification and remaining visual gate

The only product file edited for this amendment is `assets/js/shared/components/favorite_button.svelte`. Its parent button owns `fill: none` and `stroke: #eab308`; the existing saved selector changes only fill to that gold. SVG/path markup, JavaScript, geometry, hit targets, detail text, and form behavior are unchanged. Scoped source review found no defect.

Passed: scoped ESLint and `oxfmt --check`, 8 shared-favorite browser cases in Chromium/Firefox (6.06 seconds), `bun run test:unit -- tests/pages/game/ui/game.test.ts` (19 tests), `bun run typecheck` (zero errors/warnings), strict owning-change OpenSpec validation, and `git diff --check`. Logs for the latter frontend checks are under `/tmp/d20-favorite-star/`.

Chrome DevTools inspected existing page 5 at `http://localhost:6006/?path=/story/home--compact-metadata&globals=theme:dark`, confirmed as this worktree's Storybook process. Hot-reloaded unsaved paths inherited no fill, gold stroke, and full opacity. No route, account, controls, or storage was changed. Before/actual/difference crops of 12 detail cells (WithSession and WithoutSession across both themes and three native viewports) were reviewed; their changed pixels were confined to star paint. This is partial evidence, not full visual acceptance.

The full Home/detail comparison was interrupted after another task edited `home.svelte` at 23:24:17 local time and began overlapping title validation in this same worktree. The Home browser run returned 9 failures, 1 pass and 2 skips against old references and an evolving source; screenshot differences and a screenshot timeout were observed. It is not a passing behavioral or visual result. The isolated detail attempts, including `--maxWorkers=1`, were also stopped as concurrent visual runners occupied nearly all 31 GiB RAM and 31 GiB swap. Task-owned visual processes were stopped; other tasks' processes and code were preserved.

No reference images were updated by this amendment. The 228 Storybook and 18 Home browser references present at start were preserved under `/tmp/d20-favorite-star/before/`. Complete task 4.7 after the concurrent title source/reference work settles: review the combined Home/title/star changes, finish detail comparisons, narrowly update reviewed references, and rerun normal comparisons. Existing #281 below-fold capture limitations and the four whole-feature delivery gates remain open. No archive, commit, or deployment is claimed.

## Accepted redundant adapter work before the star amendment

On 2026-09-11 the user accepted redundant catalog/BGG reads during Favorites partial refreshes and removed skipped-loader behavior from application acceptance. Keep lazy callbacks and runtime/dependency code unchanged. The two controller tests retain add/remove, safe 303 redirects, private membership, authentication/errors, and omitted-prop coverage while allowing ordinary catalog fixtures. Provider-independent DELETE mutation and ordinary detail/Session validation remain required. Extra work can add latency and load and remains subject to existing error boundaries and provider fallback.

Task 4.1 is complete and progress is 13/17. `env -u BASH_ENV mix test test/d20/games/favorites_test.exs test/d20/games_test.exs test/d20_web/controllers/game_favorite_controller_test.exs test/d20_web/controllers/page_controller_test.exs` passed all 142 tests in 5.0 seconds, exit 0 (`/tmp/favorites-contract-review/backend.log`). `env MIX_ENV=test mix format test/d20_web/controllers/game_favorite_controller_test.exs test/d20_web/controllers/page_controller_test.exs` passed, exit 0. Scoped review confirmed exactly two test renames, removal of two failure-on-BGG stubs and one `ALTER TABLE games`, with all application assertions preserved and ordinary provider setup retained.

The subsequent `mix format --check-formatted` check passed, exit 0, and runtime hashes were unchanged. Earlier 140/142 and other rc5 failures below are historical evidence against the superseded no-loader requirement. Existing frontend evidence carries forward because its source and behavior are unchanged. Tasks 4.3/4.4/4.5/5.1 remain open; no archive or completion commit is included.

All earlier milestone records below retain their original commands, outcomes, and then-current gates. Their requests to preserve failing loader assertions are superseded by this acceptance decision. The current delivery gates are listed separately below.

## Historical consolidation of detail Session policy

Tasks 1.1/2.2 are verified (12/17). Shared visibility/Session resolution now lives in Games.resolve_session/2 immediately after fetch_by_slug/1. Removed the standalone Detail module and intermediate private game_session helper; Favorites and PageController call the existing Games context. Five direct clauses preserve provider/local behavior, nil/valid/mismatched/missing/malformed Session handling, hidden-game access through a matching Session, exact session fields and one Session lookup. Favorite error normalization and web-owned channel topics remain intact. Before favorites, the logic lived in PageController.render_game_detail/3 and resolve_game_session/2; the interim web/domain helper modules are no longer needed.

- `CI=true MIX_ENV=test nix develop --command mix test test/d20/games_test.exs test/d20/games/favorites_test.exs test/d20_web/controllers/game_favorite_controller_test.exs test/d20_web/controllers/page_controller_test.exs`: 140/142 passed in 3.0 seconds, exit 2. All Games/favorites/Session cases passed. Only the retained rc5 skipped-loader contracts failed at favorite-controller-test line 290 and page-controller-test line 1387. No tests or assertions were changed.
- Locked formatting and scoped `mix credo --strict` over games.ex, favorites.ex and page_controller.ex passed, exit 0. After behavioral tests, the resolver block was moved unchanged from after list_by_provider to immediately after fetch_by_slug; exact block-text equality was verified. Formatting, final source review and Credo cover this placement-only adjustment; no redundant behavioral rerun was needed.
- Independent review accepted the final four-path diff and hashes, including deletion of Detail. Protected hashes verify unchanged schema, favorite HTTP controller, tests, routes, dependencies and frontend. The old module/helper aliases and calls are absent from lib/test. There is no new entity, persistence change, lookup, protocol or frontend behavior, and prior visual evidence carries forward.

Evidence: `/tmp/favorites-detail-review/implementation.diff`, `implementation-hashes.json`, `untouched-hashes.json`, `backend.log`, `format.log`, and `credo-final.log`. The five whole-feature gates remain open, including the previously recorded adapter, stage-constraint and broad Credo failures. No commit, archive, deployment, migration or dependency change is included.

## Verified removal of custom ID validation before consolidating Session policy

Tasks 1.1/2.1 are verified (12/17). Removed expected_id/1, custom regex/type/range checks and invalid_bgg_id response/message. HTTP actions use standard String.to_integer/1; context APIs take integer IDs. Supported generated links, account/source/Session policy, schema/database constraints and storage-error handling remain intact. Obsolete friendly malformed-ID assertions are removed; valid mutation, privacy, idempotence, concurrency, FK and stale-identity coverage remains. No new entity, migration, frontend or URL change is involved.

- `CI=true MIX_ENV=test nix develop --command mix test test/d20/games/favorites_test.exs test/d20_web/controllers/game_favorite_controller_test.exs`: final result 25/26 passed in 0.6 seconds, exit 2, including all 11 context cases. The sole failure is the retained rc5 favorite-redirect skipped-loader contract at controller-test line 290 (formerly 306). The unchanged PageController suite was not rerun; its earlier separate rc5 failure remains an open whole-feature gate. No failing adapter assertion was skipped or weakened.
- Locked formatting passed. Initial scoped Credo required removing the now-redundant explicit try around the complete delete body. The correction uses equivalent function-level rescue; create retains its narrower persistence-only try. Final locked `mix credo --strict` over the four touched paths passed with no findings, exit 0. Focused tests were rerun on that final source. A transient syntax error during the correction was fixed before final formatting and tests.
- Independent review accepted all four final file hashes and the identical deletion rescue scope. Protected source hashes confirm that schema, shared detail policy, routes, PageController and its tests, dependencies and frontend remain unchanged. Old expected_id/invalid_bgg_id code and assertions are absent. Existing frontend/visual evidence carries forward without another run.

Evidence: `/tmp/favorites-id-review/implementation.diff`, `implementation-hashes.json`, `untouched-hashes.json`, `backend.log`, `backend-before-implicit-try.log`, `format.log`, `credo-initial.log`, and `credo-final.log`. The five whole-feature gates remain open, including previously recorded adapter, stage-constraint and broad Credo failures. No commit, archive, deployment, dependency or migration change is included.

## Verified create/delete context amendment before removing ID validation

Tasks 1.1/2.1 are verified (12/17). `Favorites.create/4` and `delete/2` perform persistence directly. Removed save/add/remove APIs, the mutate callback wrapper and the identity-comparison helper; retained shared strict ID parsing and source-error normalization. Authentication-first validation, identity/Session policy, idempotence, source-independent deletion and persistence-only transient-error handling are preserved. HTTP/frontend contracts, shared Detail policy and migrations are unchanged.

- `CI=true MIX_ENV=test nix develop --command mix test test/d20/games/favorites_test.exs test/d20_web/controllers/game_favorite_controller_test.exs test/d20_web/controllers/page_controller_test.exs`: 96/98 passed in 1.8 seconds, exit 2. All 11 context tests passed, including actual concurrent creates, duplicate timestamp preservation and FK changeset failures. The only failures remain the rc5 skipped-loader contracts at favorite-controller line 306 and page-controller line 1387. No assertions were skipped or weakened.
- Locked `mix format` passed for all five touched Elixir files. Locked `mix credo --strict` with those five positional paths passed with no issues, exit 0.
- Independent review accepted the five-file diff. All frozen implementation and protected-source hashes match; old Favorites save/add/remove calls are absent from lib and test. Test fixtures use Repo; mutation behavior uses the validated commands. Existing frontend/visual evidence carries forward because source and contracts are unchanged.

Evidence: `/tmp/favorites-crud-review/implementation.diff`, `implementation-hashes.json`, `untouched-hashes.json`, `backend.log`, `format.log`, and `credo.log`. The five whole-feature gates remain open, including the recorded adapter, stage-constraint and broad Credo failures. This amendment includes no commit, archive, deployment, dependency or migration change.

## Verified context-owned validation before create/delete simplification

Tasks 1.1/2.1 are verified (12/17). Following the interest-controller pattern, GameFavoriteController now delegates to `Favorites.save/4` and `remove/2` and retains HTTP inputs, safe redirects and error presentation. Account checks, strict BGG parsing, source resolution and identity comparison belong to Favorites. Trusted `add/2` remains the catalog-independent storage API; the unused public ID predicate was removed. The controller shrank from 90 to 49 lines.

Moved the existing visibility/Session resolver from `D20Web.GameDetail` to `D20.Games.Detail`, with no domain-to-web dependency. PageController adds the channel topic to its existing session prop, preserving the HTTP shape and one shared policy. The old web helper was removed. Routes, descriptor/form fields, errors, persistence, frontend and visuals are unchanged.

- Locked `CI=true MIX_ENV=test nix develop --command mix test test/d20/games/favorites_test.exs test/d20_web/controllers/game_favorite_controller_test.exs test/d20_web/controllers/page_controller_test.exs`: 96/98 passed in 2.0 seconds, exit 2. All 11 Favorites domain cases passed, including four added cases for missing-account precedence, strict IDs/malformed sources, identity drift/removal independence, and matching-Session visibility. The only failures are the same rc5 skipped-loader contracts at favorite-controller line 306 and page-controller line 1387.
- Locked formatting passed. Scoped Credo checked five files and found no issues, exit 0 (`credo-scoped.log`). An initial unsupported scope option instead checked 178 files and reproduced the three retained Koala nesting findings (`credo.log`); it is not reported as a passing run. No unrelated correction was made.
- Independent review accepted the stable six-path diff, including removal of the old helper and exact session projection. All recorded source hashes remained unchanged and `git diff --check` passed. Frontend source hashes match the previously verified uniform-route revision, so its 19 unit, 18 active browser and 90 visual results carry forward without another frontend run or reference updates.

Evidence: `/tmp/favorites-context-review/implementation.diff`, `implementation-hashes.json`, `backend.log`, `format.log`, `credo-scoped.log`, and `credo.log`. The five whole-feature gates remain open, including the recorded adapter, stage-constraint and broad Credo failures. No commit, archive, deployment, dependency or migration change is included.

## Verified uniform resource amendment before context extraction

Implemented PUT and DELETE at the exact same query-free `/favorites/:bgg_id` URL in 11 product/test files. Tasks 1.2/2.1/3.1 are verified (12/17). `FavoriteDescriptor {bggId, action, slug, session?: string}` retains original source spelling and optional validated Session context. PUT submits that context; DELETE submits only `response_to`. The path supplies expected identity through Phoenix's ordinary path-parameter precedence, with conflicting body/query IDs covered by regression tests. PUT checks resolved identity/visibility, DELETE remains independent of source/provider, and the old route/split actions/redundant hidden `bgg_id` are removed. Auth/CSRF, idempotency, safe 303 errors, selective props, copies, and all appearance remain intact. No migration, dependency, alias, or raw-header workaround was added.

- `mix test test/d20_web/controllers/game_favorite_controller_test.exs test/d20_web/controllers/page_controller_test.exs`: 85/87 passed, exit 2. All new route/source/path-override and projection cases passed. The same two rc5 failures remain: favorite form redirect invokes excluded discovery (line 306), and Home partial response invokes excluded collection loaders (line 1387), both through `collect_once_meta_if_skipped/4`. No assertion was skipped or weakened.
- `bun run test:unit -- tests/pages/game/ui/game.test.ts`: 19/19 passed in 13.71 seconds. Scoped Home/shared FavoriteButton `test:browser`: 18 passed, two existing Firefox skips, exit 0 in 40.99 seconds. Request assertions verify one action, original slug/optional Session on PUT, response target only on DELETE, retry, membership, focus and keyboard behavior.
- `bun run test:visual -- stories/pages/authenticated/home.stories.ts stories/pages/public/game.stories.ts --browser.trace off --maxWorkers 1 --reporter verbose`: all 90 normal comparisons passed in 46.56 seconds, exit 0, with the known 10-second close warning. No references were updated; hashes of all 246 existing Home/detail/browser images remain identical. Existing accepted appearance and #281 capture limitations carry forward.
- Focused Elixir formatting, ESLint/oxfmt, full TypeScript/Svelte checks (zero errors/warnings), and `git diff --check` passed. `bun run storybook:build` passed with its standard chunk warning. Independent review accepted the isolated 11-file diff and confirmed unchanged tooltip/visible markup; all 11 source hashes remained stable through validation.
- DevTools page 6 was reloaded on the original responsive Dark Home Index. Rendered favorite forms expose the new `/favorites/<id>` actions and original `slug`/`response_to` fields with no body `bgg_id`. The route/view state is unchanged and console warnings/errors are absent. This inspection does not replace the open full-feature live-app gate.
- Plain `just check` could not start because `just` was outside the shell PATH (exit 127). The repository's `nix develop --command just check` ran compilation/formatting and all backend tests: 930/933 passed in 30.3 seconds. Besides the two rc5 failures, `test/d20/games/game_test.exs:62` expected `games_stage_domain` but PostgreSQL returned `games_launch_stage_requires_engine`. The same assertion failed in an isolated Nix test (0/1, 20 excluded); its schema/test files have no working diff. Three retained Koala Credo nesting findings at lines 250, 345 and 383 followed, and the recipe exited 2 before later broad checks. The broad run is not green; no unrelated schema/test or Credo correction was made.

Evidence is under `/tmp/favorites-uniform-review/`: `implementation.diff`, `implementation-hashes.json`, `backend.log`, `unit.log`, `browser.log`, `visual.log`, `screenshots-final.json`, `lint.log`, `typecheck.log`, `storybook-build.log`, `just-check-nix.log`, and `game-stage-repro.log`. Tasks 4.1/4.3/4.4/4.5/5.1 remain open for their whole-feature gates. No archive, deployment or completion commit is recorded.

## Verified tooltip correction before uniform resource amendment

The screenshot review identified card-relative `0.75em` text and its width cap as the cause of enlarged, wrapped, left-shifted tooltips. At the preceding milestone tasks 3.5/4.6 were complete (12/17): icon tooltips use stable `0.75rem`, `max-content`, and nowrap text, with feature-detected CSS Anchor Positioning and a manual Popover top layer. A Svelte attachment initializes the popover and guards `togglePopover` against disconnection; fixed placement uses `anchor(bottom)`/`anchor(right)` with ordered block, inline, and combined flips. Initial popover initialization/disconnection runs failed diagnostically and were corrected; they are not recorded as passing evidence. The local absolute fallback below the button remains, including accepted possible carousel-edge clipping on Firefox 128/Safari 17.4 without anchors. No JS geometry, observers, portals, libraries, browser-policy changes, or favorite/detail behavior changes were introduced.

- Native browser update passed 18 tests with two existing Firefox skips in 28.30 seconds, exit 0; normal comparison passed 18 with the same skips in 28.07 seconds, exit 0 (`/tmp/favorite-tooltip-review/browser-compare.log`). The new shared semantic test covers hoverable tooltip content, no hover submission, retained focus, Escape dismissal, and keyboard favorite activation. Existing loop-copy/reduced-motion behavior remains covered.
- Authenticated Home update passed all 30 cells in 37.34 seconds, exit 0; normal comparison passed 30 in 37.33 seconds, exit 0 (`/tmp/favorite-tooltip-review/visual-compare.log`). The known 10-second close warning remains. Review accepted all 30 before/new pairs: 18 expected tooltip changes and 12 identical cells, including full Add/Remove text across six theme/viewport variants. The saved-overlap story ends with hover to expose Remove text.
- All 18 browser reference pairs were accepted: three last-focus captures changed to `documentElement` to include top-layer text; 15 remain identical. Across the 186 existing references, 21 changed (18 authenticated tooltip plus three browser) and 165 are identical, without path changes (`/tmp/favorite-tooltip-review/final-image-diff.json`). Card/title/star/metadata geometry is unchanged. No automated geometry assertions or screenshot configuration changed; #281's white/below-fold capture limitation remains.
- Manual page 6 checks at the prepared responsive width and 320-pixel mobile, in both themes, showed one-line 12-pixel tooltip text. Left-edge placement used the inline flip; at 160-pixel viewport height the block flip placed tooltip bottom and button top at 95.45 pixels (tooltip 131.20 by 26.77 pixels). Escape left zero open popovers. The original Home Index responsive Dark URL was restored with zero console warnings/errors. These checks establish the supported native path, without claiming runtime testing of every legacy browser floor.
- Focused lint/format, full typecheck, and source review passed. Approved card geometry, chip typography, and natural motion evidence carries forward; only affected tooltip references were rerun. Issue #123's final body was updated to the current 12/17 tooltip scope and exact results, preserving its five full-feature gates (`/tmp/favorite-tooltip-review/issue-body-final.md`). The five whole-feature gates remain open, and no archive or completion commit is recorded.

## Verified metadata/status typography before tooltip correction

The user approved card geometry and requested less dominant metadata/status. Task 3.5 is complete: exactly two font-size declarations changed from `0.65em` to `0.5em`, with inherited em chip dimensions shrinking proportionally. At this preceding milestone task 4.6 was also complete, bringing progress to 12/17. Headings, stars, ratios, card padding, 44-pixel targets, wrapping/clipping, and behavior remain unchanged; no new story/test files were added.

- Manual checks covered 35 canonical cards at 1280/1024/320 pixels in dark theme, 320 pixels in light theme, and the restored prepared width. No outside-card, partial-chip, overlap, or hit-target violations were found; metadata/status-to-heading font ratio error was below 0.000003 and ratio-height error below 0.01 pixels. Native mobile text measures 5.48-6.02 pixels, an explicit readability tradeoff. Evidence: `/tmp/favorite-chips-review/manual-summary.json`. Page 6 was restored to its original `home--index&globals=theme:dark` URL and responsive 697.09 by 768 viewport, with a clean console and no overrides.
- Focused ESLint/format checks, full typecheck (zero errors/warnings), Svelte autofixer, and diff checks pass. The layout/motion source is unchanged, so prior six-width geometry and natural motion evidence carries forward without unrelated revalidation.
- Native Home reference update passed 168 cases in 175.05 seconds, exit 0; final normal comparison passed all 168 in 161.23 seconds, exit 0, with the known 10-second cleanup warning. Browser update passed 16 tests with two existing Firefox skips in 26.43 seconds, exit 0; final sequential normal comparison passed 16 with the same skips in 23.07 seconds, exit 0, including both 32-link traversals.
- All 186 before/new references are reviewed and accepted: 112 desktop/tablet Home pairs, 56 mobile pairs, and 18 browser pairs (10 Chromium, eight Firefox). There are 180 changed references and six unchanged Empty references; normal comparisons left all 186 hashes unchanged in `/tmp/favorite-chips-review/final-reference-manifest.json`.
- The earlier concurrent browser comparison failed (14 passes, two existing Firefox skips, two Chromium failures) after a 15-second link-traversal timeout and a subsequent reduced-motion focus mismatch; `/tmp/favorite-chips-review/browser-compare.log` records 56.60 seconds and exit 1. The passing sequential rerun changed no source, references, or timeouts. Contention/cascading timeout remains a hypothesis, not an established cause.

Root verified issue #123 Open in D20 In Progress and amended it to the reopened 10/17 scope before product edits; immutable prior source/references are saved under `/tmp/favorite-chips-review/before`. The final issue body was submitted and reread byte-for-byte with the current 12/17 correction, results, and small-text tradeoff; #123 remains Open in D20 In Progress. The five whole-feature gates remain open. Earlier `0.65em` screenshot results below are historical for typography.

## Verified fixed-ratio geometry before typography correction

The user explicitly replaced intrinsic height growth and identical fixed content sizing with compact/Playable 2:1 and hero 2.9:1 ratios through resizing, variant-owned proportional padding, and visible content scaling from actual card containers. `--slide-basis` and geometry aliases are removed in favor of inline CSS sizing expressions; the final source uses native cqi/em scaling and removes the obsolete metadata wrapper. Metadata now uses native flex wrapping inside exactly one visible chip row with `overflow: clip`. Whole extra chips wrap below the row based on actual footer width, without count caps, width thresholds, overflow detection, or JS. Only long primary text may ellipsize; secondary chips must not be partly visible or ellipsized in the first row. Visually suppressed noninteractive category text remains in the DOM and accessible to assistive technology and is not aria-hidden. The transparent 44-pixel hit target remains independent of visible glyph alignment; behavior, detail, native focus snap, and no-JS motion/sizing boundaries remain intact.

At this preceding milestone tasks 3.5/4.6 were complete (12/17). The following evidence carries forward for unchanged geometry/motion; its screenshots precede the new typography correction:

- Manual checks covered all 35 canonical cards at each of 320/375/420/600/768/1280 CSS pixel viewport widths. No ratio, outside-card, overlap, partially visible chip, or hit-target violations were found; height error was below 0.01 pixels and SVG/heading-font difference below 0.012 pixels. Visible title, star, and lower-content insets matched the proportional geometry.
- Three CompactMetadata cards were reviewed at 320 pixels in both themes and at 600 pixels: extra chips wrapped wholly below the visible row while every category name remained accessible. Next Station's Transportation chip was wholly clipped; Shifting Stones' primary text ellipsized as intended. Compact mobile chip text is approximately 7.5-8 pixels, an intentional strict-scaling tradeoff, not a 12-pixel readability claim. The default Mobile Dark Index page 6 was restored, the console is clean, and the stylesheet was untouched.
- All 168 Home references were independently reviewed by viewport (56 each: 54 changed plus two Empty), alongside all 18 browser references (10 Chromium, eight Firefox). The 186-entry acceptance manifest is `/tmp/favorite-proportional-review/accepted-final-ref-manifest.json`.
- Native Home visual reference update passed all 168 cases in 138.57 seconds, exit 0. The following normal comparison reported 168 passes in 103.73 seconds, but its wrapper returned 143 after the known 10-second cleanup warning; it is not recorded as exit 0. The diagnostic normal rerun passed all 168 cases across 12 project/file combinations in 152.79 seconds with confirmed exit 0 (`/tmp/favorite-proportional-review/final-visual-compare-cleanup.log`). The known 10-second cleanup warning remained; the reporter identified leftover pngjs/ZLIB, Timeout, and FILEHANDLE handles. No source or configuration changes were made for this diagnostic.
- Native browser reference update passed 16 tests with two existing Firefox skips in 31.51 seconds, exit 0. Normal browser comparison then passed 16 tests with the same two skips in 21.14 seconds, exit 0 (`/tmp/favorite-proportional-review/final-browser-compare.log`).
- Final full lint and formatting passed, exit 0 (107 files); full typecheck and the Svelte autofixer passed. All four source hashes remained unchanged through review and checks. The index has zero staged entries and zero unmerged entries; the recovery stash is retained.
- Current-source native CSS motion was observed for 68.1 seconds by temporarily removing only Storybook's `animation: none` override. Playable completed three natural loops and the hero lane one; compact Games advanced ten steps, which does not establish a full 146.4-second compact loop. Settled focus held Playable's clock unchanged across 1.2 seconds, then blur resumed advancement while other lanes continued running. Evidence: `/tmp/favorite-proportional-review/final-motion.json`. The exact original stylesheet and default Mobile Dark Index page 6 were restored, with no temporary state and a clean console.
- Aggregate strict OpenSpec validation passed 86/86 items and the lifecycle check passed with 10 active changes (`final-openspec-all.log` and `final-openspec-check.log` in the same temporary review directory). The five whole-feature delivery gates below remain open; no archive or completion commit is recorded. Issue #123's final body was submitted and reread byte-for-byte, including that milestone's 12/17 scope, final validation, motion coverage, reviewed references, and the proportional-text tradeoff; it remains Open in D20 In Progress.

The interim threshold-based diagnostic (162 screenshot mismatches and six passes) is historical and its candidates were not accepted. Semantic category checks verify complete accessible text; screenshots/manual review establish clipping without `not.toBeVisible` assertions. The 60 detail references, #281 limitation, and five whole-feature delivery gates remain unchanged.

## Historical intrinsic card appearance and grid refinement

The user's review of the initial grid led to the delivered correction for inconsistent padding, differently scaled chips, clipped categories, and a cramped title. Completed tasks 3.5/4.6 deliver intrinsic content-safe minimum heights with preferred aspect ratios only where content fits, consistent shared padding and readable chip/status sizes across variants, natural category wrapping/footer stacking without cropped or hidden categories, and intentional bounded-title ellipsis with full accessible names. Cards stretch equally per lane using CSS. The header title/star grid, stable 44 by 44 CSS pixel control, existing pale/bright yellow heading scale, links/forms, focus/copies/pause/reduced motion, images/fallbacks, and detail presentation remain required.

At the intrinsic-layout milestone, progress was twelve of seventeen tasks. Tasks 3.5 and 4.6 were complete on that intrinsic layout and focused-card-only snapping source, with reviewed references, normal comparisons, manual checks, and source validation recorded below. Completed integration tasks 2.3/4.2 and their historical ten-of-fifteen evidence remain intact. The earlier 168-test appearance run was canceled with exit 130 before completion when the user requested the grid extension; no reference updates were accepted from that canceled revision.

Implementation findings and their corrections:

- Aspect-ratio transferred minimum sizing expanded a nominal 152-pixel card preview to 346 pixels. An explicit card grid column `minmax(0, 1fr)` and preview `min-inline-size: 0` keep content within the assigned inline size.
- Normal grid alignment let covers stay shorter than the equal-height lane. Preview `align-self: stretch` fills the available card height.
- Svelte-scoped `:has(:focus-visible)` excluded focus inside FavoriteButton and prevented the last canonical favorite from being revealed. Parent-scoped `:has(:global(:focus-visible))` now includes the shared control without changing CSS motion ownership.
- At 320 pixels in Chromium, the Next Station link occupied x=197..347 while the carousel viewport occupied x=16..291. The 25% viewport scroll padding plus inherited 61.8-pixel link scroll margin exceeded the available scrollport. Removing that viewport padding and its comment while retaining the link margin improved Chromium to x=79.27..229.82. Later Firefox review showed that margin-only placement was still insufficient; the final correction below supersedes that margin chain.

Final focus correction:

Native browser review found Qwinto partly outside the Firefox viewport with margin-only focus placement. The final source removes all three computed/inherited card/preview/link scroll-margin declarations and enables mandatory horizontal snapping only in the keyboard-focused viewport, with only the focused card eligible for centered snapping. The initial native check put Qwinto and Next Station at x=78.55..229.09 within x=16..291; final-source browser tests and fresh reference review then passed. An all-card snap variant was rejected because it could select a neighboring card. The existing global-focus reset/pause and unfocused overflow clipping/autoplay remain intact, without JS or hard-coded offsets.

Current validation evidence:

- Manual DevTools checks covered all 35 canonical cards at 375, 420, 600, and 768 CSS pixel viewport widths: 12-pixel content padding and chip text, 44-pixel favorite targets, heading/SVG size differences below 0.05 CSS pixels, no content overflow or overlap, and previews filling their cards. Additional checks covered long-title/metadata content at 320 pixels and both themes.
- Native Tab reached the last canonical favorite and revealed its complete target inside the carousel viewport after the focus-selector fix; its tooltip fit. The browser console was clean, and the prepared Index story was restored to Mobile Dark.
- Final focused-only-snap source passed full `bun run lint` and `bun run format.check` (107 files, exit 0), full `bun run typecheck`, and the Svelte autofixer check. All four source hashes remained unchanged through final validation. The index has zero staged entries and zero unmerged entries; the recovery stash remains retained.
- Final native browser round 9 passed 16 tests with two existing Firefox reduced-motion skips in 18.35 seconds, exit 0 (`/tmp/favorite-icon-review/intrinsic-browser-9.log`). All 18 owned focus references were reconciled after fresh review, recorded in `/tmp/favorite-icon-review/accepted-browser-references.json`. Earlier eight-failure and seven-difference runs were diagnostic; incorrectly centered candidates were withheld until their focus defects were corrected.
- The initial full 168-case grid diagnostic run was stopped with exit 143 because tracing caused excessive overhead. The same six native projects then ran with CLI-only `--browser.trace off --maxWorkers 1 --reporter verbose`, finishing in 217.04 seconds with 162 screenshot-mismatch failures and six passing Empty cases, no semantic errors, and the existing 10-second cleanup-timeout warning. No repository screenshot infrastructure was changed.
- Review approved all 168 Home cells: 54 actual images plus two Empty cells per viewport, including every available diff. The 68 actual images retained from the interrupted run were byte-identical to the corresponding reviewed rerun images. The 162 reviewed native actual images were copied to their matching Home reference paths; the temporary acceptance manifest is `/tmp/favorite-icon-review/accepted-home-references.json`. SHA-256 verification found zero mismatches across the 162 accepted reference copies; hashes for all 168 final Home references are recorded in `/tmp/favorite-icon-review/final-home-references.json`. Final normal-comparison evidence follows below.
- Final focused-only-snap source passed all 168 normal Home visual comparisons across 12 project/file combinations in 97.77 seconds, exit 0 (`/tmp/favorite-icon-review/intrinsic-visual-snap-final.log`). The known 10-second cleanup-timeout warning was reported after tests passed. The earlier pre-snap 168-case pass in 99.18 seconds remains historical (`intrinsic-visual-final.log`). The 60 detail references were unchanged.
- Actual source autoplay was reviewed in the prepared browser by temporarily disabling only Storybook preview.css's `animation: none` override. Running records covered more than two natural loops; keyboard focus showed paused clocks, and blur resumed running, including a 233725 ms observation. No exact pause timestamp stability is inferred from reads taken before settling. The stylesheet and default Mobile Dark Index page 6 were restored, with no inline overrides or temporary style and a clean console.
- After artifact reconciliation, `openspec validate --all --strict --no-interactive` passed all 86 current items, and `mix openspec.check` passed for 10 active changes. These current totals supersede the earlier integration snapshot; unrelated lifecycle changes remain owned by their separate work.

Geometry/computed-style checks remain manual; semantic tests use named article/link queries without CSS-class or DOM-depth assertions. The #281 below-fold capture limitation and the distinct backend, full-feature browser, broad-check, rollout, and finalization gates remain open as recorded below. Issue #123 was reconciled and reread with the final scope, base `577c5c34`, 12/17 progress, and current 168/16 validation; it remains Open and In Progress. This completes the bounded card-refinement implementation and validation; it does not establish completion of the full favorites delivery.

## Completed matrix integration status

The latest fast-forward from `caac3ad2edabc20db92c28c6ae4e8c8f61742817` to `577c5c34ed1fa5dbb873f90c99b3e747d2cfb0ee` succeeded. Applying recovery stash `416ebf6fa397b66e182aa9a662f5986083cc4696` with `--index` initially stopped at one `game.stories.ts` conflict and left partial staging. That conflict is now resolved, all unmerged entries are cleared, and the originally empty index is restored. The recovery stash remains retained and the unrelated `delete-user-account` change is outside this delivery.

Tasks 2.3 and 4.2 are complete. `HeaderFocused` and the five meaningful favorites states are retained, while the redundant `FavoriteSavedDark` alias is removed. The affected set contains 38 scenarios and 228 references: 23 public Home scenarios with 138 references, five authenticated Home scenarios with 30 references, and ten detail scenarios with 60 references. Each runs in native light/dark by desktop/tablet/mobile projects. All references now use `<story-path>/<theme>/<viewport>/chromium/`; stale unthemed paths are removed only within these owning story paths. Master styles, mock lifecycle, favorite behavior, dependencies, backend logic, unrelated files, and standalone browser-test paths are preserved. The scoped native reference update and following normal matrix comparison passed all 228 tests. At that integration milestone, ten of fifteen tasks were checked.

The shared capture limitation in [#281](https://github.com/ravecat/d20/issues/281) can clip below-fold content and leave white rows on long pages. Image review must record that limitation; comparison success does not establish below-fold rendering, and no capture fix is included in this task. The two locked-rc5 failures and existing Credo findings remain known delivery gaps, with no patch or skip authorized. The previous three-project evidence below is historical for this integration.

## Completed six-cell integration validation

- Source checks passed: `bun run typecheck` reported zero errors and warnings, lint and formatting passed, all 81 unit tests passed, and focused browser validation passed 30 tests with two intentional Firefox skips. Storybook build and the two layout tests passed. No backend behavior or dependency was changed in this conflict-resolution scope.
- `git diff --check` passed. HEAD and local master both remain at `577c5c34`; the shared Vite configuration, screenshot setup, package manifest, and lockfile have no diff from master.
- Three viewport reviews approved every one of the 228 theme/viewport cells, inspecting 402 distinct baseline, actual, and available diff images in total, with 134 images per viewport. Review retains #281's below-fold limitation and does not claim complete long-page coverage.
- The initial normal matrix run reported 118 passing and 110 failing tests. Failure evidence included 87 expected existing-reference differences, 21 newly created references, and five cold public Home Index timeouts; some tests reported both timeout and screenshot failures, so these cause counts are not disjoint test totals. A warmed Index rerun passed semantic checks in all six cells; all three light comparisons passed and the three dark comparisons retained expected differences. Those dark actuals were pixel-identical to the reviewed candidates.
- The scoped native `--update` run passed all 228 tests across 18 project/file combinations in 555.17 seconds and exited 0 after the existing `close timed out after 10000ms` cleanup warning. Image identity verification confirmed that every one of the 228 updated references exactly matches the reviewed candidate pixels. The subsequent normal run passed all 228 tests across all 18 project/file combinations in 551.05 seconds and exited 0 without a cleanup warning. These runs used the existing six native visual projects without screenshot-infrastructure changes.
- Recovery audit found no missing saved files. All 14 original untracked non-screenshot source files were byte-identical to the stash; only the six owning OpenSpec artifacts were revised. Of the 16 saved tracked non-screenshot files, 13 were byte-identical; `home.svelte`, public `game.stories.ts`, and public `home.stories.ts` contain the intended master integration. All 27 saved screenshot paths are accounted for by their migration. The backup remains available independently of the restored working tree.
- The owning change passes `openspec validate add-game-favorites --strict --no-interactive`. Fresh aggregate `openspec validate --all --strict --no-interactive` passed all 87 items, and `mix openspec.check` passed with 11 active changes. The unrelated account-deletion draft is now valid and remains unchanged by this work.

## Historical integration status at caac3ad2

The worktree was fast-forwarded from `5a28c182` to master `caac3ad2edabc20db92c28c6ae4e8c8f61742817`. All seven text conflicts and the detail-story collision are resolved. Restored feature files, including the stash's untracked paths, are present; the index is clear and the integrated changes remain unstaged/uncommitted. Recovery stash `66e3b9ad12d16444837fe4f3f26b460cde0a3740` is retained.

The integrated detail preserves `playable`, `SessionForm`, `InterestForm`, and socket/session mock setup and cleanup alongside favorite forms and their static descriptors/membership. All four master and six favorites detail stories remain, with complete favorite props, matching actions, and the Session query where applicable. Lazy home/favorites/schema props and ordinary detail validation remain intact without raw-header shortcuts. Persistence and dependencies are unchanged.

Integration task 2.3 and frontend validation 4.2 are complete; ten of fifteen tasks are checked. Backend and frontend results below describe this integration. Earlier results are labeled historical. No archive, completion commit, deployment, or issue closure is recorded.

## Historical integration validation at caac3ad2

- Six focused backend files covering favorites, interests, games, and their three controller boundaries passed 158 of 160 tests. Both failures are the retained rc5 loader assertions: `PageControllerTest` at line 1390 (GET at 1399) and `GameFavoriteControllerTest` at line 288 (GET at 303). They still trace through `collect_once_meta_if_skipped/4` at `deps/inertia/lib/inertia/controller.ex:915`. Interest, ordinary detail/Session visibility, and combined independent interest/favorite projection tests passed. Formatting and scoped Credo passed for four touched backend files.
- Fresh `just check` passed compilation and formatting, then recorded 927 of 929 backend tests passing with those same two adapter failures. It also reported the existing Koala Credo nesting findings at lines 250, 345, and 383, then stopped in `mix ci`; later composed gates were not reached. The broad command is not green and no skip, dependency patch, or workaround masks the failures.
- All 81 frontend unit tests passed. Focused browser checks passed 30 tests with two intentional Firefox skips. Three timeouts in the first parallel run passed on an isolated rerun without source edits.
- A reviewer inspected and approved all 90 baseline/actual/diff images for ten detail states across desktop, tablet, and mobile. The scoped update of 30 references passed, and all new references are pixel-identical to the reviewed candidates. All 117 Home/detail scenarios passed normal comparison across nine project/file combinations in 307.70 seconds. The command exited 0 after the existing `close timed out after 10000ms` cleanup warning.
- Final typecheck reported zero errors or warnings, and lint plus formatting checks passed for 107 frontend files. `bun run storybook:build` passed with 1,182 modules in 4.41 seconds and standard chunk-size/plugin-timing warnings. Svelte autofixer checks found zero issues in the game page and two changed mocks; existing bind/lifecycle suggestions were not changed as part of this integration.

## Pre-integration controller validation and adapter blocker

The user-approved implementation assumes correct standard Inertia lazy-prop behavior. The four-file focused suite passed 132 of 134 tests after an 82-of-82 targeted baseline. Two active integration assertions fail on the locked Elixir rc5 adapter:

- `PageControllerTest`: `home favorites partial reloads skip both catalog loaders` (`page_controller_test.exs:1361`).
- `GameFavoriteControllerTest`: `form redirects refresh confirmed membership without catalog discovery` (`game_favorite_controller_test.exs:288`).

Both failures trace through `collect_once_meta_if_skipped/4` at `deps/inertia/lib/inertia/controller.ex:915`, which invokes the skipped home Games loader and triggers a forbidden BGG request. Normal detail resolution, partial detail, not-found, Session validation, and expired-authentication return tests pass. The assertions remain active; no skip, dependency edit, patch, or workaround masks this blocker.

Formatting and scoped Credo passed for the three changed files: `PageController` and its two affected controller test files. Coordinating source review confirmed removal of the header helper and both shortcut branches, three lazy home props, lazy schema, and ordinary detail validation without a confirmed new defect. No frontend files changed. Task 4.1 remains open until the runtime contract can pass with correct adapter behavior.

## Verification before the latest controller simplification

- Before the latest controller revision, `just check` ran through `CI=true MIX_ENV=test MIX_TEST_PARTITION=_favorites nix develop`. Compilation, Elixir formatting, and all 901 backend tests passed. The command stopped at three pre-existing Credo nesting findings in unchanged `lib/d20/koala_rescue_club/game.ex` lines 250, 345, and 383; the broad command is not green. Scoped Credo over the seven affected files passed.
- Before the latest controller revision, backend checks passed separately: Dialyzer with one existing configured skip, `mix ex_dna lib --max-clones 14` with 10 of 14 allowed clone groups, `mix reach.check --arch --smells`, and `mix openspec.check` with 10 active linked changes. These checks do not waive the broad-check blocker.
- `mix assets.build` passed with the standard chunk-size warning. No dependency change was needed for the form transport or server partial-response handling.
- Unchanged frontend unit validation passed all 79 tests. Browser validation passed 16 tests across Chromium/Firefox with two intentional Firefox skips. Form submission, same-game pending state, shared confirmation, retained focus, same-control retry, guest/expired authentication, loop-copy activation, and unchanged page context are covered.
- Unchanged frontend: all 105 Home/detail Storybook scenarios passed normal comparison across the nine project/file combinations. Six Retry/Error screenshot references were intentionally updated for removal of the separate Retry action; normal comparison passed afterward. The visual runner emitted a cleanup-timeout warning but exited with status 0.
- `bun run typecheck` reported zero TypeScript/Svelte errors or warnings. `bun run lint` and `bun run format.check` passed; all 104 checked frontend files were formatted. The Svelte autofixer found no remaining issue.
- Fresh `bun run storybook:build` passed with exit status 0: 1,180 modules built, Vite build completed in 2.88 seconds, and only the standard warning about chunks above 500 kB was reported.

## Current remaining delivery gates

- Tasks 3.6/4.7: implement and validate the new inherited CSS outline/fill treatment on Home and detail. Record fresh browser, semantic, visual, lint/format, and typecheck evidence before checking these tasks. Prior filled pale/bright references do not validate the new star states.

- Task 4.1 is complete: all 142 focused backend tests and touched-file formatting passed under the revised application acceptance. This does not establish a fresh broad-check pass.
- Task 4.3: the earlier full-feature prepared-page validation remains uncompleted; no full manual-browser success is claimed. For the current card refinement, Home Storybook page 6 on `http://localhost:6006/` is already selected and manual checks belong to task 4.6 without another page-selection question. Automated browser checks do not replace the remaining full-feature gate.
- Task 4.4: the migration was already reversed and reapplied on the disposable test database as recorded below. The latest broad run passed 930/933 backend tests under the former acceptance and reported the separate stage-constraint assertion and three unchanged Credo findings. The two obsolete loader expectations no longer define acceptance; a fresh broad pass remains unestablished. No unrelated game code or shared development data was changed to clear the gate.
- Task 4.5: target-environment migration/release and save/remove/reload/guest checks are unapproved and unperformed. Application rollback retains favorite rows; dropping the table is not routine rollback.
- Task 5.1: authoritative specification synchronization, archive, semantic completion commit, and issue completion remain pending. The change stays active and the issue stays open and In Progress.

## Historical validation before the Inertia form simplification

- Baseline: 103 focused backend tests and 16 existing game-page unit tests passed before implementation.
- Persistence, identity, projections, controller: 124 focused backend tests passed with `MIX_TEST_PARTITION=_favorites mix test test/d20/games/favorites_test.exs test/d20/games_test.exs test/d20_web/controllers/game_favorite_controller_test.exs test/d20_web/controllers/page_controller_test.exs`. Coverage includes separate-connection concurrent adds, private batched membership, constraints and account cascade, numeric/stale routes, matching-Session visibility, real CSRF rejection and current-cookie success, provider-independent removal, handled PostgreSQL cancellation, and membership-read failure semantics.
- Touched Elixir files were formatted. Scoped Credo inspected 11 files without findings.
- Migration `20260910105219` was rolled back and reapplied on the isolated disposable `d20_test_favorites` database, then the 124 focused tests passed again. Shared development data was not reset.
- Shared state/control tests initially passed 14 checks. An additional Chromium regression reproduced native `disabled` dropping focus after keyboard activation. The control now uses `aria-disabled` and the existing per-game request guard; Enter and Space preserve focus and do not submit a duplicate pending request.
- `bun run test:browser -- tests/shared/components/favorite_button.browser.test.ts tests/pages/home/ui/home.browser.test.ts`: 16 passed, 2 intentional browser-specific skips across Chromium and Firefox. Canonical keyboard order, loop-copy links, reduced-motion behavior, shared controls, retry, and focus retention are covered. Home references were intentionally updated for the added stars and then compared normally.
- Final-source `bun run typecheck`, `bun run lint`, and `bun run format.check` passed after the new Storybook scenarios: zero TypeScript/Svelte errors or warnings, ESLint clean, all 106 checked frontend files formatted.
- `bun run test:unit -- tests/pages/game/ui/game.test.ts tests/shared/stores/favorites.test.ts`: 27 passed, including guest prompting, same-page authentication reseeding, independent hero mutations, and an awaited stale response after navigation.
- The focused loop-copy pointer scenario passed in Chromium and Firefox after extending the existing Home interaction test: natural CSS motion exposes the copy, its star saves all copies, and the Inertia visit count remains unchanged.
- Public Home Storybook passed all 72 scenarios across desktop/tablet/mobile. The full frontend normal-comparison run also passed all 105 Home/detail stories (72 public Home, 15 authenticated Home, 18 detail). Representative dark desktop, mobile fallback, long-title, saved, pending, and error references were inspected.
- Storybook now excludes the existing Svelte form packages from dependency prebundling and includes their CommonJS `jsonpointer` dependency. The new real GamePage stories exposed these import failures; the focused configuration change allows those production components to render without introducing a new dependency.
- Independent initial and final read-only reviews found no confirmed persistence, authorization, identity, request-state, page-lifecycle, or UI integration defect.
- Locked-toolchain `CI=true MIX_ENV=test MIX_TEST_PARTITION=_favorites nix develop --command ...` checks passed: Dialyzer (one existing configured skip), `mix ex_dna lib --max-clones 14` (11/14), `mix reach.check --arch --smells`, and `mix openspec.check` (10 active linked changes, including strict validation of all artifacts).

## Historical broad validation before the form simplification

- Full `bun run test` ran 361 cases: initially 350 passed, 9 failed, 2 intentionally skipped. Five Home focus references still represented the earlier overlapping metadata layout; these were inspected and updated to the corrected layout. The Firefox control test needed an explicit user-clicked starting point before its initial Tab, independent of prior iframe focus. The remaining three failures were 8-pixel differences in the unchanged `Player Count Label / Maximum Only` references; an isolated scenario and then all 12 player-count stories across three viewports passed without edits or reference updates. Final focused normal comparisons are recorded below. The full command has not been reported as green. Storybook build passed; no runtime transports were used in stories.
- `just check` was run through the locked Nix environment. Compilation, formatting, and all 893 backend tests passed; the command stopped at three pre-existing Credo nesting findings in unchanged `lib/d20/koala_rescue_club/game.ex` lines 250, 345, and 383. The remaining backend checks, frontend formatting/lint/typecheck, and Storybook build passed separately; full frontend results and successful focused reruns are recorded above. No unrelated game code was changed.
- Actual changed-worktree DevTools validation requires choosing one prepared D20 page. Both `http://localhost:5000/` and `http://localhost:5000/games/356080` matched; a selection question is pending. No prepared page has been changed and no manual-browser success is claimed. Headless tests above exercise the worktree sources but do not substitute for this gate.
- Apply the additive migration before the matching target-environment release and verify save/remove/reload and guest behavior there. No deployment is authorized or recorded. Application rollback retains favorite rows; dropping the table is not routine rollback.
- Reconcile issue/specification acceptance, synchronize and archive only after the remaining delivery tasks pass. The issue stays open and In Progress.

## Integration ownership

Game interest #275 and the updated detail/activation behavior were integrated in `caac3ad2edabc20db92c28c6ae4e8c8f61742817`; current master `577c5c34ed1fa5dbb873f90c99b3e747d2cfb0ee` additionally owns the shared theme matrix and latest detail styles. Favorites remains owned by issue #123 and this active change. This revision resolves the restored story conflict and reconciles its references locally while retaining recovery stash `416ebf6fa397b66e182aa9a662f5986083cc4696`. It does not import an unpublished sibling branch, create a merge commit, alter persistence/backend behavior, or touch the unrelated account-deletion change.

## Historical final focused verification

After the final test setup and reference corrections, normal comparison passed 16 browser checks across Chromium/Firefox (2 intentional skips), all 27 game-page/shared-state unit tests passed, and typecheck, ESLint, and frontend formatting passed. All 105 Home/detail Storybook scenarios passed normal comparison in the full frontend run. The unrelated player-count group passed all 12 focused comparisons without changes. Strict OpenSpec validation passed all 83 items. Task 4.4 remains open because the broad command is not fully green; tasks 4.3, 4.5, and 5.1 remain open for prepared-page validation, rollout, and final reconciliation. These results preceded the form simplification and do not establish a completion commit. That historical revision used base `5a28c182`; subsequent integrations are recorded separately above.

## Responsive Home title fitting - 2026-09-11

Issue #123 and tasks 3.7/4.8 own this continuation. The product diff against the pre-task copy is confined to title CSS in `assets/js/pages/home/ui/home.svelte`: smaller compact/Playable type with `min(1em, max(0.75rem, 6cqi))`, four lines normally and three below `12rem`, long-word wrapping, and inherited hero font size. No markup, geometry, metadata, favorite implementation, JavaScript or dependency change is part of this title amendment. The separately running outlined-star amendment shares this worktree and screenshot surfaces.

- `bun run oxfmt --check js/pages/home/ui/home.svelte` and `bun run eslint js/pages/home/ui/home.svelte` passed from `assets/`.
- `bun run typecheck` passed, with zero Svelte errors or warnings.
- `openspec validate --all --strict --no-interactive` passed all 86 items.
- DevTools reproduced the original two-line truncation on prepared page 5, `http://localhost:6006/?path=/story/home--compact-metadata&globals=theme:dark`. A temporary CSS prototype showed the complete 67-character title in four lines at the supplied width and bounded three-line truncation at 320px. All prototype styles and temporary frame sizing/theme changes were removed. The prepared page subsequently received an externally opened login dialog and changed route during parallel star validation; this task left that state intact and continued with native isolated screenshots.
- A fresh native dark desktop CompactMetadata test passed semantic assertions and produced the expected visual mismatch: complete four-line title, unchanged footer/card bounds, and the separately owned star paint. Reviewer inspection of reference/actual/diff found no unintended title changes.
- Initial parallel browser/visual commands failed before running tests because Vitest browser sessions did not connect before the timeout. A multi-project retry also stalled and was terminated. These are not passing runs. Single-project execution works; fresh candidates are generated separately through the native screenshotDirectory option, reviewed before baseline promotion, and then checked with normal comparison.

Tasks 3.7/4.8 are verified. Final evidence:

- Each of the six `visual-<light|dark>-<desktop|tablet|mobile>` projects passed all 28 Home cases through `bun run test --project <project> stories/pages/public/home.stories.ts stories/pages/authenticated/home.stories.ts`: 168 normal comparisons passed after reference review.
- Reviewed all 168 Home and 18 browser candidate/reference/difference images. Approved candidate hashes were checked before copying only changed references. The 175 promoted references also reflect the separately owned outlined-star change; every one of the 186 current references exactly matches its reviewed candidate. No screenshot configuration or test source was edited.
- `bun run test:browser -- tests/pages/home/ui/home.browser.test.ts tests/shared/components/favorite_button.browser.test.ts --testTimeout=60000` passed 18 tests with two existing Firefox skips. Under concurrent load, two 32-link Chromium cases had exceeded the original 15-second test timeout; both passed the targeted retry and the final full scoped run with the command-local 60-second allowance. Assertions and repository timeout configuration are unchanged.
- Some successful visual runs reported the existing 10-second process-cleanup timeout after all tests passed, then exited 0. Initial startup failures and the terminated multi-project retry remain diagnostic failures, not acceptance evidence.
- Source hashes for Home, the shared favorite, covered stories/browser tests and Vite configuration remained stable through candidate generation and final normal comparisons. Independent review found no title defects: the desktop/tablet example fits four lines, the mobile example gains a third line, and card bounds, footers, focus and overlays remain intact.

Temporary captures and logs are retained under `/tmp/d20-title-checks/`; generated candidate directories were moved out of the repository after promotion. The existing #281 below-fold screenshot limitation and all whole-feature gates remain open. This title verification does not close the separately owned star task 4.7, deploy, archive or create a completion commit.

## 2026-09-12: Home widgets and isolated stories, unstaged review

The latest user instruction requires a behavior-preserving extraction of the current worktree, with every change unstaged and no commits. Home now composes GameCollection twice and retains one shared favorites feedback state. GameCollection owns section headings, empty/singleton handling, lane partition/copies and carousel CSS. GameCard owns the previous card markup and CSS. Existing favorite forms, metadata, title fitting, geometry, links, focus, pointer-usable copies and reduced motion are preserved. No backend, dependency, runtime configuration or game-detail source changed in this amendment.

Home decreased from 613 to 69 lines. Public Home stories decreased from 391 to 202 lines and from 23 to 9 scenarios. The retained page scenarios cover composition, empty/conditional sections, OverlappingSingletons and authentication. Authenticated Home retains its five cross-collection favorites scenarios. Twelve GameCollection stories own collection sizes and explicit expected hero partitions; eight GameCard stories own existing title, image, thumbnail, fallback, metadata and status cases. The small story frame exercises 152px compact cards at the native mobile width and 256px at tablet/desktop.

Validation against the starting uncommitted state:

- Scoped ESLint and oxfmt passed for Home, the new widget module, shortened/new stories and their frame decorator. `bun run typecheck` reported zero errors and warnings. Svelte autofixer passed for all four Svelte components.
- `bun run test:browser -- tests/pages/home/ui/home.browser.test.ts tests/shared/components/favorite_button.browser.test.ts` passed 18 tests with the same two existing Firefox skips. Existing browser references were unchanged.
- Each native visual project ran the two Home files and two new widget files. All 84 retained page comparisons passed without reference updates. Each project initially reported 20 missing new widget references, created by the native runner; these were reviewed before normal comparison.
- All 168 prior Home reference cells and all 120 new widget candidates were visually inspected using full-capture/contact-sheet review and full-size detail checks. The 84 superseded public Home references were removed. The final allocation is 54 public Home, 30 authenticated Home and 120 widget references, totaling 204. No existing retained page, game-detail or browser reference changed during this extraction.
- Normal comparison then passed all 120 widget cases using the six exact `visual-<theme>-<viewport>` projects sequentially with `bun run vitest run --project <project> stories/widgets/game-card.stories.ts stories/widgets/game-collection.stories.ts`, without an update flag. A command-local 180-second process limit was not reached. Every project and the enclosing loop exited 0. Combined with the 84 unchanged page comparisons, all 204 current cells passed.
- Two parallel six-project attempts were stopped after several minutes without observable completed tests. Processes remained active; no root cause or product regression was established. Exact-project runs completed normally. Vitest printed its existing shutdown warning after successful test summaries; the final exact-project loop still exited 0.
- `bun run storybook:build` passed with the standard chunk-size warning. `mix openspec.check` passed for 10 active changes. Strict OpenSpec and diff/index checks accompany final reconciliation.

Manual prepared-page limitation: Chrome was initially disconnected. After reconnection, the open Home story at localhost:6007 contained styles from the primary checkout (`/home/max/apps/d20/assets/...`), while the current worktree Storybook process serves localhost:6008. No prepared current-worktree tab was available. No page was navigated and no unrelated application state was changed. Task 6.3 remains open only for this prepared-page inspection; automated extraction checks are complete. The existing whole-feature browser, broader acceptance, star/detail, rollout and finalization gates remain open. No archive or whole-feature completion is claimed.

The existing #281 below-fold capture limitation remains for retained page stories. Widget reference content fits within the reviewed capture area; this amendment does not change screenshot infrastructure. Source/content checks against `/tmp/d20-home-widgets/before` confirmed that unrelated pre-existing files retained their bytes. The previously staged router hunk was unstaged without content changes, as explicitly requested. HEAD remains 577c5c34 and the index is empty. All extraction edits, new references and removals remain available for review. Local run logs and the starting-state backup are under `/tmp/d20-home-widgets/`.

## 2026-09-12: Initial title/contour correction (star contour superseded)

The screenshot follow-up is implemented under tasks 7.1-7.2 in the same uncommitted worktree. Only `game-card.svelte` and `favorite_button.svelte` production CSS changes: title line height increases from 1.1 to 1.2, a compact post-clipping drop shadow replaces the broad clipped text shadow, and four zero-blur half-pixel dark shadows outline the unchanged gold SVG. Existing line clamps, font sizes, footer layout, button paint/state and forms remain intact.

Strict OpenSpec validation passed all 86 items and `mix openspec.check` passed for 10 active changes. Scoped oxfmt, ESLint, TypeScript/Svelte checks and the Svelte autofixer passed (no issues; two existing FavoriteButton effect/binding advisory suggestions left unchanged). Native initial comparisons captured 264 Storybook cells across five existing page/widget files and six projects: 246 expected visual mismatches and 18 unchanged passes. Baseline/actual/diff review covers the title/star changes, narrow footer clearance, full long hero titles, saved/empty stars, artwork/fallbacks, focus and feedback; no unrelated visual changes were accepted. Normal comparisons after reviewed updates remain pending at this checkpoint.

DevTools inspected the existing prepared `http://localhost:6008/?path=/story/home--sign-up&globals=theme:light` page. Its Vite style paths identify this worktree. Hot reload applies the compact title filter and star contour; the measured favorite target remains 44 by 44 CSS pixels, gold stroke and transparent unsaved fill remain inherited. The route, focused Qwinto control and prepared state were preserved. This focused inspection does not close the broader natural-motion, multi-viewport manual, rollout or finalization gates.

Parallel validation attempts slowed or stalled, so the final acceptance runs use one Vitest process at a time. One browser candidate run under parallel load reported two timeouts; this is not claimed as passing. Temporary candidate capture destinations were moved outside the repository after the native screenshot path resolver nested an absolute directory under the asset root. No screenshot infrastructure or test configuration changed. Original dirty browser references are preserved; reviewed candidate images are stored under `/tmp/d20-card-contrast/`. The known below-fold capture limitation #281 remains. A dark edge improves separation from varied artwork but does not establish a contrast ratio against every possible background.

## 2026-09-12: Flat icon target and CSS hover preview

The user superseded the enlarged card-favorite target, tooltip and dimensional dark outline. The final shared component removes tooltip state, handlers, anchoring/popover code, wrapper and styles; keeps a native button exactly matching its unchanged SVG box; and removes all SVG shadows. CSS fills icon-only favorites on hover-capable pointer hover or focus-visible, returning to server-confirmed paint afterwards. The detail button retains its existing dimensions and labeled presentation. The title line-height/drop-shadow correction remains. Two isolated GameCard hover/focus stories cover the new preview without changing aria-pressed; the existing favorite browser test now verifies absent tooltip and keyboard activation.

Scoped formatting/lint, TypeScript/Svelte, autofixer and strict OpenSpec validation passed (86 items). DevTools on the user's existing localhost:6008 page confirmed identical button/SVG bounds at 20.352 by 20.352 CSS pixels, absent tooltip/native title, and that the former extra hit area resolves to the card link. On the subsequently prepared dark Home Index, hover changes SVG fill from none to gold and pointer leave restores none while aria-pressed stays false and aria-busy stays false. Routes, controls and unsaved Storybook arguments were preserved.

Prior native normal comparisons passed 176 cells before the user changed the star design; those results are historical. The superseded intermediate contour references were restored from the pre-amendment backup, preserving the user's existing work. Final current-source native captures and reviewed reference reconciliation are in progress under tasks 7.2/8.2; evidence lives in /tmp/d20-flat-favorite. No commit, staging, test-config change or feature archive is authorized.

## 2026-09-12: First-line alignment and card states; visual work stopped by user

GameCard now shares its existing title font size and 1.2 line-height through local CSS values. The icon wrapper uses the title line-height for its inherited font size, making the native button and SVG square match the first title line without changing text typography or computed card padding. Manual Chrome DevTools review on the existing dark Home Index at `http://localhost:6008/?path=/story/home--index&globals=theme:dark` confirmed compact, hero, and wrapped compact title alignment. At the prepared 697 by 768 CSS-pixel preview, compact line height was 18.3263px and button/SVG height 18.3182px; hero values were 31.2773px and 31.2727px. Center differences were below 0.005px, reflecting layout rounding. The route, authentication and Storybook arguments were preserved; the console had no errors. Independent source review found no typography, padding, footer or detail regression.

Added isolated FavoriteHovered, FavoriteSaved and HeroFavoriteSaved stories, with common semantic assertions for accessible Add/Remove action names and confirmed pressed state. Existing FavoriteFocused remains. Scoped oxfmt/ESLint and TypeScript/Svelte checks passed, with zero errors or warnings. `openspec validate add-game-favorites --strict --no-interactive` and `git diff --check` passed. The focused `bun run test:browser -- tests/shared/components/favorite_button.browser.test.ts` passed all eight Chromium/Firefox cases after reviewing the eight newly generated rest/hover/pointer-left/focus references, scoped update and normal rerun. These browser fixtures provide behavioral coverage with their existing limited global styling; they do not replace themed Storybook presentation checks.

The first nine-story native visual run completed with 54 failed comparisons: expected old-reference differences, missing references and capture-stability timeouts under concurrent memory pressure. Available baseline/actual/diff images were reviewed. Scoped updates selected only the 42 cells with reviewed actual candidates; the first five project updates passed and the final project was interrupted when the user explicitly stopped visual work. These partial updates are not a complete passing visual matrix. No new saved/hover Storybook captures or comparisons were completed. Logs and selection records remain in `/tmp/d20-first-line-alignment/`.

The latest user instruction explicitly waives Storybook execution and screenshot checking for this task and replaces the large-count story matrix with concise composition examples. The in-flight update process was terminated; no further story/screenshot runs or image reviews are part of acceptance. Tasks 9.2/9.4 are reconciled against observed source/manual evidence and this waiver, not a claimed visual pass. Prior whole-feature manual, cross-stack, rollout and lifecycle gates remain open. All work stays unstaged and uncommitted.

## BGG Hot collection attribution - 2026-09-12

The bounded task 11 amendment labels the hidden Home h1 and visible provider heading `Hot (by BGG)`. The visible `by BGG` is a native underlined anchor to `https://boardgamegeek.com/hotness`; the hidden h1 contains no link. Home owns the destination, with the homepage as the specification's alternative if no dedicated route is known. The reusable collection takes optional attribution with a required href and no BGG-specific or network fallback logic. Playable and empty-provider section behavior are preserved. Parallel lane/card/story edits remain owned by their existing tasks.

URL research corroborated `/hotness` through https://asgs.sm/the-hotness-top-50-trending-games-today-bgg/. Direct automated BGG Hotness and homepage fetches returned 403, so external live availability is unverified. This is not evidence of a missing route.

- `bun run test:browser -- tests/pages/home/ui/home.browser.test.ts -t 'attributes Hot to BGG' --browser.screenshotFailures=false`: 2 passed across Chromium and Firefox, with 12 unrelated cases filtered out. Verified exact h1/h2/region text, a single visible correct provider link, no link in the hidden heading or Playable, keyboard focus advancing to the first game, and empty-provider omission.
- The initial targeted run detected Svelte's whitespace trimming (`Hot(by BGG)`); explicit space text fixed the source and the repeat passed. The initial diagnostic failure capture was removed; no screenshot reference was updated.
- Source review found and corrected a nested metadata-list query that threw in Home stories and a stale `Games` region query in the shared favorite browser test. Story semantics now distinguish provider and game links; keyboard assertions account for the attribution stop.
- `openspec validate --all --strict --no-interactive`: 86 passed, zero failed. `mix openspec.check`: passed for 10 active changes.
- Issue #123 scope was extended and re-read through `gh`; it remains In Progress.

No Storybook matrix or screenshot/reference comparison was run for this amendment. The prepared localhost:6008 page was an isolated Two Browse Games widget, not the changed Home composition; its route and state were left intact. Actual Home rendered/reference review remains part of the existing open visual delivery gates. Static and focused semantic passes do not establish visual acceptance or complete the wider favorites delivery. All work remains unstaged with no commit or archive.

## 2026-09-12: Two compact Playable rows and concise stories

GameCollection now derives and renders ordered lanes through one keyed loop. One through six Playable entries stay in one compact lane; more than six split at ceil(n / 2), so seven yields 4/3 and eight yields 4/4. Both rows use compact GameCard styling and retain the shared CSS cycle, direction, section hover/focus pause and reduced-motion rules. Each lane owns its own canonical count, duplicate sequence and wrap. The existing browse hero-quarter/compact-rest split and list names are retained. Server selection still returns at most eight Playable games; no backend change is needed.

Collection stories retain Games counts 0-4 and Playable 1/2/4/7. Seven is the minimum example of the new two-row branch. Public Home, authenticated Home and collection defaults now use four browse games; shared larger fixtures remain for independent browser tests. Five high-count stories and exactly 30 directly orphaned PNG references were removed. The previously added FavoriteHovered, FavoriteSaved, HeroFavoriteSaved and FavoriteFocused card stories remain. No further stories, screenshot captures, retained reference updates or image reviews were run after the user's waiver. Existing retained references are not reported as current or passing.

Passed checks:

- Scoped `bunx eslint` and `bunx oxfmt --check` on GameCollection, collection stories, both Home story files and the new focused test.
- `bun run typecheck`: zero TypeScript/Svelte errors and warnings.
- `bun run test:browser -- tests/widgets/game-collection/ui/game-collection.browser.test.ts`: all six cases passed, covering 6/7/8 membership and row order plus native keyboard traversal through canonical links and favorite buttons in Chromium and Firefox. The file imports production CSS and contains no screenshot, geometry, styling or animation assertions. This focused boundary test is justified by the explicit Storybook-execution waiver.
- Independent source review found no regression in row partition, favorite bindings, duplicate cutoff, browse composition or shared CSS pause/focus rules.
- `openspec validate add-game-favorites --strict --no-interactive` and `mix openspec.check` passed; the lifecycle check reported 10 active changes.

The actual rendered two-row composition and natural animation were not visually verified, per the user's instruction. Concurrent Hot heading/attribution edits were preserved and are not claimed as part of this row refinement. Tasks 9 and 10 use the updated acceptance; separate whole-feature delivery gates remain open. Changes are unstaged and uncommitted.

Final task 11 checks:

- `bunx oxfmt --check js/pages/home/ui/home.svelte js/widgets/game-collection/ui/game-collection.svelte stories/pages/public/home.stories.ts tests/pages/home/ui/home.browser.test.ts tests/shared/components/favorite_button.browser.test.ts` and `bunx eslint` with those same five paths: passed.
- `bun run typecheck`: passed, zero errors and warnings. Final spacing uses the ordinary HTML space entity `&#32;` after ESLint rejected literal mustache whitespace; the final-source attribution test again passed both browsers (2 passed, 12 unrelated filtered cases).
- `bun run test:browser -- tests/shared/components/favorite_button.browser.test.ts -t 'shares pending state' --browser.screenshotFailures=false`: 2 passed across Chromium and Firefox, 6 unrelated cases filtered out.
- Final bounded source review resolved both findings and found no remaining attribution-specific issues. `git diff --check` passed on the touched tracked source/tests. Repeated owning-change OpenSpec validation and lifecycle check passed.

Tasks 11.1-11.2 are verified for their recorded implementation, semantic/static checks and evidence reconciliation. Existing visual, rollout and finalization tasks remain open.

## Collection-story attribution and Empty removal - 2026-09-13

Task 11.3 aligns all four browse GameCollection examples with Home's `Hot (by BGG)` heading and native `/hotness` link. Each of the four Playable examples explicitly clears inherited attribution. Source review confirms heading/region assertions use the full accessible name, provider-link assertions verify its name/destination separately, and canonical game/lane membership/order assertions remain intact. The existing production widget and Home empty-provider coverage were unchanged.

Removed the standalone Empty export, its empty-only assertion branch and exactly six `Empty-1.png` files under `assets/__screenshots__/stories/widgets/game-collection.stories.ts/{light,dark}/{desktop,tablet,mobile}/chromium/`. Inventory confirms eight retained story exports (Hot 1/2/3/4, Playable 1/2/4/7), four explicit attribution overrides, unchanged retained export IDs and no remaining Empty references.

Checks from assets all passed:

- `bun x --no-install oxfmt --check stories/widgets/game-collection.stories.ts`
- `bun x --no-install eslint stories/widgets/game-collection.stories.ts`
- `bun run typecheck`: zero errors and warnings.

The owning OpenSpec change validates strictly and `mix openspec.check` passes for 10 active changes. Issue #123 scope was extended and verified through gh. No Storybook execution, new screenshot generation, retained-reference updates or rendered comparison occurred; retained visual references are not claimed to match the new heading. This source-only story follow-up preserves the existing visual waiver and broader open delivery gates. Parallel work is preserved and changes remain unstaged without a commit or archive.

## Favorites use game data without runtime Session context

Issue #123 and tasks 12.1/12.2 own this correction in the existing game-favorites worktree. Favorites now exposes `create(user, bgg_id, slug)` and resolves the original source through `Games.fetch_by_slug/1` with the existing identity comparison and source-error normalization. The favorite descriptor is exactly `{bggId, action, slug}`; PUT submits only `slug` and `response_to`. No favorite mutation resolves a runtime Session or applies page visibility. Hidden local data can be saved without a running party. Detail GET visibility/Session validation, the page Session prop, current-query return path, live lobby, authentication/CSRF, persistence, and source-independent DELETE remain unchanged. No migration, dependency, route or AsyncAPI change is involved. Matching backend/client code can be reverted without touching saved rows.

Verification:

- Before edits, the three favorites context/controller/page-controller suites passed 97 tests and the existing game-page unit suite passed 19 tests. The old hidden-source/matching-Session tests characterized the dependency being removed.
- `env -u BASH_ENV MIX_ENV=test mix test test/d20/games/favorites_test.exs test/d20/games_test.exs test/d20_web/controllers/game_favorite_controller_test.exs test/d20_web/controllers/page_controller_test.exs` passed 142 tests, exit 0. The first run exposed one remaining obsolete partial-header visibility assertion; it now verifies unchanged stale-BGG-identity rejection, and the complete focused suite was rerun successfully. Hidden local and provider source saves, ignored extraneous missing/malformed Session fields, unchanged malformed source/identity/authentication/storage checks, ordinary detail validation and session-free favorite projection pass. Source review confirms the HTTP adapter never forwards Session input and Favorites never calls the Session resolver.
- `bun run test:unit -- tests/pages/game/ui/game.test.ts` passed 19 tests. The existing lobby test now explicitly sets `/games/qwinto?session=session-a`, verifies the exact PUT body contains only `slug` and `response_to`, and confirms the attached lobby remains intact.
- `bun run test:browser -- tests/shared/components/favorite_button.browser.test.ts -t 'shares pending|keeps confirmed|displays server' --browser.screenshotFailures=false` passed 6 tests across Chromium and Firefox; two screenshot-bearing cases were excluded by the explicit name filter. Pending synchronization, focus, confirmed membership, retry and identity error handling pass. No screenshots or references were generated or updated.
- Scoped `mix format --check-formatted` for the six touched Elixir files, Oxfmt/ESLint for the four touched frontend files, and `bun run typecheck` passed; Svelte reports zero errors and warnings. `git diff --check` passed.
- `openspec validate --all --strict --no-interactive` passed all 86 items; `mix openspec.check` passed for 10 active changes.
- Using devtools-validations, the existing worktree Storybook page at `http://localhost:6007/?path=/story/widgets-game-collection--one-browse-game` was inspected without navigation or state changes. Its existing iframe points to authenticated Home. Vite style provenance confirms this worktree; the rendered `/favorites/1009` form has only `slug` and `response_to` inputs. This limited read-only inspection does not replace the full-feature manual/visual gates or claim a manual WithSession submission.

Evidence: `/tmp/d20-favorites-session-backend-final.log`, `-unit.log`, `-browser.log`, `-typecheck.log`, `-frontend-format.log`, `-lint.log`, `-openspec.log`, `-lifecycle.log`; the incremental product patch is `/tmp/d20-favorites-session.diff`. All paths use the `/tmp/d20-favorites-session` prefix. Issue #123 remains In Progress and the owning change remains active. No staging, commit or archive is performed; the pre-existing staged router hunk and all unrelated work are preserved. Broader visual, rollout and finalization tasks remain open.

Broad-check result for this correction: `env -u BASH_ENV MIX_ENV=test CI=true nix develop --command just check` passed compilation with warnings-as-errors, formatting and all 936 backend tests, then stopped at the same three pre-existing Credo nesting findings in unchanged `lib/d20/koala_rescue_club/game.ex` lines 250, 345 and 383 (exit 8). Later recipe stages were not executed by this command; the focused frontend and lifecycle checks above were run separately. The plain `just check` attempt could not start because `just` was absent from the host PATH. An initial Nix run encountered mixed local/Nix dependency compilation; the complete rerun used Nix without concurrent Mix commands and reached the existing Credo gate. Final evidence: `/tmp/d20-favorites-session-check-final.log`. No dependency or toolchain configuration was changed. Strict validation after task reconciliation again passed all 86 items (`/tmp/d20-favorites-session-openspec-final.log`).

## Inline favorite source resolution

The same task 12 follow-up removes the private `resolve_game/1` wrapper from `favorites.ex`. `create/3` checks the slug and directly matches `{:ok, %{bgg_id: ^bgg_id}}` from `Games.fetch_by_slug/1` in `with`. Its `else` distinguishes successful resolution of a different identity (`:game_identity_changed`) from invalid or unresolvable input (`:game_not_found`). The authentication clause and persistence body, including scoped storage error handling, are unchanged. No Session, frontend, persistence or public route changes are added by this simplification.

`env -u BASH_ENV MIX_ENV=test nix develop --command mix test test/d20/games/favorites_test.exs test/d20_web/controllers/game_favorite_controller_test.exs` passed all 26 tests in 0.8 seconds, exit 0 (`/tmp/d20-favorites-inline-tests.log`). Existing tests cover malformed input before lookup, provider resolution failure, hidden-game data, stale identity, ownership, idempotency and handled storage cancellation. No new tests were needed. Nix touched-file formatting and `git diff --check` passed. Earlier page, frontend and full-backend evidence above remains applicable; no unrelated suite or visual work is rerun for this one-file refactor. Tasks 12.1/12.2 remain verified and broader gates remain open. Issue #123 owns this continuation; no staging, commit or archive is performed.

After reconciliation, strict OpenSpec validation passed all 86 items and the Nix lifecycle check passed for 10 active changes; final diff whitespace validation passed. Evidence: `/tmp/d20-favorites-inline-openspec.log` and `/tmp/d20-favorites-inline-lifecycle.log`.

## Provider-neutral favorites list API

The same task 12 naming cleanup renames `Favorites.list_bgg_ids/1` to `Favorites.list/1` in its definition, typespec, both PageController callers and existing favorites context/controller assertions. The account-scoped query, sorting, integer result and `nil` returning `[]` are unchanged; no compatibility alias remains. The read API documentation now describes saved game identities without naming a provider. The design, delta specification and tasks reflect the same existing delivery outcome.

`env -u BASH_ENV MIX_ENV=test nix develop --command mix test test/d20/games/favorites_test.exs test/d20_web/controllers/game_favorite_controller_test.exs test/d20_web/controllers/page_controller_test.exs` passed all 97 tests in 2.3 seconds, exit 0 (`/tmp/d20-favorites-list-tests.log`). Existing coverage verifies private membership, guest results, persistence and Home/detail projections. The first formatting check requested collapsing one now-shorter PageController assignment; native formatting applied only that change and the final four-file formatting check passed (`/tmp/d20-favorites-list-format-final.log`). Source/test/frontend reference scanning found no old API references. No behavior, frontend, schema or dependency changes, new tests, staging, commits or archive are included. Earlier frontend and broad validation remains historical evidence for unchanged behavior; broader delivery gates stay open. Issue #123 and tasks 12.1/12.2 remain the owning records (33/41).

Final strict OpenSpec validation passed all 86 items and lifecycle validation passed for 10 active changes; `git diff --check` passed. Logs: `/tmp/d20-favorites-list-openspec.log` and `/tmp/d20-favorites-list-lifecycle.log`.

## Return Favorite entities from the context

Task 12 now makes `Favorites.list/1` return `[Favorite.t()]`: all persisted favorites for the caller, with the existing account filter and `Repo.all/1`, without `order_by` or ID-only `select`. Nil still returns `[]`; no ordering is promised. This supersedes the preceding sorted-integer list result. Both PageController lazy favorites props map the entities to `bgg_id` inline, preserving the existing private numeric membership payload and avoiding exposing entity fields. No additional context API or projection helper was added.

Existing context/controller assertions now match Favorite entities. The private-list case verifies two full persisted structs for one user using an order-independent comparison, exclusion of another user's record and an empty guest result. Existing page-controller assertions still require numeric membership. `env -u BASH_ENV MIX_ENV=test nix develop --command mix test test/d20/games/favorites_test.exs test/d20_web/controllers/game_favorite_controller_test.exs test/d20_web/controllers/page_controller_test.exs` passed all 97 tests in 2.4 seconds, exit 0 (`/tmp/d20-favorites-entities-tests.log`). Native formatting passed for all four touched Elixir files. The incremental patch is `/tmp/d20-favorites-entities.diff`. No frontend, schema, dependency, runtime-session or write-semantics changes are involved. Broader delivery gates remain open; task 12 is verified at 33/41 overall, without staging, commits or archive.

Final strict OpenSpec validation passed all 86 items, lifecycle validation passed for 10 active changes, and diff whitespace validation passed. Evidence: `/tmp/d20-favorites-entities-openspec.log`, `/tmp/d20-favorites-entities-lifecycle.log`, and `/tmp/d20-favorites-entities-format-final.log`.

## Trust the caller's slug contract

The user explicitly removes defensive slug validation from this same task 12 correction. `Favorites.create/3` now declares `String.t()` and directly calls `Games.fetch_by_slug/1` in its `with`, without checking binary type or emptiness first. Expected lookup error tuples still become `:game_not_found`, successful mismatched identity remains `:game_identity_changed`, and authentication/persistence handling is unchanged. Invalid caller arguments are no longer a friendly Favorites error contract; no guard, validator, plug or rescue was introduced elsewhere. Exactly two obsolete tests promising prelookup rejection of missing/malformed inputs were removed. Existing actual provider-failure, identity, ownership and storage cases remain.

`env -u BASH_ENV MIX_ENV=test nix develop --command mix test test/d20/games/favorites_test.exs test/d20_web/controllers/game_favorite_controller_test.exs` passed all 24 retained tests in 0.8 seconds, exit 0 (`/tmp/d20-favorites-slug-tests.log`). Native formatting passed for the three touched Elixir files. The incremental patch is `/tmp/d20-favorites-slug.diff`. Active proposal/design/spec/task wording now reflects caller-owned valid string inputs; historical validation above retains the superseded contract for provenance. No frontend, Games resolver, schema, dependency or runtime-session changes were added. Tasks 12.1/12.2 are verified (33/41), broader delivery gates remain open, and no staging, commit or archive is performed.

Final strict OpenSpec validation passed all 86 items and lifecycle validation passed for 10 active changes; formatting and diff whitespace checks passed. Logs: `/tmp/d20-favorites-slug-openspec.log`, `/tmp/d20-favorites-slug-lifecycle.log`, `/tmp/d20-favorites-slug-format-final.log`.

## Direct Repo calls and standard infrastructure errors

Task 12 removes both Favorites try/rescue blocks, the transient PostgreSQL error allowlist and the unused controller unavailable mapping. Create and delete call Repo directly; infrastructure exceptions propagate to the existing Phoenix endpoint error boundary. Successful :ok responses, returned changeset errors, authentication, source/identity checks, ownership and idempotency remain unchanged. This supersedes the historical storage-cancellation normalization described above. No replacement exception handler, retry or logging layer was introduced.

The existing PostgreSQL cancellation test now verifies that both PUT and DELETE produce the framework 500 response using assert_error_sent. The helper observes the response and catches the reraised exception; it does not generate the response. The existing game-page failed-removal unit fixture now uses its network-error responder and retains retry feedback and confirmed membership assertions. The shared form already handles HTTP exceptions with the same generic retry feedback; its production code is unchanged.

`env -u BASH_ENV MIX_ENV=test nix develop --command mix test test/d20/games/favorites_test.exs test/d20_web/controllers/game_favorite_controller_test.exs` passed all 24 tests in 0.7 seconds. From assets, `bun run test:unit -- tests/pages/game/ui/game.test.ts` passed all 19 tests. Scoped Oxfmt and ESLint checks passed. Evidence: `/tmp/d20-favorites-repo-tests.log`, `/tmp/d20-favorites-repo-unit.log`, `/tmp/d20-favorites-repo-unit-format.log`, and `/tmp/d20-favorites-repo-unit-lint.log`. Earlier validation remains applicable to unchanged page/session/frontend behavior. Tasks 12.1/12.2 are verified at 33/41 overall; broader visual, rollout and finalization gates remain open. No staging, commit or archive is performed; the existing staged router hunk is preserved.

Final touched-file Mix formatting, strict OpenSpec validation (86 items), lifecycle validation (10 active changes) and diff whitespace checks passed. Evidence: `/tmp/d20-favorites-repo-format-final.log`, `/tmp/d20-favorites-repo-openspec.log`, and `/tmp/d20-favorites-repo-lifecycle.log`.

## Favorite numeric constraint removal - 2026-09-13

Removed Favorite numeric-range validation, the positive CHECK mapping, and CHECK creation in the unpublished initial migration. Existing tests now retain nil/noninteger and missing-account errors, accept 0/-1/2147483648 at changeset validation, persist zero/negative values directly, and still reject duplicate account/game pairs. Integer storage, required fields, account cascade, source identity and mutation semantics remain unchanged. Values outside PostgreSQL integer storage are not promised to persist.

Validation used `env -u BASH_ENV CI=true MIX_ENV=test MIX_TEST_PARTITION=_favorite_constraints_20260913 nix develop --command ...` on a confirmed absent, dedicated database:

- `mix test test/d20/games/favorites_test.exs test/d20_web/controllers/game_favorite_controller_test.exs`: 24 passed after fresh automatic creation/migration.
- SQL catalog inspection confirmed only `game_favorites_pkey` and `game_favorites_user_id_fkey`, without the removed CHECK.
- `mix ecto.rollback --step 1`: reversed only favorites migration 20260910105219; `to_regclass` confirmed the table absent.
- The same targeted test command reapplied the migration and passed all 24 tests again; SQL inspection reconfirmed PK/FK only.
- `mix ecto.drop --force`: removed only the task-created disposable database after verification. Existing development/test databases were not changed.
- `mix format --check-formatted lib/d20/games/favorite.ex priv/repo/migrations/20260910105219_create_game_favorites.exs test/d20/games/favorites_test.exs`: passed in the Nix test environment.
- `openspec validate --all --strict --no-interactive`: 86 passed, 0 failed. `mix openspec.check`: passed for 10 active changes.

Tasks 13.1/13.2 are verified, 35/43 overall. The edited initial migration does not retroactively remove a CHECK in an already-migrated environment; any such target requires reconciliation before the still-open rollout gate. No frontend checks are required by these three backend/persistence file edits. Broader visual, rollout and finalization gates remain open; no staging, commit or archive.

## Favorites notifications deferred - 2026-09-13

Removed `FavoriteFeedback`, the `favorites_feedback.svelte` renderer, Home/detail feedback state, all widget/card feedback props and bindings, result IDs and custom success/error/HTTP/network notification callbacks. FavoriteButton uses the existing Form `processing` snippet for its own pending label and aria-busy/aria-disabled state; prevented default repeat clicks retain keyboard focus. Other forms do not inherit its busy state. Confirmed favorites props still synchronize saved state. Guest and authentication-error dialog handling, action/method/fields/partial props and backend contracts remain unchanged. Default Inertia behavior now handles HTTP/network exceptions without favorite-specific presentation. No replacement notification or pending system was introduced; no mock changes were needed.

Updated existing browser/game-page tests and authenticated Home/public Game story assertions, retaining every story ID. Existing source-only acceptance means retained visual references are not claimed current after removal of notification UI.

Checks from `assets/`:

- `./node_modules/.bin/oxfmt` on the ten changed Svelte/TypeScript/test/story files: passed; final scoped `--check` also passed. The worker's initial `bun exec oxfmt` attempt could not resolve the executable; explicit local binary succeeded without installation.
- `./node_modules/.bin/eslint` on those same ten files: passed.
- `bun run typecheck`: passed, Svelte reported 0 errors and 0 warnings.
- `bun run test:unit -- tests/pages/game/ui/game.test.ts`: all 19 tests passed, including guest/expired-auth handling and retained detail/session behavior.
- `bun run test:browser -- tests/shared/components/favorite_button.browser.test.ts tests/pages/home/ui/home.browser.test.ts -t 'keeps processing local|keeps confirmed|attributes Hot' --browser.screenshotFailures=false`: 8 passed in Chromium/Firefox, 14 intentionally filtered out. Covers local pending/focus/repeat prevention, confirmed membership across copies, network/identity failure without notifications, retry, and Home attribution/empty composition. Screenshot assertions were excluded.
- Source scan found no remaining `FavoriteFeedback`, `favorites_feedback`, `feedbackId`, `bind:feedback`, `feedback.pending` or `feedback.result` in assets/js, assets/tests or assets/stories.
- `openspec validate --all --strict --no-interactive`: 86 passed, 0 failed. `mix openspec.check` in the Nix test environment: passed for 10 active changes. `git diff --check`: passed.

DevTools read-only inspection selected existing page 8 at `http://localhost:6007/?path=/story/widgets-game-collection--one-browse-game`, preserving its route/state. It reports `Server timed out` and requests server restart; its existing preview is stale and cannot verify the changed source. No restart, new tab, Storybook execution, screenshot capture or visual pass was performed. Automated semantic browser checks provide the fresh behavior evidence; the pre-existing broader live/visual gate remains open.

Tasks 14.1/14.2 are verified (37/45 overall). The eight broader gates remain open; issue #123 remains In Progress and the change remains active. Existing index contents are preserved with no staging, commit or archive.

## Parent-owned collection header snippets - 2026-09-13

GameCollection now accepts `header: Snippet`, renders it inside its existing h2, and creates the section/heading association with `$props.id()`. Removed the title, headingAttribution and headingId input props. Home supplies Playable and Hot/native BGG link markup through named header snippets. The scoped `.section-heading :global(a)` selector preserves link decoration across snippet ownership. Compact two-row names are now Row 1/Row 2 within the named section; single-row behavior, lane partition/order, empty omission and favorites remain unchanged. Existing collection stories and tests use fixed single-root createRawSnippet fixtures without new wrapper components, runtime text extraction or interpolated user HTML. Eight existing story IDs are preserved.

Exactly four code/test/story paths changed for this amendment: GameCollection, Home, collection stories and collection browser tests. Checks from assets:

- `./node_modules/.bin/oxfmt` and scoped `--check` on those four files: passed.
- `./node_modules/.bin/eslint` on those four files: passed.
- `bun run typecheck`: passed; Svelte reported 0 errors and 0 warnings.
- `bun run test:browser -- tests/widgets/game-collection/ui/game-collection.browser.test.ts tests/pages/home/ui/home.browser.test.ts -t 'keeps.*playable|attributes Hot' --browser.screenshotFailures=false`: 8 passed in Chromium/Firefox, 12 intentionally filtered out. Verified real Home snippet heading/section names, native link target and focus, empty-provider omission, and 6/7/8 compact game partition/order plus keyboard traversal with raw-snippet test headers. Screenshot assertions were excluded.
- Source review confirmed all production callers supply named snippets, old heading input props are absent, and no BGG-specific markup remains in the widget. Internal headingId remains solely for the generated accessible association.
- `openspec validate --all --strict --no-interactive`: 86 passed, 0 failed. `mix openspec.check` in the Nix test environment: passed for 10 active changes. `git diff --check`: passed.

Story source was reviewed without Storybook execution, screenshots, retained baseline changes or a rendered appearance claim under the existing waiver. No fresh manual validation is claimed on the previously unavailable prepared Storybook page. Tasks 15.1/15.2 are verified (39/47 overall); eight broader gates stay open. Issue #123 remains In Progress, add-game-favorites stays active, and the pre-existing index remains unchanged without staging, commits or archive.

## Home component and favorite feature relocation - 2026-09-13

Moved GameCollection and GameCard to `assets/js/pages/home/ui/`, retaining local default imports and no new exports from the Home public API. Moved FavoriteButton to `assets/js/features/toggle-game-favorite/ui/favorite-button.svelte` and added the named `FavoriteButton` feature entry point used by Home's card and game detail. Removed the old widget barrel from the working tree without changing the existing index. Updated only component imports in both existing card/collection stories; moved the screenshot-free collection semantic test beside Home tests. Retained story paths/titles/IDs/decorator and the Home-based favorite integration test's legacy path, preserving their reference identities.

Pre-edit `bun run typecheck` passed with zero diagnostics. Pre-edit focused browser checks passed 12 cases and failed the Home h1 expectation in both browsers: current production already renders `Games`, while the test expected `Hot (by BGG)`. Corrected only that test expectation and reconciled the active delta; the product heading and visible Hot section were unchanged. The initial failing runner unexpectedly wrote an automatic diagnostic PNG despite `--browser.screenshotFailures=false`; that task-owned file was moved outside the worktree to `/tmp/d20-fsd-relocation-20260913/baseline-heading-failure.png`. No screenshot assertion or visual comparison was executed, no reference was accepted or updated, and the diagnostic is not visual acceptance evidence.

Post-edit checks from `assets/`:

- `./node_modules/.bin/oxfmt --check` on the ten touched current files: the six product/index files pass; the same four pre-existing failures remain in `stories/widgets/game-card.stories.ts`, `stories/widgets/game-collection.stories.ts`, `tests/pages/home/ui/home.browser.test.ts` and the relocated `tests/pages/home/ui/game-collection.browser.test.ts`. Their existing formatting was deliberately preserved to avoid unrelated churn; the scoped check as a whole is not claimed passing.
- `./node_modules/.bin/eslint` on those ten files: passed.
- `bun run typecheck`: passed with zero Svelte errors and warnings.
- `bun run test:unit -- tests/pages/game/ui/game.test.ts`: 19 passed, preserving both feature consumers' request/authentication/detail behavior.
- `bun run test:browser -- tests/pages/home/ui/game-collection.browser.test.ts tests/pages/home/ui/home.browser.test.ts tests/shared/components/favorite_button.browser.test.ts -t 'keeps.*playable|keeps processing local|keeps confirmed|attributes Hot' --browser.screenshotFailures=false`: 14 passed in Chromium/Firefox; 14 intentionally filtered out, including screenshot cases. Covers Home headings/attribution/empty section, compact 6/7/8 partition/order and keyboard traversal, local processing/focus/repeat prevention, confirmed membership on repeated cards, failure and retry without notifications. Final log: `/tmp/d20-fsd-relocation-20260913/browser.log`.
- Source comparison against task-specific pre-edit copies confirms GameCollection and FavoriteButton are byte-identical after moving, with only six import replacements and the stale Home h1 test expectation changed elsewhere. Production old-path scans return no matches, page-local components remain private and both feature consumers use its public API.
- SHA-256 comparison confirms all 414 existing screenshot references and their path inventory are unchanged. The complete pre-existing staged diff is byte-identical to its saved baseline. `git diff --check` passed.

Read-only DevTools inspection reused the existing prepared `http://localhost:6007/?path=/story/widgets-game-collection--four-browse-games` page without navigation, reload, interaction or screenshots. Its accessible tree contained Hot attribution, four game links and favorite actions. This does not establish post-relocation visual or motion acceptance; no Storybook command or screenshot comparison was run. No backend, persistence, migration, runtime or dependency changes were required.

Independent source review found no defects in the bounded relocation. Strict OpenSpec validation passed all 87 items and the native `CI=true MIX_ENV=test nix develop --command mix openspec.check` passed for 10 active changes. Tasks 16.1/16.2 are verified (41/49 overall); eight wider delivery gates remain open. The complete feature is not finalized: issue #123 remains In Progress and add-game-favorites remains active. No staging, unstaging, commit or archive was performed.

## One icon-only favorite control and view-owned messages - 2026-09-13

Removed FavoriteButton's presentation variant API, detail text span, derived action/label copy and detail-only box rules. Both Home and game detail now use the same icon-only button; detail retains its existing title prop and wrapper. Complete Add/Remove messages and title/untitled alternatives are declared directly in markup aria-label, without script text fragments or new localization infrastructure. Pending keeps the confirmed action name and empty visible text while exposing aria-busy/aria-disabled and preventing repeated submission. The resource favorite.action, PUT/DELETE fields/options, guest/expired-auth behavior, safe return target, confirmed membership and failure/retry semantics are unchanged.

Merged the effective icon declarations into base button/star rules and removed the gap formerly needed for visible detail text. Home inherits the same 1em icon sizing, pointer behavior, flat gold states and focus/hover preview. Detail intentionally loses its previous text-button box and uses those shared icon rules; no hero wrapper or other page styles changed. Existing game tests and story assertions use exact fixture-specific accessible action names, with generic untitled fallback retained. The existing mutation test now also checks empty text in idle/pending/saved/removal states, stable action and busy/disabled states, and one submission after repeated activation. No new test file, story, mock, backend or dependency change was introduced.

Checks from assets:

- `./node_modules/.bin/oxfmt --check js/features/toggle-game-favorite/ui/favorite-button.svelte js/pages/game/ui/game.svelte tests/pages/game/ui/game.test.ts stories/pages/public/game.stories.ts`: both product files pass; the same two pre-existing formatting failures remain in game.test.ts and game.stories.ts. Their broader formatting was preserved. Product-only formatting passed again after removing the inert gap. The full four-file formatting check is not claimed passing.
- `./node_modules/.bin/eslint` on those four files: passed.
- `bun run typecheck`: passed with zero Svelte errors and warnings.
- `bun run test:unit -- tests/pages/game/ui/game.test.ts`: all 19 passed, including saved/pending/removal, authentication, request fields and retained Session/interest behavior.
- `bun run test:browser -- tests/shared/components/favorite_button.browser.test.ts -t 'keeps processing local|keeps confirmed' --browser.screenshotFailures=false`: six passed across Chromium/Firefox; the two screenshot cases were intentionally filtered out. Verified Home title-aware and untitled names, local processing/focus/repeat prevention, confirmed membership and error/retry behavior. Log: `/tmp/d20-favorite-icon-only-20260913/browser.log`.
- Source scan confirms no variant, derived UI messages or Saving/Removing text remains in FavoriteButton; favorite.action remains the form target. `git diff --check` passed.
- The current pre-edit index is byte-identical and all 414 screenshot reference paths/content hashes are unchanged.

The existing Storybook/screenshot/rendered-review waiver remains in effect. No Storybook command, screenshot capture, screenshot comparison or fresh visual/motion review ran. Retained detail screenshots still show earlier presentation and do not establish the new icon-only design. Evidence and pre-edit copies are under `/tmp/d20-favorite-icon-only-20260913`. Independent scoped review found no defects or task 17 inconsistencies. Strict OpenSpec validation passed all 87 items; `CI=true MIX_ENV=test nix develop --command mix openspec.check` passed for 10 active changes. Tasks 17.1/17.2 are verified (43/51 overall). The eight wider delivery gates remain open; issue #123 remains In Progress and add-game-favorites remains active. No staging, unstaging, commit or archive.

## Favorite Game naming (task 18, 2026-09-13)

Renamed the slice to `assets/js/features/favorite-game` and updated Home GameCard and detail public imports. `FavoriteButton` and its component/barrel bytes are unchanged; earlier `toggle-game-favorite` paths above are historical.

- From assets, `./node_modules/.bin/oxfmt --check js/features/favorite-game/index.ts js/features/favorite-game/ui/favorite-button.svelte js/pages/game/ui/game.svelte js/pages/home/ui/game-card.svelte` and the same file list through `./node_modules/.bin/eslint`: passed.
- `bun run test:unit -- tests/pages/game/ui/game.test.ts`: 19 passed. `bun run typecheck`: passed, Svelte 0 errors/warnings.
- `openspec validate --all --strict --no-interactive`: 87 passed, 0 failed. `env -u BASH_ENV CI=true MIX_ENV=test nix develop --command mix openspec.check`: passed for 10 active changes.
- Reference scan found no old feature imports in assets/js, assets/tests or assets/stories; original component/barrel SHA-256 and cached diff hash are unchanged. `git diff --check` passed. No Storybook, screenshots or visual pass; no index changes, commits or archive. Task 18 is verified, 45/53 overall, with the same eight wider gates open.

## Favorite Game reads current page authentication - 2026-09-13

FavoriteButton now reads `page.props.auth.authenticated` inside beforeSubmit on every attempt, using its existing usePage object. Removed the authenticated prop, defaults and forwarding from FavoriteButton, GameCollection, GameCard and Home/detail consumers. Removed the now-unused Home/detail auth destructuring; independent SessionForm/InterestForm code is unchanged. Kept the current favorite-game feature path and per-game favorite/saved/title/tabindex API. No captured or derived authentication boolean, fallback, new store, provider or shared-mock behavior was added. Form action/method/fields/options, guest and authentication-error dialog, confirmed membership, pending/focus/repeat prevention, labels and styles remain unchanged.

Existing detail and Home-based browser tests initialize actual page auth through inertiaMock.setPage. The existing detail auth case verifies guest -> signed in -> guest on the same mounted control with page-only updates and unchanged guest auth props, then verifies an authentication error on a later authenticated attempt without replay. Isolated card/collection stories reset page context on entry and cleanup; the existing saved card examples prepare authenticated page context, while other current examples retain guest context. Removed obsolete authenticated card-story args. Existing page-story withLayout initialization, shared mocks, story identities and reference paths are unchanged.

Checks from assets:

- Scoped `./node_modules/.bin/oxfmt --check` on nine touched files: all five product files pass. The same four pre-edit formatting failures remain in tests/pages/game/ui/game.test.ts, tests/shared/components/favorite_button.browser.test.ts and stories/widgets/game-card.stories.ts/game-collection.stories.ts. Existing formatting debt was preserved; the entire nine-file formatting check is not claimed passing.
- `./node_modules/.bin/eslint` on the same nine files: passed.
- `bun run typecheck`: passed with zero Svelte errors/warnings.
- `bun run test:unit -- tests/pages/game/ui/game.test.ts`: all 19 passed, including current-page auth transitions, guest/expired-auth handling, no automatic replay and unchanged detail/Session/interest/request behavior.
- `bun run test:browser -- tests/shared/components/favorite_button.browser.test.ts -t 'keeps processing local|keeps confirmed' --browser.screenshotFailures=false`: six passed across Chromium/Firefox, two screenshot cases intentionally filtered out. Covers actual authenticated context, local processing/focus/repeated activation, named and untitled controls, confirmed membership and failure/retry. Log: `/tmp/d20-favorite-page-auth-20260913/browser.log`.
- Production chain source scan finds only the direct page-auth read and authentication-error callback, with no authenticated props or forwarding. All five product style blocks and both shared mocks match their task-start copies.
- Independent nine-file review found no defects or task 19 conflicts. Installed Inertia usePage returns the stable reactive page object updated by setPage, matching the tested fresh-read behavior.
- The complete current Git index and all 414 screenshot reference paths/content hashes match the task-start baseline. `git diff --check` passed.

No Storybook execution, screenshots/reference updates or fresh rendered review ran under the existing waiver. Story context isolation is source-reviewed and typechecked; it is not claimed as freshly executed Storybook evidence. No backend, migration, dependency or configuration changes were required. Evidence and before-copies: `/tmp/d20-favorite-page-auth-20260913`. Strict OpenSpec validation passed for all 87 items, and `CI=true MIX_ENV=test nix develop --command mix openspec.check` passed for 10 active changes. Tasks 19.1/19.2 are verified (47/55 overall). Eight wider delivery gates remain open, with the issue In Progress, the change active and no staging, unstaging, commits or archive.

## Local delivery preparation - 2026-09-13

The user authorized committing Favorite Game and transferring it to local master. Task 20 supersedes the earlier no-staging/no-commit restriction for this owning work. Task 19 was already verified at 47/55; its old tasks.md progress sentence was stale and has been corrected. The feature remains owned by issue #123 and this active change.

Preparation formatted nine owned test/story files through the native formatter, initialized actual mocked page authentication in the existing Home test, and replaced Favorites.create/3 single-clause with/else by equivalent case branches. No request, identity, persistence or error contract changed. Authoritative specifications now include game-favorites and the implemented changes to game-detail and home-game-discovery, preserving unrelated requirements and unaddressed scenarios. The domain delta now describes the verified case expression. Sync does not constitute rollout or archive.

Fresh evidence under `/tmp/d20-favorite-delivery`:

- Frontend unit suite: 81 passed across 12 files (`unit.log`).
- Focused Home, collection and favorites browser semantic checks: 16 passed across Chromium/Firefox, 12 intentionally filtered out (`browser-final.log`). No screenshot assertions ran.
- Full frontend native formatting and lint plus TypeScript/Svelte checks passed (`format-final.log`, `lint-final.log`, `typecheck.log`); Svelte reported zero errors and warnings. The earlier preserved formatting failures are resolved in the owning files.
- Full backend tests passed 934 before the case rewrite (`backend-ci.log`); 95 focused persistence/controller/detail tests passed after it (`backend-focused.log`). This distinguishes the two verified source points.
- `mix ci` stopped at Credo: one favorite single-clause with finding, subsequently fixed, and three pre-existing Koala Rescue Club nesting findings. Final full Credo retains only those three unrelated findings (`backend-credo-final.log`); scoped favorite Credo passes. Full CI is not reported passing. Final Dialyzer passed under the existing ignore policy (`backend-dialyzer-final.log`).
- Disposable test-database reverse migration, absent-table check, forward reapply and constraints were verified (`backend-migration-rollback.log`, `backend-migration-absent.log`, `backend-migration-forward.log`, `backend-migration-constraints.log`). Shared development and target-environment data were not reset.

The prior Storybook/screenshot/rendered-review waiver remains in effect; no fresh visual or motion pass is claimed. Tasks 4.3/4.4/4.5/4.7/5.1/6.3/7.2/8.2 remain open, including incomplete full cross-stack acceptance and unauthorized target rollout. Task 20.2 transfer is pending. Task 20.3 cannot safely complete while the pre-existing PostgreSQL process uses the worktree's `.pg_data` and editor/services remain active; retain the source worktree and local branch without force deletion. No commit, transfer or cleanup is claimed by this preparation record.

Strict validation after authoritative synchronization passed all 88 items with zero failures (`openspec-sync.log`). Task 20.1 is verified; current progress is 48/58, with the eight wider gates and pending transfer/retirement tasks still open. All required planning artifacts report done; implementation was not started by this specification-only reconciliation.


## Verified local master transfer - 2026-09-13

Committed the owning implementation, migration, tests, fixtures, prior scoped reference changes and synchronized specifications as `fbda52cf14eabcc2bbaa2658b6e68e26a3ba666e` (`feat(games): add private account favorites`). Pinned master was `31a50f2334b53ad04a66be83f0cadeb4ce7a5f99`. Rebase was a no-op on that exact base; the committed candidate passed 140 focused domain/controller tests and 16 semantic browser checks before the clean local master was fast-forwarded. Candidate ancestry and clean target state were verified. No remote publication or deployment occurred.

Post-transfer validation ran from `/home/max/apps/d20` and its `assets/` directory:

- `CI=true MIX_ENV=test MIX_TEST_PARTITION=_favorites_delivery nix develop --command mix test test/d20/games/favorites_test.exs test/d20/games_test.exs test/d20_web/controllers/game_favorite_controller_test.exs test/d20_web/controllers/page_controller_test.exs`: 140 passed.
- `bun run format.check`, `bun run lint`, and `bun run typecheck`: passed; zero Svelte errors/warnings.
- `bun run test:unit`: 81 passed across 12 files.
- `bun run test:browser -- tests/pages/home/ui/home.browser.test.ts tests/pages/home/ui/game-collection.browser.test.ts tests/shared/components/favorite_button.browser.test.ts -t 'attributes Hot|delivers pointer|keeps .*playable games|keeps processing local|keeps confirmed' --browser.screenshotFailures=false`: 16 passed, 12 filtered out across Chromium/Firefox.
- `CI=true MIX_ENV=test MIX_TEST_PARTITION=_favorites_delivery nix develop --command mix openspec.check`: passed for 10 active changes. Synced strict OpenSpec validation passed all 88 items.

Logs are `/tmp/d20-favorite-delivery/candidate-*.log` and `master-*.log`. All 414 screenshot reference paths/content hashes matched the task-start baseline before commit. Existing visual waiver and the three pre-existing Koala Credo findings remain explicit limitations; no full CI or fresh visual pass is claimed.

Task 20.2 is verified and reconciled with issue #123. This record is a separate documentation finalization commit because post-transfer results followed the semantic implementation commit. Progress is 49/58. The eight wider acceptance/rollout/archive gates remain open. Task 20.3 remains open: the pre-existing PostgreSQL process uses `.worktrees/game-favorites/.pg_data`, with active user editor/services in the worktree. Preserve the clean worktree and local branch; do not stop user runtime, remove its data or force retirement. The feature issue remains In Progress and this change remains active pending its wider gates.


## Source worktree retired - 2026-09-13

The user explicitly requested removal after the verified master transfer. Both the source worktree and master were clean at `a5f57bfdefafb266a72c1a9bd6af5f6fb9c08d56`; source ancestry in master was verified. No Phoenix, Vite or Storybook runtime was active. PostgreSQL had no client connections and was stopped normally with PostgreSQL 17 `pg_ctl stop -m fast -w`.

Preserved its data directory at `/home/max/.local/state/d20/retired-worktrees/game-favorites-20260913/postgres`. Normal `git worktree remove` removed registration but reported a nonempty residual directory containing image artifacts. Preserved that remaining directory at `/home/max/.local/state/d20/retired-worktrees/game-favorites-20260913/remaining-files`, then deleted the fully integrated local branch with `git branch -d worktree/game-favorites`. No force removal, force branch deletion, data reset or editor shutdown was used.

Verified that the original worktree path, Git worktree registration and local candidate branch are absent. User editor/tool processes were preserved. Task 20.3 is complete; progress is 50/58, with the eight broader acceptance/rollout/archive gates still open. This lifecycle closeout record is committed in the surviving primary checkout after retirement; no replacement worktree or product changes are required. Issue #123 remains In Progress.
