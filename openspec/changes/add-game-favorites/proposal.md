## Why

Players need to mark games they want to revisit directly from discovery and game detail. Existing [issue #123](https://github.com/ravecat/d20/issues/123) owns the first favorites interaction, its standard Inertia form simplification, and the requested refinement to clean heading-sized stars aligned with titles through local CSS Grid structure with fixed-ratio cards and content that scales from each card's actual size.

## What Changes

- Add durable, private account favorites for local and provider-only games, identified by the resolved BoardGameGeek game ID. Add and remove are explicit, idempotent operations.
- Place a visually bare star at the top right of every carousel card: a gold outline with a transparent interior when absent and the same gold outline plus gold fill when saved. The parent favorite button's CSS owns SVG paint through the existing saved-state selector; retain one unchanged SVG path without conditional paint attributes, inline paint styles, icon swaps, or runtime paint logic. Keep its existing card-relative icon size independent of the fitted title font, with a clean thin outline, no black/white glow or visible button box, and the existing transparent 44 by 44 CSS pixel hit target and native button semantics.
- Place the same icon-only favorite button on Home and at the top right of the detail hero. Its unchanged SVG follows the same gold outline/fill and hover/focus treatment. Remove the former labeled detail variant and keep accessible Add/Remove messages directly in button markup. Favorites remain independent of Play, existing Sessions, and requests for online availability.
- Return caller-specific membership in a separate `favorites` page prop, derive every repeated card from that prop, and refresh membership/authentication through Inertia without replacing the current carousel collections.
- Replace the custom JSON transport and favorites membership store with a shared Inertia `<Form>` control and redirect responses. Use each form's local processing state without shared pending coordination or result announcements; the form owns submission, errors, cancellation, and request lifecycle. Notifications are deferred separately.
- Use the exact same query-free `PUT /favorites/:bgg_id` and `DELETE /favorites/:bgg_id` resource URL. Project one `{bggId, action, slug}` descriptor; submit the original source slug as form data without a Session field, with the path ID as the only mutation identity. PUT resolves and checks source identity independently of detail visibility or runtime Sessions; DELETE ignores source context. Remove the former route and split action fields without a compatibility alias.
- Remove the custom BGG ID validator and friendly malformed-ID error contract. Convert URL IDs with standard `String.to_integer/1` in the HTTP boundary; context commands take integer IDs from the supported application workflow. Preserve account/source identity rules, required fields, account relationships and composite uniqueness; remove Favorite numeric-range validation and its positive-ID database check.
- Expose `Favorites.create/3` and `delete/2` as the validated mutation API, inline persistence and identity comparison, and remove the intermediate save/add/remove and callback-wrapper APIs. Preserve domain and returned changeset errors and use Repo for storage-only test fixtures. Remove persistence rescue and the custom unavailable mapping so infrastructure exceptions propagate through the ordinary Phoenix error boundary.
- Keep favorite controllers as HTTP adapters: delegate account/source/identity checks to `D20.Games.Favorites`, which resolves the original slug through `D20.Games.fetch_by_slug/1` without detail visibility or runtime Session validation. `PageController` retains ordinary detail visibility/Session validation through `D20.Games.resolve_session/2`; favorites do not call that policy.
- Keep standard Inertia lazy callbacks for home collections and independent favorites/schema props, ordinary detail resolution, and Session validation. Accept redundant catalog/BGG reads and schema evaluation by the adapter during partial refreshes; test returned membership/auth/errors and omitted collection props rather than skipped-loader internals.
- Integrate the restored favorites work with updated master: retain `playable`, `SessionForm`, `InterestForm`, `HeaderFocused`, and all meaningful game-detail states while adding matching favorite descriptors, membership, and visible hero stars.
- Adopt master's existing light/dark by desktop/tablet/mobile Storybook matrix for every affected Home and detail scenario. Declare each state once, remove the redundant `FavoriteSavedDark` alias, and replace obsolete unthemed favorites references with six reviewed references per state in the new theme/viewport hierarchy.
- Keep local common Grid structure but make card ratios fixed through resizing: compact/Playable 2:1 and Games hero 2.9:1, with no mobile ratio switch or content-driven lane-height growth. Each variant owns proportional padding; visible text, stars, chips/status, and spacing scale through native card-container units/queries. Inline sizing calculations instead of `--slide-basis` or other geometry alias variables. Use native flex wrapping inside exactly one visible metadata chip row with `overflow: clip`: complete additional chips wrap below the visible row according to actual available footer width. Only long primary text may ellipsize; all category text remains in the DOM and accessible to assistive technology, without `aria-hidden` for visual suppression. Keep the 44-pixel transparent interaction target separate from visual glyph alignment, and preserve full-card links, separate forms, native focus snapping, and detail presentation.
- Reduce carousel metadata chips and development-status badges from `0.65em` to `0.5em` across all card variants; their existing em-based dimensions shrink proportionally while approved card geometry, headings, stars, and interaction remain unchanged.
- Give carousel favorite tooltips a stable `0.75rem` single-line `max-content` presentation. Use native CSS Anchor Positioning and a Popover top layer, with ordered below/above placement fallbacks, where supported; retain a simple local absolute fallback below the button elsewhere. Preserve favorite submission and detail presentation.
- Reuse the existing account dialog for guests; save only after an explicit authenticated activation. Include pending, failure, retry, removal, and persisted reload states.

The first version uses durable database storage, account-only saving, and controls only, without a separate Favorites page or filter. The user authorized implementation of this prepared specification. These scope choices follow the referenced game-interest proposal. The earlier #123 scope included guest persistence and guest-to-account merging; this proposal explicitly narrows the first delivery and defers those requirements rather than silently treating them as implemented.

The earlier tasks 1.1 and 2.2 verified compact shared Session resolution in Games; task 12 now removes Favorites from that policy while retaining ordinary detail validation. The following counts and results describe the earlier adapter/star milestone. The user accepts the adapter's redundant work and removes skipped-loader behavior from acceptance. Fresh focused backend validation passed all 142 tests and touched-file formatting passed, completing task 4.1. The prior 140/142 result describes the superseded assertions. New tasks 3.6 and 4.7 cover implementation and validation of CSS-owned outlined/filled stars, bringing current progress to 14/19 after the CSS implementation; visual validation remains pending. Four whole-feature gates remain open; prior appearance evidence does not establish the newly requested star treatment.

## Responsive Home titles

Improve the supplied CompactMetadata case through CSS only: smaller card-relative compact/Playable headings, four lines normally and three on the narrowest cards, and wrapping for unbroken words. Preserve hero font sizing, card ratios, footer typography, favorite geometry, accessible names and carousel interactions. This title-specific sizing supersedes earlier heading/star size equality. Complete arbitrary-length text and content-measured maximum font fitting are not promised. Tasks 3.7/4.8 are verified: all 168 normal Home visual comparisons and 18 Chromium/Firefox browser tests pass (two existing skips), alongside scoped formatting/lint and typecheck. The whole-feature gates and separately owned star validation remain open.

## Home widget and Storybook extraction

Historical extraction milestone: the production widget placement below is superseded by task 16 and the page/feature ownership amendment at the end of this proposal. Its completed work and validation remain historical evidence.

Continue issue #123 in the same active change by extracting production `GameCard` and `GameCollection` widgets from Home. Move collection-count and lane-partition variants to collection stories, and title, image, fallback and metadata variants to card stories. Keep page composition, conditional sections, authentication and same-BGG favorites membership overlap and local-processing/retry scenarios at Home level. Preserve each source state's existing CSS appearance, card links, favorites interactions, CSS motion and accessible focus behavior; use the existing six theme/viewport projects without adding screenshot infrastructure.

The latest user instruction requires all changes to remain unstaged for review, with no commits. Extract only the current working-tree functionality and preserve existing behavior and appearance. Tasks 6.1-6.4 track this amendment; broader favorites validation, rollout and finalization remain outside this task.

## Capabilities

### New Capabilities

- `game-favorites`: Private membership persistence, identity, authenticated read/write contracts, safe retries, and account lifecycle.

### Modified Capabilities

- `home-game-discovery`: Add accessible icon-only favorite controls and two compact Playable rows above six games, preserving collection order, CSS motion and interactive visible loop copies.
- `game-detail`: Add caller-specific favorite state and an independent hero control on local and provider-only pages without changing game resolution or activation policy.

## Impact

- Backend: a new favorites association and context under `lib/d20/games/`, an additive Ecto migration, `lib/d20/games.ex` resolved identity, `PageController` projections, and a focused `GameFavoriteController` with session-authenticated Inertia form routes, safe return redirects, standard lazy Inertia props, and existing CSRF protection.
- Frontend: home and game page UI, Home-owned GameCollection/GameCard components, the `features/favorite-game` public API for the reused Inertia favorites form, catalog types and nearby tests/stories. Local processing remains without a feedback type, renderer or page/card feedback props. No new library is required.
- Validation: persistence/controller tests, Svelte component and carousel browser tests, accessible desktop/mobile checks, six theme/viewport Storybook comparisons per affected state, migration reversal on a disposable database, and native OpenSpec validation. The existing below-fold capture limitation remains tracked by [#281](https://github.com/ravecat/d20/issues/281); this integration does not change screenshot infrastructure.
- No game-engine, Session/channel, iframe, or AsyncAPI contract changes. Rolling back application code can retain the new table; dropping it deletes saved preferences.
- The current pinned master base is `577c5c34ed1fa5dbb873f90c99b3e747d2cfb0ee`, which includes [game interest #275](https://github.com/ravecat/d20/issues/275), updated detail styling, and the six-cell Storybook matrix. This change preserves request counts, membership, `playable`, and `SessionForm`/`InterestForm` while resolving the restored favorites conflict. No backend behavior, persistence identity, dependency, or screenshot-runner change is included in this integration. The separate `delete-user-account` change is outside its scope.

## Card title clipping and favorite contrast

The screenshot follow-up refines the extracted widgets in issue #123. Remove the hard-edged gray title-shadow artifact and prevent visible descenders from being clipped while retaining the existing three/four-line limits, font sizes, fixed card ratios, footer clearance and full accessible names. Add a thin dark contour around the existing gold favorite glyph in both membership states, without a button background or functional changes. Keep all work unstaged and uncommitted.

## Icon-sized favorite target without tooltip

The latest screenshot follow-up explicitly replaces the earlier 44-pixel minimum target and tooltip requirements for icon-only card favorites. The native button must wrap the existing SVG box without extra padding or minimum target dimensions. Remove tooltip markup, anchoring/popover logic, hover/focus/Escape visibility state and the wrapper made unnecessary by that removal. Preserve SVG size/path/gold paint/dark contour, card heading reservation and layout, keyboard focus, accessible action/pressed/busy state, form submission and server-confirmed duplicate-card membership. The labeled game-detail variant retains its existing size and appearance. Tooltips are deferred; do not add a replacement native title tooltip. Work remains unstaged without commits. Tasks 8.1-8.2 own this refinement and final checks also complete the interrupted task 7.2.

## Flat favorite hover and keyboard-focus preview

The latest feedback rejects the dark multi-shadow edge as too dimensional. Remove those SVG filters and retain a single flat gold outline. For icon-only card favorites, CSS hover on hover-capable devices and keyboard focus-visible temporarily fill an unsaved star gold. Leaving both states restores the server-confirmed empty/filled appearance; saved stars remain filled. This preview must not mutate membership, aria-pressed, submit forms or introduce JavaScript hover state. Keep the icon-sized button and absent tooltip from the preceding amendment, and retain the labeled detail button layout. This supersedes earlier requirements that hover preserve the unsaved empty interior or that stars have a dark outer contour.

## Favorite alignment with the first title line

Match each card favorite button and SVG square to the height of the first title line box, with their horizontal centerlines aligned for both single-line and wrapped titles. Preserve existing compact/Playable and hero title font sizes and 1.2 line height, wrapping/clamps, card ratios, padding and footer layout. This supersedes only the earlier independent fixed icon sizing; retain the icon-sized native target, flat gold hover/focus treatment, absent tooltip and unchanged detail presentation. Tasks 9.1-9.2 track implementation and source validation under the latest explicit Storybook waiver in issue #123; all work remains unstaged and unrelated delivery gates remain open.

The card-state follow-up adds isolated unsaved hover and confirmed saved compact/hero stories so the filled favorite and its first-line alignment are directly reviewable. Reuse the existing native theme/viewport matrix and semantic assertions without duplicating page stories.

## Two compact Playable rows and smaller collection stories

The latest follow-up keeps one compact Playable row for one through six games and uses two compact rows for more than six games, without a hero card. Split the ordered input into contiguous balanced halves, with the extra game in the first row: seven games become 4/3. Both rows retain the existing shared CSS cadence and direction, section-wide hover/focus pause, independent seamless loops, accessible canonical order and favorites state. Games/Hot Games keeps its existing hero/compact composition and provider selection; the server's existing Playable limit remains unchanged.

Simplify collection stories to Hot counts 1-4 and Playable counts 1, 2 and 4. Retain seven Playable games only as the smallest distinct two-row example, replacing the redundant eight-game example. Remove Games 5/8/31/32 stories and use four browse games in default public Home, authenticated Home and GameCollection composition while preserving isolated card hover and confirmed saved compact/hero states. Remove only the 30 screenshot references directly orphaned by the five removed high-count stories. Keep larger shared fixtures used by existing browser tests and all runtime selection limits unchanged. This supersedes the former required high-count story and motion-demonstration matrix for this bounded amendment.

The user explicitly waives further Storybook runs, screenshot capture, reference updates and visual review for this task. Use source review, scoped formatting/lint, TypeScript/Svelte checks and focused non-screenshot six/seven/eight boundary and keyboard checks; do not report unrun visual or motion checks as passing. Tasks 9.2/9.4 use this revised acceptance and tasks 10.1-10.3 own the row/story amendment. Keep all changes unstaged and uncommitted, with whole-feature validation, rollout and lifecycle gates open.

## BGG Hot collection attribution

Extend this same issue #123 delivery in the existing game-favorites worktree: label the provider discovery collection `Hot (by BGG)` instead of `Games`, with a visible `by BGG` link to BGG's Hotness page at `https://boardgamegeek.com/hotness`. The attribution identifies the third-party selection, including its covers and content, rather than implying D20 editorial recommendations. When no dedicated Hot page URL is available, retain `by BGG` linked to `https://boardgamegeek.com`. Keep Playable separate and preserve selection, cards, favorites and ongoing parallel work. Update the referenced visually hidden Home h1 to plain `Hot (by BGG)` without hiding a focusable link. Keep changes unstaged and this wider change active while its existing delivery gates remain open.

## Collection stories share Hot attribution

Extend task 11 to the isolated Widgets/Game Collection examples: every browse example SHALL use the same `Hot (by BGG)` heading and BGG Hotness link as Home; Playable examples remain unattributed. Remove the standalone Empty collection story and its six orphaned references, since an empty collection renders no widget and belongs to page-composition coverage. Keep Home's empty/conditional coverage and production empty-input behavior. This supersedes the earlier zero-game collection example; retain Hot counts 1-4 and Playable 1/2/4/7. Preserve existing story IDs and unrelated parallel work, with no fresh screenshot generation or retained reference updates.

## Favorites depend on game identity, not runtime Sessions

The user correction removes runtime Session context from Favorites throughout this same issue #123 delivery. The hero favorite always targets the resolved game, including when the detail URL contains a Session query. Project only `{bggId, action, slug}`, submit no Session field, and expose `Favorites.create/3` with account, expected BGG ID and original slug. Pass the caller-provided string slug directly to `Games.fetch_by_slug/1` and compare BGG identity without a visibility, Session or defensive slug-validation gate. The existing resolver owns its input contract; retain returned source failures without new malformed-input guards or exception handling. Hidden local records can therefore be favorited when their source identity resolves; ordinary detail GET visibility, Session validation and return-page queries remain unchanged. Tasks 12.1/12.2 own the bounded implementation and verification. Persistence data, account/CSRF rules and existing broader delivery gates remain unchanged; both mutations call Repo directly and let infrastructure exceptions propagate without a custom favorites-unavailable error; no staging, commit or archive is included.

## Remove redundant favorite ID range rules

Remove the Favorite changeset numeric range validation and positive-ID constraint mapping, and remove the positive CHECK from the unpublished create-game-favorites migration. Keep the integer column, required identity, account foreign key/cascade and composite primary key. Storage type limits remain technical limits rather than duplicated favorites business validation. Update existing persistence tests and verify the edited migration on a disposable test database. This continues issue #123 with tasks 13.1/13.2; do not stage, commit or archive the wider unfinished change.

## Defer favorites feedback and notifications

Remove favorites success/error notifications and the feedback plumbing from this same issue #123 delivery. Delete `FavoriteFeedback` and `favorites_feedback.svelte`; remove `feedback`/`feedbackId` props, bindings, result associations and page-owned feedback objects from Home, detail, GameCollection, GameCard and FavoriteButton. Do not introduce a notification service, store, context, replacement notice or shared pending coordinator.

FavoriteButton uses the existing Inertia Form children snippet's local `processing` state for its pending label, busy/disabled semantics and repeat-activation guard while retaining keyboard focus. Server favorites props still own saved state for every occurrence. Preserve explicit methods, resource action, source fields, partial reloads, guest authentication and the authentication-error dialog. Expected favorite failures retain confirmed membership and permit an explicit retry without a custom notice; HTTP/network failures use the standard adapter behavior. Backend error contracts remain unchanged.

Tasks 14.1/14.2 own implementation and focused verification. The existing instruction against Storybook execution, screenshot generation/reference updates and rendered review also applies here: update affected story sources and semantic tests, then run scoped formatting/lint/typecheck and non-screenshot unit/browser checks. Earlier announcement and global-pending evidence remains historical and does not establish this amendment. Leave the eight existing wider delivery gates open, preserve current index contents, and do not stage, commit or archive.

## Parent-owned collection header snippets

Replace GameCollection title, headingAttribution and headingId props with a required header: Snippet. Home supplies Playable text and Hot (by BGG) native-link markup through named header snippets. The widget retains its h2 and section styling, generates a unique heading ID with $props.id(), and renders the snippet inside the heading. Compact multiple rows use generic Row 1/Row 2 labels within their named section rather than reconstructing text from a snippet. Preserve visible headings, link style/destination, empty omission, lane partition/order, carousel behavior, favorites and keyboard access. Existing TS stories/tests use static createRawSnippet fixtures without a new wrapper component or runtime HTML parsing. Keep story IDs and the prior Storybook/screenshot waiver. Tasks 15.1/15.2 continue issue #123 in the existing worktree without staging, commits or archive.

## Home component ownership and reusable favorite feature

Continue issue #123 in this same game-favorites worktree. GameCollection and GameCard serve only Home, so move them from `assets/js/widgets/game-collection/ui/` to `assets/js/pages/home/ui/game-collection.svelte` and `game-card.svelte`. Home and its collection use relative default imports; remove the old widget barrel without aliases and keep both components internal to the Home slice. The favorite interaction is reused by Home and detail, so move `assets/js/shared/components/favorite_button.svelte` to `assets/js/features/favorite-game/ui/favorite-button.svelte` and expose `FavoriteButton` through that feature's `index.ts`. Both production consumers use `~/features/favorite-game`.

Move only the screenshot-free collection semantic test to `assets/tests/pages/home/ui/game-collection.browser.test.ts`. Update existing card/collection story imports to their Home-owned production components while retaining story paths, titles, IDs, decorator, fixtures and every screenshot file. Keep `assets/tests/shared/components/favorite_button.browser.test.ts` in place: it exercises Home and its path owns existing screenshot references. These retained verification paths do not define production FSD ownership. Reconcile the stale Home test/spec h1 expectation with the current `Games` page h1; the visible provider h2 remains `Hot (by BGG)` without any product heading change.

Tasks 16.1/16.2 cover this relocation and focused existing semantic/static checks. Props, rendered markup, CSS, carousel behavior, authentication, local Form processing, server membership and request contracts remain unchanged. Preserve the existing Storybook/screenshot/rendered-review waiver, existing index contents and eight wider open gates. No backend, persistence, migration, dependency, runtime Session or public game protocol changes are included; no new-entity persistence gate applies. Leave changes unstaged without commits or archive. The earlier widget extraction is historical; it does not require retaining widget ownership or adding compatibility re-exports.

## One icon-only favorite control with view-owned messages

Continue issue #123 with tasks 17.1/17.2: remove FavoriteButton's visible detail label, `variant` API/modifiers, detail box CSS and now-unused `$derived` UI `action`/`label` variables. Promote existing icon styling to the base button, so Home and detail use one icon-only control. Keep complete accessible Add/Remove messages directly in the markup's `aria-label`, with optional title interpolation and generic fallback; do not assemble sentence fragments in script or introduce localization infrastructure. Detail already supplies `game.name`, so its accessible action becomes title-aware and remains stable while processing. The resource URL `favorite.action`, request methods/fields/options, authentication, confirmed membership, focus, busy/disabled semantics and retry behavior remain unchanged.

This supersedes earlier labeled-detail, visible Saving/Removing and detail-box requirements; historical completed records remain intact. Update only the existing detail unit/story assertions and scoped checks, preserving story IDs and the existing Storybook/screenshot waiver. No new test files, backend/persistence changes, unrelated formatting, index mutation, commit or archive; the same eight wider gates remain open.

## Favorite Game feature naming

Task 18 names the reused interaction **Favorite Game** at `features/favorite-game`. Rename the slice and its two public imports together, preserving `FavoriteButton` and its component bytes. Earlier checked tasks and validation commands retain the superseded path as historical evidence. No behavior, presentation, persistence or test coverage changes apply.

## Favorite Game reads current page authentication

Continue issue #123 with tasks 19.1/19.2 in the existing `assets/js/features/favorite-game` slice. Remove the `authenticated` prop from FavoriteButton, GameCard and GameCollection and remove the corresponding Home/detail bindings. FavoriteButton's existing `beforeSubmit` reads `page.props.auth.authenticated` directly from its existing `usePage()` result on every submission. Keep `favorite`, `saved`, optional `title` and `tabindex`; no captured/derived auth boolean, new store/provider or default-auth fallback is needed. Remove page auth destructuring made unused by this change while preserving independent SessionForm/InterestForm behavior.

Use the existing test/story page-context setters for authentication, and verify guest/sign-in/sign-out transitions on the same mounted favorite through page-only updates without rerendering auth props. Preserve the existing auth-error dialog, no replay, saved/request state, one-icon presentation and resource contract. Remove obsolete card-story authenticated args and initialize/reset isolated story context without changing paths, IDs or screenshots. Historical prop-passing records are superseded for this boundary only. Keep the existing visual waiver, index, formatting-debt allowance and eight wider gates; no new tests files, backend/persistence changes, commits or archive.

## Local Favorite Game delivery

The user now authorizes committing Favorite Game and transferring it linearly into local `master`. This supersedes earlier unstaged/index-preservation/no-commit restrictions for the owning work only. Task 20 prepares and synchronizes the implemented contracts, commits the semantic feature with its delivery artifacts, validates the pinned target before a fast-forward, and records post-transfer validation. No remote publication or target-environment deployment is authorized. Keep issue #123 and this change active while the eight wider visual, cross-stack, rollout and archive gates remain open; the existing Storybook/screenshot/rendered-review waiver continues.

Preparation includes native formatting of the nine already-owned tests/stories, authenticating the existing Home test through actual mocked page context, and replacing the Favorite create single-clause `with` with `case` while preserving all outcomes. Synchronize game-favorites, game-detail and home-game-discovery into authoritative specifications without archive. Retire the source worktree and local branch only after successful integration and safe removal; its active PostgreSQL data directory and user services currently require retention without force deletion.
