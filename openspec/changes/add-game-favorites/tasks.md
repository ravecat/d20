## 1. Persistence and identity

Current continuation: task 21 restores visual checks for the user-approved current rendering. Earlier Storybook/screenshot waivers below are historical for their amendments and do not block task 21. The eight prior wider gates remain open unless every outstanding clause receives its own evidence.

- [x] 1.1 Historical completed baseline; its Session coupling is superseded by tasks 12.1/12.2 and its positive BGG constraint by tasks 13.1/13.2. Add `D20.Games.Favorite`, the focused `D20.Games.Favorites` context, and an additive `game_favorites` migration with account cascade, positive BGG constraint, composite primary key, and first-save timestamp. Use `Favorites.create/4` and `delete/2` to own missing-account rejection, integer input, original source/identity validation and shared domain detail/Session policy. Remove expected_id and its custom validation response; use native URL conversion at the HTTP boundary. Inline persistence and identity comparison; remove save/add/remove compatibility APIs and the mutate callback helper. Preserve persistence-only exception handling; seed existing test records through Repo. Keep the shared resolver in `D20.Games.resolve_session/2` beside route loading; remove the standalone Detail module and inline its game_session helper into direct public clauses and let PageController add channel topics only in its projection. Verify direct-context rejection/success and add/remove, private membership, duplicate/concurrent adds, invalid records, and account deletion in `test/d20/games/favorites_test.exs` using `D20.DataCase`.
- [x] 1.2 Historical completed baseline; its Session coupling is superseded by tasks 12.1/12.2. Retain authoritative `bgg_id` in the resolved detail result and construct `{bggId, action, slug, session?}` descriptors with the same query-free `/favorites/<bggId>` action for PUT/DELETE, original source spelling, and a Session string only when validated without changing existing local/provider resolution or metadata fallback. Extend `test/d20/games_test.exs` for local/provider identity, exact numeric slugs, leading zeroes, and stale bindings; update page projection assertions for one action, source preservation, and optional Session omission.

## 2. Web boundary and projections

- [x] 2.1 Historical completed baseline; its Session coupling is superseded by tasks 12.1/12.2. Use `PUT /favorites/:bgg_id` and `DELETE /favorites/:bgg_id` in the existing Inertia pipeline, removing the former slug-based PUT without an alias; replace JSON responses with safe `response_to` 303 redirects and `assign_errors` for authentication/favorite failures. Convert the generated path ID with String.to_integer/1; delegate account, PUT source/Session and identity checks to the Favorites context; keep only HTTP input/output handling in the controller. DELETE delegates source-independent removal. Preserve ordinary Phoenix path precedence. Preserve ownership, provider-independent removal, and validated matching-Session visibility. Update controller tests for form values, redirects, unsafe return targets, errors, CSRF, expired auth, absent/malformed source, numeric collisions/stale bindings, DELETE with invalid source fields, and no writes on failure. Verify removed-route absence in the router.
- [x] 2.2 Remove `favorites_partial?` and both custom raw-header response branches. Use lazy favorites/playable_games/games props in the ordinary home pipeline, retain normal detail route and Session validation through the existing Games context with unchanged flat props, and make only independently deferrable schema calculation lazy. Retain ordinary detail not-found/Session-error handling and successful partial refresh response filtering; accept redundant catalog/BGG/schema evaluation under the revised test acceptance in task 4.1. Preserve provider fallback and DELETE mutation independence. Do not change runtime code or dependencies, add memoization/duplicate resolution, or reintroduce header workarounds for the test amendment.

- [x] 2.3 Resolve the restored favorites conflict on pinned master `577c5c34ed1fa5dbb873f90c99b3e747d2cfb0ee`. Retain all five master detail stories, including `HeaderFocused`, and the five meaningful favorites stories with matching descriptors/membership; remove redundant `FavoriteSavedDark`. Preserve master's styles and six-project theme/viewport matrix, `playable`, `SessionForm`, `InterestForm`, mock cleanup, and lazy props. Reconcile only affected Home/detail references into the theme/viewport hierarchy, retain recovery stash `416ebf6fa397b66e182aa9a662f5986083cc4696`, clear unmerged entries, and restore the originally empty index without losing unrelated files. Do not change backend behavior, dependencies, or screenshot infrastructure.

## 3. Client form baseline (feedback superseded by task 14)

- [x] 3.1 Historical completed baseline; its Session coupling is superseded by tasks 12.1/12.2 and its shared pending/result behavior by tasks 14.1/14.2. Replace the custom favorites membership/request store with reusable Inertia `<Form>` controls, server-prop-derived saved state, and one page-local pending/result object. Use the same query-free `favorite.action` for explicit PUT/DELETE, submit original `slug` and optional validated `session` only for PUT alongside safe current-page `response_to`, send only `response_to` for DELETE, remove redundant hidden `bgg_id`, and retain `only: ["favorites", "auth", "errors"]`, preserveState, and preserveScroll. Remove manual fetch, CSRF cookie code, abort/generation/reset/disposal logic, retry registry, and tests made obsolete by that transport. Update the shared descriptor, existing fixtures/mocks, unit/browser and authenticated Home/detail stories; verify identical action URLs under both methods, source-field serialization without `bgg_id`, same-game busy state, server confirmation, error feedback, same-control retry, and retained focus.
- [x] 3.2 Wire carousel stars to the shared form and server membership props while preserving current markup, visible loop copies, styling, CSS motion, focus reveal, and reduced motion. Adapt fixtures, Inertia Storybook mocks, semantic stories, and browser tests to form lifecycle and partial server prop updates; verify collections are retained and duplicate BGG controls update together.
- [x] 3.3 Historical completed baseline; its feedback behavior is superseded by tasks 14.1/14.2. Wire the detail hero to the same Inertia form; remove its favorites reset/dispose effects and router subscription. Extend game-page tests/stories for server prop updates, pending/error feedback, local/provider detail, retained launch/Session state, fallback images, and narrow layout.
- [x] 3.4 Wire guest and expired-session form activation to the existing account dialog with a safe current-page return path. Verify cancel/login/reload, refreshed auth/favorites props, no automatic replay or guest storage, and removal of favorites-specific account reset logic.
- [x] 3.5 Amend the existing Home card Grid and shared favorite control for fixed compact/Playable 2:1 and hero 2.9:1 ratios at every width, removing the mobile ratio switch, content/max-content lane-height growth, `--slide-basis`, and geometry alias variables. Inline CSS sizing expressions; let each card/variant own proportional padding and scale all visible content from its actual container through native units/queries. Use native flex wrapping in exactly one visible metadata chip row with `overflow: clip`: complete extra chips wrap below according to available footer width, without count caps, width thresholds, overflow detection, or JS. Only long primary chip text may ellipsize; prevent partial or ellipsized secondary chips in the first row. All noninteractive category text remains in the DOM and accessible to assistive technology, without `aria-hidden` for visual suppression. Keep the 44 by 44 transparent hit area independent of heading-sized pale/bright glyph alignment so it creates no visual inset mismatch or row growth. Preserve links/forms, semantics, image/fallback, native focused-card snapping, copies, CSS motion, and detail presentation with no JS sizing. Extend existing CompactMetadata semantic coverage only if useful; add no needless stories. Also reduce metadata-chip/status-badge font sizes from `0.65em` to `0.5em`, shrinking inherited em dimensions consistently without changing approved geometry or behavior. Also provide stable `0.75rem`, `max-content`, nowrap icon tooltips using feature-detected native anchor positioning/Popover and ordered below/above fallbacks, with a local absolute fallback that may clip at legacy carousel edges. Allow only small visibility handlers for hover/focus/Escape; preserve submission, accessible state, 44-pixel targets, copies, and detail presentation with no JS geometry or new dependency.

- [x] 3.6 Update only the shared favorite button's CSS so the unchanged SVG/path inherit gold stroke and `fill: none` from the parent button, with the existing saved-state selector adding the same gold fill. Apply this to Home and detail; remove brightness/opacity-only state differences, conflicting descendant paint, and star glow. Preserve geometry, detail text/box, 44-pixel targets, tooltips, focus, authoritative pending/error state, and form behavior. No new component, paint prop, icon swap, inline paint attributes/styles, or JS paint logic.

- [x] 3.7 Fit more Home title text using title-only CSS: smaller card-relative compact/Playable typography, inherited hero font, four lines normally and three below 12rem, and long-word wrapping. Preserve fixed ratios, footer, favorite size/target, full accessible names, links and motion.

## 4. Validation and rollout

- [x] 4.1 Replace the two partial-refresh tests' failure-on-BGG stubs with ordinary fixtures and remove the Home test's renamed games table; retain both tests and their add/remove, 303 redirect, private membership, authentication/errors, and omitted-prop assertions. Do not assert skipped catalog/schema callback execution or invocation counts. Preserve provider-independent DELETE and detail/Session coverage. Format touched Elixir files and rerun `mix test test/d20/games/favorites_test.exs test/d20/games_test.exs test/d20_web/controllers/game_favorite_controller_test.exs test/d20_web/controllers/page_controller_test.exs`. Record fresh results against this accepted response contract without skipping tests or changing runtime code/dependencies.
- [x] 4.2 After conflict resolution, run `bun run test:visual -- stories/pages/public/game.stories.ts stories/pages/public/home.stories.ts stories/pages/authenticated/home.stories.ts` from `assets/` across all six native projects. Review baseline, actual, and available diff images for every affected light/dark and desktop/tablet/mobile cell; narrowly update intentional references and rerun normal comparison. Expect 38 retained scenarios and 228 references, including 60 detail references; remove stale unthemed/theme-only references only in these paths. Preserve HeaderFocused and favorite semantic assertions. Run relevant shared form/game-page tests, Home browser tests, typecheck, lint, formatting, and Storybook build. Record #281's below-fold limitation without changing capture infrastructure; prior three-project evidence is historical.
- [ ] 4.3 Use `devtools-validations` against the actual changed worktree to verify desktop/mobile and both themes: star-only carousel appearance, hero button placement, 44-pixel hit targets, contrast/focus, confirmed saved/remove/error state without favorite announcements, same-BGG membership synchronization, no card navigation or collection replacement on favorite clicks, visible loop-copy actions, natural CSS looping/pause, and reduced-motion focus reveal. Record evidence in the owning change.
- [ ] 4.4 Run forward and reverse migrations on a disposable test database and record constraint/cascade results. Run fresh `just check` for the uniform route/projection/form cross-stack amendment before implementation acceptance, including frontend formatting, lint, tests, typecheck, Storybook build, and the repository lifecycle check. Do not reset shared development data.
- [ ] 4.5 Apply the additive migration before the matching backend/frontend release; deploy the uniform action/projection/form contract together without a backward route alias, verify save/remove/reload and guest behavior in the target environment, and record the application rollback path that retains favorite rows. Keep this task open until rollout evidence exists; dropping production favorites is not part of routine rollback.
- [x] 4.6 Validate tooltip hover/focus, Add/Remove, Escape, native edge placement and unsupported-feature fallback, same-control activation, pending/retry, loop copies, and reduced-motion focus. Review native viewports/both themes and restore the prepared responsive Dark Index page 6. Inspect tooltip-rendering coverage; run affected Home browser focus comparisons, shared favorite behavioral checks, and authenticated Home visual cells, including FavoritesPendingOverlap/FavoritesRetry and final-hover FavoritesSavedOverlap. Review and narrowly update only affected references, then rerun normal comparisons and native typecheck/lint/format checks. Carry forward approved geometry, chip typography, and natural motion evidence; no blanket 168-cell rerun, automated geometry/style assertions, unnecessary stories, or changes to detail references/#281/other gates.

- [ ] 4.7 Use `devtools-validations` to inspect inherited paint on the selected prepared worktree page without changing its route or user state. Review Home and detail outlined/filled states through the existing native screenshot matrix in both themes and viewports, including actual/fallback covers, pending/failure/retry states, focus, duplicate cards, and unchanged hit targets/layout. Inspect source and inherited SVG paint manually; reuse semantic tests without new geometry/style assertions. From `assets/`, run `bun run test:browser -- tests/shared/components/favorite_button.browser.test.ts tests/pages/home/ui/home.browser.test.ts`, `bun run test:unit -- tests/pages/game/ui/game.test.ts`, and `bun run test:visual -- stories/pages/public/game.stories.ts stories/pages/public/home.stories.ts stories/pages/authenticated/home.stories.ts <relocated-widget-story-paths>`. Review affected baseline/actual/diffs, narrowly update intentional star changes and rerun normal comparisons across the retained page and relocated widget stories plus affected browser references; the former 228 Storybook references describe the pre-extraction allocation. Run native scoped lint/format and `bun run typecheck`; record fresh results and the existing #281 capture limit in validation.md. Keep the four whole-feature gates open.

- [x] 4.8 Verify title fitting on the prepared CompactMetadata page across narrow/wide widths and both themes; review affected native Home visual references and rerun normal comparisons, existing Home/shared-favorite browser tests, scoped format/lint and typecheck. Record screenshot limits and evidence in validation.md; leave unrelated star and whole-feature gates intact.

## 5. Delivery reconciliation

- [ ] 5.1 Reconcile verified acceptance with issue #123 and any intervening game-interest changes. Synchronize the favorite deltas into authoritative specifications, archive this change only after implementation and rollout tasks are complete, run `openspec validate --all --strict --no-interactive` and `mix openspec.check`, and verify it no longer appears in `openspec list --json`. Include final delivery artifacts with the semantic implementation/finalization commit.

Current progress before local delivery preparation is 47/55: tasks 19.1/19.2 are verified for fresh page-owned authentication. Task 20 supersedes historical no-staging/no-commit restrictions under the user's explicit local commit and master-transfer authorization. The eight wider gates 4.3/4.4/4.5/4.7/5.1/6.3/7.2/8.2 remain open; Storybook/screenshot/rendered-review work remains waived for this bounded delivery. Local transfer and source retirement are verified in tasks 20.2/20.3; current progress is 50/58. The eight wider gates remain open, with evidence in validation.md.

## 6. Home widget extraction for unstaged review

- [x] 6.1 Historical completed extraction; its shared feedback ownership is superseded by tasks 14.1/14.2 and its production widget placement/public barrel by tasks 16.1/16.2. Extract production GameCard and GameCollection widgets with their owning CSS, lane derivation and loop copies; reduce Home to page composition and shared authentication/favorites feedback ownership. Preserve links, current card/title/image/metadata appearance, CSS container sizing, section naming, focus reveal, pause/looping and reduced motion. Preserve current working-tree functionality without losing prior changes.
- [x] 6.2 Move Playable counts and Games lane-partition cases to collection stories (the original high-count matrix is superseded by task 10.2); move title/image/fallback/metadata/status cases to card stories. Retain Home default/empty and conditional-section composition, OverlappingSingletons, auth dialogs and saved/pending/retry favorites overlap cases. Relocate semantic assertions, use production imports and deterministic fixtures, and reconcile only reviewed corresponding references under the existing six-cell matrix.
- [ ] 6.3 Validate the extracted working tree against its current source behavior and references. Run affected page/widget stories with `bun run test:visual -- <affected-story-paths>`, normal comparisons after reviewed scoped reference updates, existing `bun run test:browser -- tests/pages/home/ui/home.browser.test.ts` plus shared-favorite checks, scoped native formatting/lint, `bun run typecheck` and `bun run storybook:build` from assets. Use devtools-validations to inspect current-source composition, links, focus and natural CSS motion while preserving prepared OverlappingSingletons state. Record commands, actual story/reference counts, visual review and #281 limits in validation.md. Run strict owning-change validation and `mix openspec.check`; keep distinct whole-feature gates open.
- [x] 6.4 Reconcile this amendment's verified scope and acceptance with issue #123 and these artifacts. Verify that all changes are unstaged and no commit was created; preserve unrelated file contents. Keep the issue In Progress and add-game-favorites active while whole-feature gates remain open.

## Historical implementation milestones before accepting redundant adapter work

The restored detail-story conflict on master `577c5c34ed1fa5dbb873f90c99b3e747d2cfb0ee` is resolved, all unmerged entries are cleared, and the originally empty index is restored. Recovery stash `416ebf6fa397b66e182aa9a662f5986083cc4696` remains retained. Tasks 2.3 and 4.2 are complete: 38 meaningful Home/detail scenarios have 228 reviewed references exclusively in the theme/viewport hierarchy, with HeaderFocused preserved and FavoriteSavedDark removed. The scoped native update and subsequent normal comparison each passed all 228 tests; updated images exactly match the reviewed candidates. At integration completion, ten of fifteen tasks were checked. Tasks 3.5/4.6 remain complete for the tooltip correction. Tasks 1.2/3.1 remain verified for the uniform resource contract. Tasks 1.1/2.2 are verified for compact shared Session resolution in Games; current progress is twelve of seventeen. Latest focused Games/favorites/controller validation passed 140/142 with the two retained rc5 failures; formatting, scoped Credo and independent review passed. The following ID simplification results precede this amendment. The latest context/controller suite passed 25/26, including all 11 context cases, with the retained rc5 redirect failure. Final formatting, scoped Credo and independent review passed. The following create/delete results precede this amendment. The latest focused suite passed 96/98, including all 11 context cases; only the same two rc5 failures remain. Formatting, scoped Credo and independent review passed. The following context-extraction results precede this amendment. The latest focused backend suite passed 96/98 cases, including all 11 Favorites domain cases, with the two retained rc5 failures; formatting, scoped Credo and source review passed. Fresh validation passed 19 unit tests, 18 browser tests with two existing skips, and all 90 authenticated Home/detail visual cells without reference changes. Focused backend passed 85/87 with the two retained rc5 failures; broad validation passed 930/933 backend tests and reported the separate stage-constraint assertion plus three retained Credo findings. See `validation.md` for exact commands and limits. The previous smaller-chip amendment and its 168/16 passes remain recorded in `validation.md`; unchanged geometry/motion evidence carries forward. Current tooltip normal comparisons passed 30 Home visual cells and 18 browser tests with two existing skips, both exit 0; affected reference review and manual/source checks passed. Of 186 existing references, 21 changed and 165 are identical; details and corrected diagnostic failures are recorded in `validation.md`. Tasks 4.1, 4.3, 4.4, 4.5, and 5.1 remain open for backend contracts, full-feature browser coverage, broad checks, rollout, and synchronization/archive/commit respectively. The separate `delete-user-account` change is outside this task. No completion commit or archive is authorized by this revision.

## 7. Card title clipping and favorite contrast

- [x] 7.1 Correct clipped title ink and the gray shadow edge using CSS only. The initially implemented dark star contour is superseded by the flat treatment in task 8.1. Preserve line limits, font sizes, card/footer geometry, accessible controls and form behavior.
- [ ] 7.2 Review affected native theme/viewport screenshots, update only accepted references and rerun normal comparisons. Run scoped lint/format, typecheck and relevant browser tests; inspect the prepared changed-worktree page without changing its route/state. Record evidence and leave all changes unstaged without a commit.

## 8. Icon-sized target without tooltip

- [x] 8.1 Remove all card-favorite tooltip code and the redundant wrapper. Size only the icon variant to its unchanged SVG box without the former 44-pixel minimum or padding. Preserve the labeled detail button, focus, accessible state, forms and card composition. Replace the obsolete tooltip test with hover/focus-without-tooltip and keyboard-activation coverage. Remove the dimensional SVG shadows; add CSS-only gold fill on hover-capable pointer hover and keyboard focus-visible, restoring confirmed paint afterwards. Add isolated card hover/focus stories without duplicating page scenarios.
- [ ] 8.2 Verify button/SVG bounds and absent tooltip on the prepared changed-worktree page. Review affected native page/widget and browser screenshot candidates, narrowly update accepted references and rerun normal comparisons alongside focused favorite/Home browser tests, scoped lint/format and typecheck. Reconcile combined title/flat-hover/target verification with task 7.2 and issue #123; keep all work unstaged and broader feature gates open.

## 9. Favorite alignment with the first title line

- [x] 9.1 Share existing title typography in GameCard CSS so the icon button and SVG square equal the first title line height and share its horizontal centerline for single-line and wrapped compact/Playable and hero titles. Preserve computed preview padding, title font and 1.2 line height, wrapping/clamps, heading reservation, footer/card geometry, flat hover/focus, native form semantics, absent tooltip and detail presentation.
- [x] 9.2 Review the shared title font/line-height source contract for first-line center alignment and equal line/button/SVG height across compact/hero and short/wrapped titles; run scoped formatting/lint. Record actual prior attempted captures separately and the user's explicit waiver of further Storybook runs, screenshots and rendered review. Do not claim a visual pass or add geometry tests, stage, commit or close unrelated delivery gates.

- [x] 9.3 Add isolated GameCard stories for unsaved hover and authenticated saved compact/hero states, reusing common semantic content and favorite action/pressed assertions without page duplicates or geometry tests.
- [x] 9.4 Review the isolated card story source and semantic assertions: hover retains unpressed membership and both saved variants expose Remove with pressed state true. Run scoped lint/format and record the user's explicit waiver of Storybook execution and screenshot/reference review; report source/check evidence without claiming those rendered states passed. Keep unrelated delivery gates open.

## 10. Two compact Playable rows and smaller collection stories

- [x] 10.1 Update GameCollection so one through six Playable games use one compact row and more than six use two compact rows split contiguously at `ceil(n / 2)`, with seven yielding 4/3. Preserve Games hero/compact partition, empty/singleton behavior, compact card styling, canonical order, accessible row naming, favorites state, existing shared CSS cadence/direction/hover-focus pause, independent lane loops and reduced-motion focus reveal. Add no scripted carousel control, backend change or generic abstraction.
- [x] 10.2 Remove redundant Games 5/8/31/32 collection stories and replace Playable eight with four plus the minimum seven-game two-row example. Retain Hot 1-4, Playable 1/2/4/7, isolated unsaved hover and saved compact/hero card states, and all distinct page/favorites scenarios. Use four browse games in default public Home, authenticated Home and GameCollection composition; preserve larger shared browser fixtures and runtime selection limits. Update semantic story source to check exact row membership/order. Remove only the 30 screenshot references directly orphaned by the five removed high-count stories; do not run stories, generate screenshots, update retained references or perform fresh image review.
- [x] 10.3 Review source against the six/seven threshold, no-hero rule, lossless row order, shared pause/cadence and independent loops. Add and run focused non-screenshot semantic component checks for six in one row, seven in 4/3, eight in 4/4 and canonical keyboard order, justified by the user's Storybook execution waiver; do not add geometry/style/animation assertions. Run scoped native oxfmt/ESLint, `bun run typecheck`, `openspec validate add-game-favorites --strict --no-interactive` and `mix openspec.check`. Record exact outcomes and unverified rendered/motion behavior in validation.md, reconcile issue #123 and task 9 evidence, and retain unstaged changes with whole-feature delivery gates open.

The latest user instruction waives further Storybook runs, screenshots/reference updates, visual review and high-count motion demonstrations for tasks 9.2/9.4 and 10.1-10.3. Existing broader open tasks are not passed by this waiver; prior attempted captures remain historical evidence. This amendment does not authorize archiving, staging or committing the feature.

## 11. BGG Hot collection attribution

- [x] 11.1 Change Home's hidden h1 and visible provider collection heading to `Hot (by BGG)`. Make only the visible `by BGG` a native link to `https://boardgamegeek.com/hotness`, using `https://boardgamegeek.com` instead if no dedicated URL can be established. Historical prop interface, superseded by task 15: Home owns the destination and the reusable collection accepted a required href. Keep Playable unattributed, omit the visible provider section when empty, and preserve concurrent carousel/favorite work.
- [x] 11.2 Update affected heading, provider-link, card-link and keyboard semantic expectations. Run scoped native formatting/lint, TypeScript/Svelte checks and a focused semantic check without screenshot mutation; record exact results, URL-verification limitations and any pending rendered/reference review in validation.md. Reconcile issue #123 and the owning OpenSpec artifacts without staging, committing, archiving or closing the wider delivery.

- [x] 11.3 Match all browse GameCollection stories to Home's `Hot (by BGG)` heading and `/hotness` link; keep Playable attribution absent and preserve canonical game/lane assertions separately from the provider link. Remove the standalone Empty story, empty-only play branch and exactly six directly orphaned references; retain page-level empty behavior/coverage and existing remaining story IDs. Run scoped oxfmt/ESLint and typecheck, review the eight retained examples and reference inventory, and record results without Storybook execution or fresh screenshot/reference claims. Reconcile this same issue/change and preserve unstaged parallel work.

## 12. Favorites use game identity without runtime Session context

- [x] 12.1 Remove runtime Session from `PageController`'s favorite helper/descriptor, the shared TypeScript descriptor, favorite PUT form, HTTP delegation and the `Favorites.create` API. Expose only `create(user, bgg_id, slug)` and preserve `delete/2`. Use `list/1` without a compatibility alias to return all current-account `Favorite` entities, with `[Favorite.t()]` typespec and `[]` for nil. Remove the BGG ordering and ID selection from the account-scoped query. Map entities to numeric BGG membership explicitly inside both PageController lazy favorites props. Update existing entity assertions and compare multiple persisted favorites order-independently while preserving page numeric-prop tests. Remove the redundant private `resolve_game/1` wrapper and call `Games.fetch_by_slug/1` directly in the `create/3` with chain using a `String.t()` slug contract, without a type/emptiness guard or replacement validator. Remove the two obsolete context/controller malformed-slug validation tests without adding caller-error tests. Match BGG identity and handle returned source failures separately from a successful mismatched identity in else, retaining authentication-first behavior, source-error normalization, idempotency and returned persistence results. Remove the transient-error list, create/delete rescue and controller unavailable mapping; use direct Repo calls, preserve successful :ok/returned changeset results and let infrastructure exceptions reach the existing Phoenix error boundary without a new handler. Do not call `Games.resolve_session/2` or check visibility for favorite mutations; accept hidden local sources with matching identity and ignore extraneous Session input. Preserve detail GET visibility/Session rules, page-level Session props, return-page query, interest/launch controls, account/CSRF ownership and storage. Update affected context/controller/projection/form assertions and detail story fixtures, replacing Session-dependent rejection requirements without new stories or presentation changes.
- [x] 12.2 Historical completed validation; its feedback assertions and shared-pending browser filter are superseded by tasks 14.1/14.2. Run `mix test test/d20/games/favorites_test.exs test/d20/games_test.exs test/d20_web/controllers/game_favorite_controller_test.exs test/d20_web/controllers/page_controller_test.exs` plus touched-file `mix format --check-formatted`; for the `list/1` entity correction, run existing favorites context/controller and page-controller tests plus a reference scan, verifying full persisted entities, account isolation, nil and unchanged numeric page projections without assuming result order; carry forward unchanged frontend checks without new tests; for the helper and slug-guard removal, rerun the existing favorites context/controller files covering provider-failure, stale-identity, authentication and storage-error behavior; replace the existing storage-cancellation redirect test with both PUT and DELETE assertions of the standard Phoenix 500 error path; update the existing game-page failed-removal unit fixture to invoke its network-error responder instead of the removed storage form-error payload, retaining retry feedback and confirmed membership assertions, carrying forward the preceding four-file and unchanged frontend validation without duplicate tests; verify hidden/visible source success, returned source failures and stale-identity rejection, representative absent/nonexistent/malformed Session fields with source review confirming no Session lookup, unchanged DELETE and ordinary detail GET validation. New valid or mismatched runtime Session fixtures are unnecessary. From `assets/`, run `bun run test:unit -- tests/pages/game/ui/game.test.ts`, `bun run test:browser -- tests/shared/components/favorite_button.browser.test.ts -t 'shares pending|keeps confirmed|displays server' --browser.screenshotFailures=false`, scoped native formatting/lint and `bun run typecheck`; the browser filter selects three semantic tests per browser and excludes the explicit screenshot assertion. Verify no Session descriptor/form field even on WithSession detail and retained safe return query, without screenshot capture/reference updates or Storybook execution. Run `openspec validate add-game-favorites --strict --no-interactive` and `mix openspec.check`. Record exact outcomes and limitations in validation.md and reconcile issue #123; retain broader open gates with no staging, commit or archive.

## 13. Remove redundant Favorite numeric constraints

- [x] 13.1 Remove Favorite numeric-range validation and positive CHECK mapping plus the CHECK creation in the unpublished initial migration. Preserve integer storage, required fields, user foreign key/cascade, composite uniqueness, and ordinary game source identity behavior. Update the existing invalid-input and database uniqueness tests to match the removed numeric rules.
- [x] 13.2 Run focused favorites context/controller tests and scoped formatting, validate initial migration rollback/reapply on a disposable test partition, and run strict OpenSpec and native lifecycle checks. Record results and unchanged storage limits in validation.md and issue #123; keep broader delivery gates open without staging, commit or archive.

## 14. Remove favorites feedback and defer notifications

This amendment supersedes all earlier shared page-level pending coordination, same-game/all-page busy propagation, success/error announcements and feedback associations. Prior checked tasks and validation evidence describe their historical implementation only. Existing Storybook, screenshot/reference and rendered-review waivers apply to this amendment. Preserve existing index contents; do not stage, unstage, commit, archive or close the wider delivery.

- [x] 14.1 Delete `FavoriteFeedback` and `favorites_feedback.svelte`; remove unused imports, page-owned pending/result state, `feedback`/`feedbackId` props/bindings and result-based `aria-describedby` throughout Home, detail, GameCollection, GameCard and FavoriteButton. Use the existing Inertia Form children snippet's local `processing` for busy/disabled semantics, detail Saving/Removing labels and same-button repeat prevention with focus retained through `aria-disabled` and prevented default activation, without native `disabled`. Remove success/result/finish and custom HTTP/network notification callbacks, preserving the existing guest guard and authentication-error dialog. Keep confirmed favorites props, exact action/method/source fields, selective reloads, safe return path, visual star states, card links and backend error contracts. Expected favorite failures retain confirmed membership and permit retry without a notice; HTTP/network failures follow the standard adapter. Add no shared pending coordinator, store, context or replacement notification system. Update existing Home/detail/widget story source and semantic tests for local processing, confirmed membership synchronization, focus and failure/retry without notifications; retain existing story IDs and use the mocks' existing processing snippet without infrastructure changes when sufficient.
- [x] 14.2 Run scoped native oxfmt/ESLint and `bun run typecheck` from assets; run `bun run test:unit -- tests/pages/game/ui/game.test.ts` and focused favorite/collection browser semantic checks with `--browser.screenshotFailures=false`, filtering out every screenshot assertion. Verify own-button repeat prevention and focus retention, no shared busy propagation, confirmed prop synchronization, failure/retry without favorite notices, guest/expired-auth behavior and unchanged action/fields/partial reloads. Review affected story source without executing Storybook, generating/updating screenshots or claiming a rendered pass. Scan production references to confirm feedback type/renderer/props/IDs and notification callbacks are removed. Run `openspec validate add-game-favorites --strict --no-interactive` and `mix openspec.check`; record actual evidence and the unavailable live-page limitation in validation.md and issue #123. Keep tasks 4.3/4.4/4.5/4.7/5.1/6.3/7.2/8.2 open with no staging, commit or archive.

## 15. Compose collection headers through snippets

- [x] 15.1 Replace GameCollection title/headingAttribution/headingId props with required header Snippet content, generated heading ID and generic numbered compact row labels. Supply named snippets from Home, retaining heading/link styling with scoped snippet-aware CSS. Update existing collection stories and compact-row browser fixtures/assertions to pass static snippets, preserving story IDs and all other behavior.
- [x] 15.2 Run scoped formatting/ESLint, TypeScript/Svelte checks, existing Home header/link/empty/focus and collection row-order browser tests without screenshots. Review updated story source without Storybook execution or baseline changes, verify old heading props are absent, and reconcile evidence in validation.md and issue #123. Run strict OpenSpec and lifecycle checks; retain the wider eight open gates and unchanged index without commits/archive.

## 16. Place Home components and favorite interaction in their owning slices

This amendment supersedes the historical production widget placement without rewriting completed extraction/validation records. Preserve the existing index and all screenshots; retained legacy story/favorite-test paths are verification infrastructure, not production FSD ownership. Existing Storybook/screenshot/rendered-review waivers remain effective. Keep the eight unrelated gates 4.3/4.4/4.5/4.7/5.1/6.3/7.2/8.2 open without staging, unstaging, commits or archive.

- [x] 16.1 Move `assets/js/widgets/game-collection/ui/game-collection.svelte` and `game-card.svelte` to `assets/js/pages/home/ui/`, use relative default imports inside Home and remove the old widget barrel with no compatibility alias or new Home public exports. Move `assets/js/shared/components/favorite_button.svelte` to `assets/js/features/toggle-game-favorite/ui/favorite-button.svelte`, add its feature `index.ts` exporting named `FavoriteButton`, and switch GameCard and the game page to that feature public API. Move the screenshot-free collection semantic test to `assets/tests/pages/home/ui/game-collection.browser.test.ts` and update its import. Update only production imports in the two existing `assets/stories/widgets/game-{card,collection}.stories.ts` files; retain their paths, titles, IDs, decorator, fixtures and assertions. Keep `assets/tests/shared/components/favorite_button.browser.test.ts` unchanged at its current path and preserve every screenshot file. Correct only the stale Home browser test h1 expectation to the existing `Games` page heading, retaining visible `Hot (by BGG)` attribution. Preserve props, markup, CSS, snippet/row naming, carousel behavior, auth, local processing and mutation contracts. No persistence gate, migration, runtime or backend change applies.
- [x] 16.2 From assets, run `bun run test:unit -- tests/pages/game/ui/game.test.ts` and `bun run test:browser -- tests/pages/home/ui/game-collection.browser.test.ts tests/pages/home/ui/home.browser.test.ts tests/shared/components/favorite_button.browser.test.ts -t 'keeps.*playable|attributes Hot|keeps processing local|keeps confirmed' --browser.screenshotFailures=false`, excluding screenshot assertions. Run scoped native oxfmt/ESLint and `bun run typecheck`; require product/new files to pass formatting and preserve the characterized pre-existing formatting failure set in the four existing test/story files without unrelated churn, recording that limitation. Verify imports resolve through page-local components and the feature public API, no old production paths/barrel or compatibility exports remain, component bodies retain current behavior, story identities and screenshot files are unchanged, and the pre-existing index is preserved. Review story source without Storybook execution or visual claims. Run `openspec validate --all --strict --no-interactive` and `mix openspec.check`; record actual results and the characterized pre-edit h1 assertion mismatch in validation.md and issue #123, leaving the wider eight gates open and the change active without staging, commits or archive.

## 17. Use one icon-only favorite control and keep messages in the view

This amendment supersedes earlier labeled-detail, visible Saving/Removing, detail-box and detail-specific target requirements. Keep historical checked tasks unchanged, the existing Storybook/screenshot/rendered-review waiver and index intact, and the eight wider gates 4.3/4.4/4.5/4.7/5.1/6.3/7.2/8.2 open. No backend/persistence changes, staging, commits or archive.

- [x] 17.1 In `assets/js/features/toggle-game-favorite/ui/favorite-button.svelte`, remove the visible label span, variant prop/type/default/modifiers, detail-only box CSS and unused `$derived` UI action/label variables. Collapse existing icon styles into base button/star selectors. Put complete Add/Remove messages directly in markup `aria-label`, interpolating optional title in each complete sentence with generic fallback. Keep title optional and stable while processing, retain detail's `title={game.name}` and remove only its `variant="detail"` input. Preserve `favorite.action`, request fields/options/methods, auth, server saved state, local processing, aria-pressed/busy/disabled, focus and failure/retry. Update nearest existing detail unit tests and story assertions for title-aware accessible names and busy state instead of Saving/Removing text, including no-visible-label checks within existing tests; preserve story IDs and introduce no test files or screenshot changes.
- [x] 17.2 Run affected-file native oxfmt/ESLint, `bun run typecheck`, `bun run test:unit -- tests/pages/game/ui/game.test.ts` and `bun run test:browser -- tests/shared/components/favorite_button.browser.test.ts -t 'keeps processing local|keeps confirmed' --browser.screenshotFailures=false` from assets, excluding screenshot assertions. Require production formatting to pass without widening pre-existing test/story formatting failures or unrelated churn. Verify one icon-only API, complete markup-owned accessible messages with title/fallback, stable pending action and busy state, absent visible labels/derived UI copy, unchanged `favorite.action` and preserved save/remove/guest/failure/retry behavior. Review story source without execution or screenshots. Run `openspec validate --all --strict --no-interactive` and `mix openspec.check`, record evidence in validation.md and issue #123, and retain the active change, unchanged index and eight wider gates without staging, commits or archive.

## 18. Name the feature Favorite Game

Earlier task/validation paths are historical and superseded by this rename. Preserve the existing index, screenshots and eight wider open gates without staging, commits or archive.

- [x] 18.1 Rename `assets/js/features/toggle-game-favorite` to `assets/js/features/favorite-game` and update GameCard/detail public imports. Preserve `FavoriteButton`, component contents and all behavior; remove the old slice without an alias.
- [x] 18.2 Verify scoped native formatting/lint, existing detail unit tests, typecheck, reference/content checks, strict OpenSpec validation and `mix openspec.check`; reconcile validation.md and issue #123 without new tests, Storybook or screenshots.

## 19. Read authentication inside Favorite Game

Earlier authentication-prop wiring is superseded without rewriting completed milestones. Preserve `features/favorite-game`, current index, screenshots/Storybook waiver and the eight wider gates 4.3/4.4/4.5/4.7/5.1/6.3/7.2/8.2. No backend/persistence work, new test files, staging, commits or archive.

- [x] 19.1 Remove `authenticated` props/defaults/bindings from FavoriteButton, GameCollection, GameCard and Home/detail favorite consumers. Retain FavoriteButton's favorite/saved/title/tabindex and usePage; read `page.props.auth.authenticated` directly inside beforeSubmit on every attempt without captured/derived auth, new store/provider or fallback. Remove Home/detail auth destructuring only when made unused; preserve independent SessionForm/InterestForm behavior. Initialize actual mocked page auth through existing test/story setPage/set APIs, remove obsolete card-story authenticated args and isolate/reset story context without changing paths/IDs. Extend the existing game auth test to prove guest -> signed in -> guest changes through page-only updates on the same mounted control, without rerendered auth props, retaining authentication-error flow, no replay and request/membership checks. Preserve form contract, icon UI, saved/pending/failure/retry and focus; add no test files or broad mock changes.
- [x] 19.2 Run scoped native oxfmt/ESLint, `bun run typecheck`, `bun run test:unit -- tests/pages/game/ui/game.test.ts` and `bun run test:browser -- tests/shared/components/favorite_button.browser.test.ts -t 'keeps processing local|keeps confirmed' --browser.screenshotFailures=false` from assets. Verify all existing detail cases and three semantic favorite cases per browser, direct fresh auth reads and absent obsolete favorite auth props/story args, page-only auth transitions, guest/expired-auth handling and preserved requests/membership/focus. Require product formatting to pass while retaining characterized pre-existing test/story formatting debt without broad churn. Review story-context setup/reset without Storybook or screenshots; verify existing index, feature path, story IDs and screenshot files unchanged. Run `openspec validate --all --strict --no-interactive` and `mix openspec.check`; reconcile actual evidence in validation.md and issue #123 with eight wider gates open and no staging, commits or archive.

## 20. Commit Favorite Game and transfer it to local master

- [x] 20.1 Prepare the owning feature for local delivery: apply native formatting to the nine owned tests/stories, fix the Home auth fixture through current page context, replace Favorites.create/3 single-clause with by equivalent case branches, and record scoped and broad validation with actual limits. Synchronize the implemented game-favorites, game-detail and home-game-discovery deltas into authoritative specifications, preserving unrelated contracts, and pass strict OpenSpec validation without archive.
- [x] 20.2 Commit the owning Favorite Game code, tests, migration and reconciled delivery artifacts together. Pin local master, rebase and validate the candidate as needed, fast-forward the clean target, and record successful post-transfer validation and the commit in this change and issue #123. Do not publish, deploy, close the issue or archive while wider gates remain open.
- [x] 20.3 After successful integration, remove the source worktree and local candidate branch only when safe for existing user/runtime data. Retain both while the pre-existing PostgreSQL data directory and active editor/services occupy this worktree; record that safety limitation without force deletion or a cleanup completion claim.

## 21. Reconcile Favorites visual checks with approved rendering

- [x] 21.1 Correct evidenced stale semantic expectations in the existing Favorites-owned stories, starting with public Home's `Games` h1 and absent favorites status region while retaining `Hot (by BGG)` h2/link checks. Characterize the existing Home browser test's final-Tab boundary failure with a normal rerun; if confirmed, use the neighboring 32-entry test's explicit `After games` sentinel and cleanup pattern to provide a defined next focus target. Retain every canonical link/button traversal assertion and screenshot state, and assert actual focus leaves the collection for that sentinel rather than removing the exit assertion. Correct the subsequently reproduced public and authenticated Home pointer-state leaks through each file's existing story-local beforeEach using native Vitest `userEvent.unhover(document.body, { position: { x: 0, y: 0 } })`, guarded by the existing `import.meta.env.VITEST` convention; the provider position selects a neutral body origin instead of its possibly interactive center, without DOM geometry queries. Retain intentional hover/focus interactions. Require all 84 combined Home cells (54 public and 30 authenticated) to pass normal comparison before the full normal run, without updating baselines to accept leaked hover. Preserve meaningful assertions, existing scenario IDs, fixture intent, production source and all .storybook/test-performance/configuration/package/lock files. Characterize failures before correction; no new skips, test removal or screenshot tolerance changes.
- [x] 21.2 Run the existing native visual matrix for public Home/Game, authenticated Home and GameCard/GameCollection stories, plus Home/favorite browser comparisons in Chromium and Firefox. Review every affected existing/new reference candidate and available diff, update only accepted current states in their existing paths, then pass normal scoped comparisons. Record current scenario/reference counts and the existing #281 capture limit; preserve unrelated references and all six theme/viewport cells.
- [x] 21.3 Run ordinary full `bun run test` from assets without update mode or configuration overrides and require zero failures with only existing skips. Run scoped native oxfmt/ESLint and `bun run typecheck`; record exact commands, counts, elapsed duration, any shutdown warning and protected-source/configuration checks in validation.md. Reconcile issue #123 and these artifacts, then pass `openspec validate --all --strict --no-interactive` and `mix openspec.check`. Reuse this evidence for older visual clauses only where it directly satisfies them; keep remaining manual/DevTools, build/motion, cross-stack, rollout and archive gates explicit and open.
